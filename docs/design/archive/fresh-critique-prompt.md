# TASK: Fresh-eyes internal critique of ArkLib's oracle-reduction design document

Read /Users/quangdao/Documents/Lean/ArkLib-Oracle-Reduction-Design.md fully — a design that has been through three audit rounds. Your job is NOT to re-litigate the settled points (behavioral carrier, AcceptedRun closing, subst-not-bind, Γ/Δ split — those are decided). Your job is the NEXT level of critique:

1. INTERNAL CONSISTENCY: contradictions between sections; places where a later section silently changes an earlier definition's signature; places where §10's dispositions don't match what §6 actually says; dangling references (e.g. duplicated paragraphs — check §6.5 which appears to state the left-associated-sums paragraph twice); notation drift (OracleClaim's Src is OracleSpec in some places, SourceCtx in others — check every signature for such mismatches).

2. UNDERSPECIFIED LOAD-BEARING OBJECTS: which objects that theorems depend on are still just names? Candidates to check hard: ClaimSchema (never defined); the "joint execution result" runClosed's actual type over transcripts+worlds+distributions (Dist? PMF? subprobability for faults?); AcceptedRun vs transcript-indexed reality; challenge "kernels" and "reachable prefixes"; SourcesAt p and prefix weakening; ViewReduction; the Terminal functor's interaction with Dist; how `stmt` being computed from oracle queries interacts with ClosedClaim equality (is stmt deterministic given env?); FiniteConsumer; ConsistencyCompiler; LinkArgument; StagedOpeningProtocol; Ownership; EncodesPromise. For each: is it (a) fine to leave open, (b) needs a sketch now because a §8 migration step depends on it, or (c) hides a real design decision?

3. CONSOLIDATION: the doc has grown to 1288 lines with three audit layers. Where is there redundancy to merge (e.g. §2 ontology vs §6.1 overview vs §7 Q&A restating the same points)? Which of the 15 success criteria are actually the same criterion? Could §6.10's compiler treatment be split into its own document? Is the audit-traceability section (A-W findings) still earning its place or should it move to an appendix/archive?

4. MISSING "A-HA"s: given everything in the doc, what natural unifications has it not noticed? Candidates to evaluate (agree/refute with reasons, and add your own):
   a. Is `OracleClaim`/`ClosedClaim` secretly the same pattern as the prover's HonestProverOutput (data + witness) — i.e., is there one "claim functor" with two evaluations (honest data / behavior)?
   b. Fiat-Shamir and BCS both consume "public-coin + replayable" structure; the doc treats FS as a separate pass — is there a shared "transcript compiler" substrate?
   c. The Γ world semantics (step/State/Trace) looks like it wants to be VCVio's simulation oracles (QueryImpl into StateT) — is the doc reinventing VCVio machinery that already exists, and should it name the identification?
   d. RBR state functions, KnowledgeClaimTree, and special-soundness trees all quantify over "constrained prefix trees" — is there one tree object with three decorations?
   e. subst + rebase + tensor + share: is this just the free cartesian/monoidal structure on SourceCtx, and would stating it as such (a category with explicit associators) actually reduce Lean work or increase it?
   f. Is `admissible` on Problem the same mechanism as the "promise" in promise problems AND the "well-formedness of accumulators" in folding — one name for all three?
5. MIGRATION REALISM: 11 steps, each gated. Which steps hide months of work behind one sentence? Is the ordering right given that ProofSystem/ ports (sumcheck, Spartan, FRI) currently build against the OLD security definitions? What breaks for downstream users during steps 6-7? Is there a smaller "minimum viable slice" that gets ONE end-to-end sorry-free result (e.g. single-round sumcheck completeness through the new claim layer) earlier than the plan implies?

6. LEAN-SPECIFIC FEASIBILITY spot-checks: `OracleFamily.Behavior := QueryImpl [Out.Obj]ₒ Id` — check against actual VCVio QueryImpl definition in the repo (.lake/packages or VCV-io worktree at /Users/quangdao/Documents/Lean/VCV-io) for universe/implicit issues; `SourceCtx.tensor` needs OracleSpec sum + QueryImpl.add — verify they exist with the right associativity lemmas; `Spec.OracleMessagesAt` — any positivity/universe issue building it by recursion on Spec : Type 1? Does `AcceptedRun` as stated even typecheck given transcript dependence (claim's Src depends on pt which depends on the run)?

Output: markdown, severity-ranked findings with section references, then a concrete list of "edits I would make" (title-level, not full rewrites). Be direct. Final answer = the complete report.
