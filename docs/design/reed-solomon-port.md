# Reed–Solomon beyond-Johnson port ledger

This ledger records the review units of the port tracked by
[issue #907](https://github.com/Verified-zkEVM/ArkLib/issues/907): the six first-tranche units
below and the later slices listed after them. Commit
`a5aa2677fee4e3a79d6bb05136631cce4a08587d` is the immutable overarching paper-port snapshot and
the direct source for the weighted-support, exact-list, agreement-list, and pairwise-Johnson units,
and for every later slice unless its entry names another source.
The fraction-field resultant and exact weighted-product units are sourced from their immutable
pull-request donor heads below; those theorem families are not present in `a5aa2677`. Donor heads
are source evidence, not merge bases. Final acceptance uses the main branch containing the Lean
4.34 migration from pull request #903.

Per-file source correspondence (old names, generalizations, deferred declarations) is in
[`reed-solomon-port-correspondence.md`](reed-solomon-port-correspondence.md); module docstrings
describe only the present API, as [`docs/wiki/porting-conventions.md`](../wiki/porting-conventions.md)
requires.

Current heads, hosted CI, migration gates, and landing status are maintained in
[issue #907](https://github.com/Verified-zkEVM/ArkLib/issues/907), the canonical live dashboard.

The immutable donor heads for the refreshed pull requests are `f37f25ba3d0d6701f054c00cb53a7b73d363c0d0`
(#857), `8b1698ab6f73d89bcc36b7936a8ce87cd20bc9d4` (#877), and
`0ffecb528a8a63eb9522b68d7061e0b671339a47` (#875).

| Unit | Source declarations | Destination owner | Dependencies and overlap |
| --- | --- | --- | --- |
| Weighted support and substitution (#857) | `restrictWeightedDegree` and the exact inventory below; `MvPolynomial.aeval_shift_mem_restrictDegree` consumer in `EvenAndOdd.lean` | `ArkLib.Data.MvPolynomial.WeightedDegree`; acceptance canaries in `ArkLibTest` | Independent. Does not own exact product or divisor equalities. |
| Fraction-field resultant (#877) | `resultant_derivative_ne_zero_of_separable`, `isCoprime_map_of_resultant_ne_zero`, `separable_map_of_resultant_derivative_ne_zero`, `resultant_derivative_ne_zero_of_fractionField_separable`, `resultant_derivative_ne_zero_of_separable_map_fractionField` | `ArkLib.Data.Polynomial.FractionFieldResultant` | Independent. Reuses Mathlib's padded resultant identity and fraction-ring injection. |
| [Exact-list candidate filtering (#908)](https://github.com/Verified-zkEVM/ArkLib/pull/908), replacing #855 | `MessagePolynomial`, `Decoder`, `IsExactDecoder`, `DecoderCertificate`, `CandidateCertificate`, embeddings and filtered-decoder laws, exactness and oversized-threshold consequences | Generic exact finite-enumeration and candidate-filtering interfaces in `ArkLib.Data.Finset.Enumeration`; Reed–Solomon predicates and adapters in `ArkLib.Data.CodingTheory.ReedSolomon.ListSpecification` | Independent. Replaces admitted hidden-derivative contracts with proved extensional interfaces; makes no running-time claim. |
| [Agreement-list finiteness and incidence (#909)](https://github.com/Verified-zkEVM/ArkLib/pull/909) | `closePolynomialSet`, `exists_closePolynomial_finset_with_incidence_bound`, `closePolynomialSet_finite` | Generic sample-incidence bounds in `ArkLib.Data.Finset.SampleIncidence`; arbitrary-code adapter in `ArkLib.Data.CodingTheory.ListDecodability.SampleIncidence`; Reed–Solomon specialization in `ArkLib.Data.CodingTheory.ReedSolomon.AgreementList`, including the added direct set corollary `closePolynomialSet_ncard_mul_choose_le` | The generic layers are independent. The Reed–Solomon specialization uses `ReedSolomon.polynomialAgreementSet` from #906. The source declarations `agreeingPolynomials`, `agreeingPolynomials_antitone`, `exists_finset_polynomial_list`, and `agreeingPolynomials_eq_empty_of_card_lt` are deferred until an exact-list consumer needs them. |
| [Pairwise Johnson counting and code bound (#910)](https://github.com/Verified-zkEVM/ArkLib/pull/910) | source-private `card_mul_johnsonDenominator_le`; `closePolynomialSet_finite_and_ncard_le_johnsonPairwise` | `Finset.card_mul_sq_sub_card_mul_le_of_inter_card_le` in `ArkLib.Data.Finset.PairwiseIntersection`; arbitrary-alphabet finite-family, complete-set, `Lambda`, and `IsListDecodable` results in `ArkLib.Data.CodingTheory.JohnsonBound.Pairwise`; thin Reed–Solomon specializations in `ArkLib.Data.CodingTheory.ReedSolomon.ListDecodability.PairwiseJohnson` | The generic Johnson layer does not depend on agreement-list incidence. Only the Reed–Solomon polynomial specialization imports `AgreementList`. Keeps the sharp `n * (A - D)` numerator and avoids finite-alphabet, MCA, or geometric hypotheses. |
| Exact weighted products and divisors (#875) | `weightedTotalDegree_mul`, `weightedTotalDegree_prod`, `weightedTotalDegree_le_of_dvd`, `sum_weightedTotalDegree_le_of_prod_dvd` | `ArkLib.Data.MvPolynomial.WeightedDegree.Products` | Independent main-based module with direct Mathlib imports. Complements #857 without adding the same file. |

Each final head must pass `./scripts/validate.sh --axioms` without new admissions, native trust,
policy suppressions, or unauthorized dependency-pin changes. Before landing, refresh main, adapt against the merged
Lean 4.34 dependency APIs, rerun the full gate, and record the reviewed head in the pull request.

## Later slices

Each row is one bounded slice of a work package from issue #907. A package is not complete until
its remaining units, listed under deferred scope, are ported or explicitly retired.

| Unit | Source declarations | Destination owner | Generalization and deferred scope |
| --- | --- | --- | --- |
| [P1 slice 1: partial-derivative and weighted-degree laws (#921)](https://github.com/Verified-zkEVM/ArkLib/pull/921) | `degreeOf_pderiv_le_sub_one`, `degreeOf_pderiv_le`, `pderiv_ne_zero_of_degreeOf_pos_of_lt_ringChar`, `degreeOf_pderiv_eq_sub_one_of_lt_ringChar`, `iteratePDeriv`, `degreeOf_iteratePDeriv_le`, `degreeOf_iteratePDeriv_eq_sub_of_lt_ringChar`, `iteratePDeriv_ne_zero_of_lt_ringChar` in `ToMathlib/MvPolynomial/PDeriv.lean`; `pderiv_ne_zero_and_degreeOf_eq_sub_one_of_natCast_ne_zero` in `HiddenDerivative/Interpolation/FirstOrder/HybridDescent.lean`; `weightedTotalDegree_pderiv_le_sub`, `weightedTotalDegree_pderiv_le`, `natDegree_differentialSpecialization_le` in `HiddenDerivative/RootFinding/DegreeBounds/SpecializationDegree.lean` | `MvPolynomial.degreeOf_pderiv_le_sub_one`, `degreeOf_pderiv_le`, `coeff_pderiv_sub_single_one`, `pderiv_ne_zero_of_natCast_ne_zero`, `degreeOf_pderiv_eq_sub_one_of_natCast_ne_zero`, `degreeOf_iterate_pderiv_le_sub`, `degreeOf_iterate_pderiv_le`, `pderiv_eq_zero_of_degreeOf_eq_zero`, `iterate_pderiv_eq_zero_of_degreeOf_lt`, `degreeOf_iterate_pderiv_eq_sub_of_natCast_ne_zero`, `iterate_pderiv_ne_zero_of_natCast_ne_zero`, and `natCast_ne_zero_of_ringChar_eq_zero_or_lt` in `ArkLib.ToMathlib.MvPolynomial.PDeriv`; `MvPolynomial.weightedTotalDegree_pderiv_le_sub`, `weightedTotalDegree_pderiv_le`, `pderiv_mem_restrictWeightedDegree`, `natDegree_aeval_le_weightedTotalDegree`, `natDegree_aeval_le_weightedTotalDegree_of_le` in `ArkLib.Data.MvPolynomial.WeightedDegree` | The source's `0 < degreeOf i p`, `degreeOf i p < ringChar R` and `[Nontrivial R]` become one cast hypothesis `(degreeOf i p : R) ≠ 0` with `[NoZeroDivisors R]`, which covers characteristic zero; `natCast_ne_zero_of_ringChar_eq_zero_or_lt` converts the old guard. The iterate hypothesis names only the `a` differentiated degrees, so it also covers degrees above the characteristic (for example one derivative of `X^6` over `ZMod 5`) and implies `a ≤ degreeOf i p`; nonvanishing of iterates takes `p ≠ 0`. Iterates are Mathlib's `(pderiv i)^[a]` instead of the source's `iteratePDeriv`. The weighted-derivative bounds hold for any variable type, not only jet variables, and the univariate-substitution bound is the general form behind `natDegree_differentialSpecialization_le`. Deferred: the `_of_lt_ringChar` wrappers (consumers compose with the guard lemma), the jet-specific specialization statement and separant corollaries (P1 PR2), and the root-counting wrappers. |
| [P1 slice 2: differential-polynomial core (#923)](https://github.com/Verified-zkEVM/ArkLib/pull/923) | `JetVariable`, `DifferentialPolynomial`, `differentialSpecializationHom`, `differentialSpecialization`, `jetEvaluation`, `polynomialJet`, `jetDegree`, `separant`, `differentialWeight`, and `differentialWeightedDegree` in `Data/Polynomial/Differential/{Types,Basic}.lean`; `natDegree_differentialVariable_le`, `natDegree_differentialSpecialization_le`, and the two separant specialization bounds in `HiddenDerivative/RootFinding/DegreeBounds/SpecializationDegree.lean`; `jetTotalDegree`, `jetTotalDegree_le_iff`, `jetDegree_le_total`, and `separant_total_le` in `HiddenDerivative/RootFinding/Counting/TotalJetDegreeRootCount.lean`; exponent-level `totalJetDegree` in `HiddenDerivative/Interpolation/Space.lean` | `ArkLib.Data.Polynomial.Differential.Types` owns the shared variable and polynomial abbreviations; `.Basic` owns the specialization homomorphism, generator equations, scalar-jet evaluation, and evaluation comparison; `.JetDegree` owns individual, specialization-weighted, exponent-level, and polynomial total-jet degrees and their bridges. Ordinary-import clients are in `ArkLibTest.Data.Polynomial.Differential`. | The specialization and degree upper bounds hold over any commutative semiring and need no characteristic guard. The exponent-level `totalJetDegree` moves forward from P3 into this generic owner and is definitionally built from the same `jetDegreeWeight` as polynomial `jetTotalDegree`; `totalJetDegree_eq_degree_some` preserves the source interpolation spelling. Zero specialization weights remain valid but do not imply finite-dimensional support. Exact separant nonvanishing continues to use P1 slice 1's cast hypotheses. Deferred: bounded solutions, active/highest jets, regular jets, characteristic certificates, Taylor/contact/multiplicity/descent, partial jet fibers, and every root-counting theorem. |
| [P1 slice 3: finite Taylor prefixes (#928)](https://github.com/Verified-zkEVM/ArkLib/pull/928) | `centeredCoefficientPrefix`, `taylor_centeredCoefficientPrefix`, `coeff_taylor_centeredCoefficientPrefix`, `degree_centeredCoefficientPrefix_lt`, `polynomialJet_centeredCoefficientPrefix`, and `centeredCoefficientPrefix_succ` in `HiddenDerivative/RootFinding/Taylor/Numerator.lean` | `Polynomial.centeredCoefficientPrefix`, its translated-coefficient, Hasse-coefficient, strict-degree, exact-jet, shorter-jet, zero, and successor laws in `ArkLib.Data.Polynomial.TaylorPrefix`; ordinary-import clients in `ArkLibTest.Data.Polynomial.TaylorPrefix` | The construction and all laws are generalized from fields to commutative rings and are stated directly with the generic Hasse-jet API. The successor law exposes the canonical added term `C (c K) * (X - C center) ^ K`, rather than depending on the root-finding-only `regularLiftCandidate`. There are no factorial divisions or characteristic hypotheses. Deferred: the thin `PolynomialDifferential.polynomialJet` adapter, single-coefficient perturbation API, rational Taylor numerators, separant denominators, residual comparisons, and every regular/singular root-finding consumer. |
| [P1 slice 4: rational Taylor numerators and separant denominators (#936)](https://github.com/Verified-zkEVM/ArkLib/pull/936) | `supportWeightLE` and its substitution/coefficient lemmas in `ToMathlib/MvPolynomial/SupportWeight.lean`; `clearedSubstitution` and its ring-hom, map, evaluation, and degree laws in `ToMathlib/MvPolynomial/ClearedSubstitution.lean`; `taylor_denominator_weight_le` in `HiddenDerivative/RootFinding/Taylor/Denominator.lean`; the universal Taylor jet and residual in `.../Taylor/Residual.lean`; `TaylorExponentSufficient` and its instances, `initialJetSeparant`, `rationalTaylorNumerator`, `totalDegree_rationalTaylorNumerator_le`, `rationalTaylorCoefficient`, `_initial`, `_residual`, and the induction inside `rationalTaylorCoefficient_eq_solution` in `.../Taylor/Numerator.lean` | `MvPolynomial.supportWeightLE` (built on Mathlib's `restrictSupport`), `aeval_mem_supportWeightLE`, `weight_le_of_mem_coeff_optionEquivLeft`, `weightedTotalDegree_coeff_optionEquivLeft_le`, `Finsupp.weight_two_mul_sub_one_le` in `ArkLib.ToMathlib.MvPolynomial.SupportWeight`; `MvPolynomial.clearedSubstitution` and its laws in `ArkLib.ToMathlib.MvPolynomial.ClearedSubstitution`; `PolynomialDifferential.universalTaylorJet`, `universalTaylorResidual`, its support-weight, denominator-budget, degree, and evaluation laws in `ArkLib.Data.Polynomial.Differential.TaylorResidual`; `initialJetSeparant`, `rationalTaylorNumerator`, `rationalTaylorCoefficient`, `rationalTaylorCoefficient_residual{,_prefix}`, `eq_rationalTaylorCoefficient_of_residual`, `TaylorExponentSufficient` in `ArkLib.Data.Polynomial.Differential.RationalTaylor`; acceptance cases in the matching `ArkLibTest` files | The denominator budget holds for any index type and weight, without `0 < h`. The cleared substitution lands in any commutative semiring. The numerator degree bound drops the source's `0 < jetTotalDegree Q`, and `taylorExponentSufficient_two_mul_sub_three` drops `2 ≤ K`. Uniqueness is stated for any coefficient sequence solving the affine equations, separated from the solution property; the separant and binomial-pivot hypotheses are both shown necessary in the docstrings. Deferred: `solution_taylorCoefficient_residual` and `rationalTaylorCoefficient_eq_solution`, which need the one-coefficient perturbation and contact lemmas from `RootFinding/Regular/{Lifting,Iteration}.lean`. |
| [P1 slice 7: derivative descent and singular recursion (#963)](https://github.com/Verified-zkEVM/ArkLib/pull/963) | `IsBelowCharacteristic`, `jetDerivative`, `derivativeDescent` and its degree and nonvanishing lemmas in `Data/CodingTheory/ReedSolomon/HiddenDerivative/DerivativeDescent.lean`; `SingularStep`, `jetDegreeMeasure`, `singularStep_wellFounded`, the bounded-solution branches, the terminal-equation lemmas, `RegularRecursionLeaf` and `exists_regularRecursionLeaf` in `.../RootFinding/Regular/SingularRecursion.lean` | `JetDegreeCastsNeZero{,.mono,.natCast_jetDegree_ne_zero}`, `jetDegreeCastsNeZero_of_ringChar`, `jetDegree_separant_{le,le_sub_one,eq_sub_one}`, `separant_ne_zero`, `jetDerivative{,_eq_iterate,_zero,_succ,_one,_ne_zero}`, `jetDegree_jetDerivative_{le_sub,le,eq_sub}`, `derivativeDescent{,_ne_zero,_spec_of_highestActiveJet_eq_some}`, `jetDegree_derivativeDescent_{eq_zero,le}`, `not_dependsOnJet_derivativeDescent`, `active_lt_of_derivativeDescent`, `highestActiveJet_derivativeDescent_lt` in `ArkLib.Data.Polynomial.Differential.DerivativeDescent`; `jetDegreeCastsNeZero_separant`, `jetTotalDegree_separant_lt`, `SingularStep`, `singularStep_separant`, `jetTotalDegree_lt_of_singularStep`, `singularStep_wellFounded`, `singularStep_preserves`, `SingularBoundedSolution{,.toSeparantSolution,.toBoundedSolution}`, `RegularBranchBoundedSolution{,.toBoundedSolution,.isRegularJet_of_eval_ne_zero}`, `boundedSolutionBranch{,_forgets}`, `jetDegree_eq_zero_of_highestActiveJet_eq_none`, `exists_toMvPolynomial_eq_of_highestActiveJet_eq_none`, `differentialSpecialization_toMvPolynomial`, `eq_zero_of_{differentialSpecialization_eq_zero,boundedSolution}_of_highestActiveJet_eq_none`, `isEmpty_boundedSolution_of_highestActiveJet_eq_none`, `RegularRecursionLeaf{,.isRegularJet_of_eval_ne_zero}`, `exists_regularRecursionLeaf{,_of_boundedSolution}` in `ArkLib.Data.Polynomial.Differential.SingularRecursion`; acceptance cases in the matching `ArkLibTest` files | Both files mention no Reed–Solomon object and live in `PolynomialDifferential` over a commutative semiring. `IsBelowCharacteristic` (`jetDegree < ringChar F`, which excludes characteristic zero) is replaced by the weaker `JetDegreeCastsNeZero` (`(k : F) ≠ 0` for `0 < k ≤ jetDegree Q s`); `jetDegreeCastsNeZero_of_ringChar` recovers it from the source guard. The plan's P7a note said to preserve `IsBelowCharacteristic`; this is a deliberate deviation. Degree bounds and the ordering of active jets hold in every characteristic; only nonvanishing needs `NoZeroDivisors` and the casts. `SingularStep` drops the characteristic conjunct, and well-foundedness uses `jetTotalDegree` instead of a new `jetDegreeMeasure`. `exists_regularRecursionLeaf` takes any solution. |
| [P1 slice 6: regular jet prefixes, fixed-jet uniqueness and direct regular lifting (#960)](https://github.com/Verified-zkEVM/ArkLib/pull/960) | `DependsOnJet`, `activeJets`, `highestActiveJet`, `IsHighestActiveJet`, `IsRegularJet`, `RegularJet`, `BoundedSolution` and their lemmas in `Data/Polynomial/Differential/Basic.lean`; `jetPrefixEmbedding{,_none,_top}`, `restrictJet`, `restrictJet_polynomialJet`, `vars_subset_range_jetPrefixEmbedding`, `exists_prefixDifferentialPolynomial`, the `rename_jetPrefixEmbedding` transport lemmas, `isRegularJet_rename_jetPrefixEmbedding_iff`, `existsUnique_regularLiftCoefficient_centered_of_isHighestActiveJet` in `RootFinding/Regular/JetPrefix.lean`; `hasseCoeffAt_add_order_eq_of_regular_solutions_of_eq_below`, `eq_of_regular_solutions_of_degree_le_of_polynomialJet_eq{,_of_isHighestActiveJet}`, `BoundedSolution.eq_of_polynomialJet_eq_of_isHighestActiveJet` in `RootFinding/Regular/Iteration.lean`; the mathematical content of `effectiveDirectRegularCoefficient{,_sound_unique}`, `directRegularIteration{,_natDegree_le}`, `directRegularSolution_eq_some_iff` in `RootFinding/Regular/DirectRegular{Coefficient,Iteration}.lean` | The `Basic.lean` predicates and the jet-prefix API in `ArkLib.Data.Polynomial.Differential.JetPrefix` (with `jetPrefixEmbedding_some`, `jetPrefixEmbedding_some_last`, `jetEvaluation_separant_rename_jetPrefixEmbedding`); `coeff_shiftedJetSubstitution_sub_eq_of_taylor_coeff_eq`, `taylor_coeff_eq_of_taylor_coeff_eq_of_isLeftRegular`, `eq_of_polynomialJet_eq_of_isLeftRegular`, `eq_of_polynomialJet_eq_of_isHighestActiveJet`, `BoundedSolution.eq_of_polynomialJet_eq_of_isHighestActiveJet`, `existsUnique_regularLiftCoefficient_centered_of_isHighestActiveJet` in `...RegularIteration`; `regularLiftCoefficient`, `regularLift`, `coeff_shiftedJetSubstitution_add_hassePerturbation_eq_iff`, `X_pow_succ_dvd_shiftedJetSubstitution_regularLift`, `polynomialJet_regularLift`, `regularIterate{,_zero,_succ}`, `polynomialJet_regularIterate`, `coeff_taylor_regularIterate_succ`, `natDegree_regularIterate_le`, `X_pow_succ_dvd_shiftedJetSubstitution_regularIterate`, `eq_regularIterate_of_polynomialJet_eq`, `solution_iff_eq_regularIterate` in `...DirectRegularLift`; classic acceptance files | All in `PolynomialDifferential`, over commutative (semi)rings: none of it mentions a Reed–Solomon object. Uniqueness takes an `IsLeftRegular` slope (weaker than a unit) and equal specializations instead of two solution hypotheses; it is proved from the slice-5 congruence and affine law, not from `existsUnique_regularLiftCoefficient`. `IsRegularJet` keeps its `≠ 0` definition; the ring theorems take unit/left-regular slopes and the tests derive the prime-characteristic forms. The direct regular files are executable `CompPoly` definitions in the source; only their mathematics is ported, as `regularLiftCoefficient` (via `Ring.inverse`) and `regularIterate`. Deferred: `RootFinding/Regular/SingularRecursion.lean` (needs `DerivativeDescent.lean`) and the executable scans. |
| [P1 slice 5: Hasse perturbations, first-order Taylor, regular lifting and local contact (#959)](https://github.com/Verified-zkEVM/ArkLib/pull/959) | `hassePerturbation`, `taylor_hassePerturbation`, `hasseCoeffAt_hassePerturbation`, `hasseCoeffAt_add_hassePerturbation{,_of_lt,_self}`, `hasseJet_add_hassePerturbation_of_le`, `hasseDeriv_hassePerturbation`, `hasseDeriv_add_hassePerturbation`, `hasseCoeffAt_hasseDeriv_add_hassePerturbation{,_of_lt}`, `…_injective_of_choose_ne_zero`, `…_eq_of_choose_ne_zero`, `natCast_choose_ne_zero_of_lt_charP` in `RootFinding/HasseTaylor/Lifting.lean`; `firstOrderIncrement` and its `I ^ 2` / `u ^ (k + 1)` congruences in `RootFinding/MvPolynomial/FirstOrderTaylor.lean`; `shiftedJetValues`, `regularLiftIncrement`, `regularLiftCandidate` and its lemmas, `existsUnique_regularLiftCoefficient{,_centered}`, `X_pow_succ_dvd_iff_coeff_eq_zero_of_X_pow_dvd`, `X_pow_dvd_taylor_iff_X_sub_C_pow_dvd`, `eval_zero_shiftedJetSubstitution_separant` in `RootFinding/Regular/Lifting.lean`; the congruence lemmas of `RootFinding/Regular/Iteration.lean`; `solution_taylorCoefficient_residual`, `rationalTaylorCoefficient_eq_solution` in `RootFinding/Taylor/Numerator.lean`; `pow_dvd_eval₂Hom_of_lowContact_coeff_zero`, `X_pow_dvd_localPolynomialEvaluation_of_lowContact`, `X_pow_dvd_shiftedJetSubstitution_of_contact`, `X_sub_C_pow_dvd_differentialSpecialization_of_contact` in `Interpolation/Local/Contact.lean` | `Polynomial.hassePerturbation` and its Taylor/Hasse lemmas, `hasseCoeffAt_hasseDeriv_add_hassePerturbation_injective` (`IsLeftRegular`), `existsUnique_hasseCoeffAt_hasseDeriv_add_hassePerturbation_eq` (`IsUnit`), `X_pow_dvd_taylor_iff_X_sub_C_pow_dvd`, `X_pow_dvd_taylor_hasseDeriv_sub_of_X_pow_add_dvd`, `X_pow_succ_dvd_iff_coeff_eq_zero_of_X_pow_dvd`, `natCast_choose_ne_zero_of_lt_charP` in `ArkLib.ToMathlib.Polynomial.HasseTaylor.Lifting`; `MvPolynomial.firstOrderIncrement`, `exists_mem_sq_eval₂Hom_add_eq`, `eval₂Hom_add_sub_firstOrderIncrement_mem_sq{,_univ}`, `pow_succ_dvd_eval₂Hom_add_sub_{firstOrderIncrement,pderiv}`, `eval₂Hom_sub_eval₂Hom_mem`, `dvd_eval₂Hom_sub_eval₂Hom` in `ArkLib.ToMathlib.MvPolynomial.FirstOrderTaylor`; `MvPolynomial.pow_dvd_eval₂Hom_of_mem_restrictWeightedOrder` in `ArkLib.Data.MvPolynomial.WeightedOrder`; `PolynomialDifferential.coeff_zero_shiftedJetSubstitution` in `...ShiftedJet`; `shiftedJetValues`, `regularLiftIncrement{,_last}`, `shiftedJetValues_add_hassePerturbation`, `X_pow_succ_dvd_shiftedJetSubstitution_add_hassePerturbation_sub`, `coeff_shiftedJetSubstitution_add_hassePerturbation`, `X_pow_dvd_shiftedJetSubstitution_add_hassePerturbation`, `existsUnique_regularLiftCoefficient{,_centered}`, `X_pow_dvd_shiftedJetSubstitution_sub_of_X_pow_add_dvd`, `X_sub_C_pow_dvd_differentialSpecialization_sub_of_X_sub_C_pow_add_dvd`, `coeff_shiftedJetSubstitution_eq_centeredCoefficientPrefix_add` in `ArkLib.Data.Polynomial.Differential.RegularLift`; `taylorCoefficient_residual_eq`, `solution_taylorCoefficient_residual`, `rationalTaylorCoefficient_eq_solution` in `...RationalTaylor`; the three contact theorems in `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Local.Contact`; classic acceptance files | Everything is over a commutative ring (the first-order congruence and weighted-order bound over semirings) rather than a field. The regular lift has no coding-theory content, so it lives next to `shiftedJetSubstitution` in `PolynomialDifferential`. One `IsUnit`/`IsLeftRegular` hypothesis replaces each pair of field `≠ 0` hypotheses; tests derive the field and prime-characteristic forms. `s ≤ i` is dropped from the Hasse-coefficient law and `0 < k` from the preservation lemma; `0 < k` stays in the affine law (for `Y₀²` at `k = 0` the change is `γ²`). `regularLiftCandidate` is not defined (statements use `P + hassePerturbation …`), and the `ringChar`/`IsRegularJet` wrappers and two restatements of existing contact lemmas are not ported. Tests show the unit, regularity, prime-bound, `0 < k` and binomial hypotheses are needed. Deferred: `RootFinding/Regular/JetPrefix.lean` and the uniqueness theorems of `Iteration.lean`, which need `IsRegularJet`, `IsHighestActiveJet` and `BoundedSolution`. |
| [P2 slice 1: row bases and uniform kernel height (#911)](https://github.com/Verified-zkEVM/ArkLib/pull/911) | `Matrix.exists_rows_fin_rank`, `Matrix.exists_ne_zero_mulVec_eq_zero_natDegree_le` in `ToMathlib/LinearAlgebra/PolynomialKernelHeight.lean` | `Matrix.exists_rows_linearIndependent_span_eq` in `ArkLib.ToMathlib.LinearAlgebra.Matrix.RowBasis` (no polynomial imports); `Matrix.exists_ne_zero_mulVec_eq_zero_degreeLT` and `Matrix.exists_ne_zero_mulVec_eq_zero_natDegree_le` in `ArkLib.ToMathlib.LinearAlgebra.PolynomialKernelHeight` | Arbitrary finite row and column index types replace `Fin`. The principal kernel theorem records `Polynomial.degreeLT` membership, which also constrains zero coordinates; the natural-degree theorem recovers the source statement. Deferred: the intrinsic-rank form `exists_ne_zero_mulVec_eq_zero_natDegree_le_of_rank_eq`, primitive kernel vectors (`PrimitivePolynomialKernel.lean`), and column and shifted budgets (`ShiftedDegreeKernel.lean`). |
| [P12a slice 1: finite-submodule avoidance (#912)](https://github.com/Verified-zkEVM/ArkLib/pull/912) | `exists_vector_avoiding_submodules` in `ReedSolomon/Interleaved/PowerAgreement.lean` | `Submodule.exists_forall_notMem_of_card_le` in `ArkLib.ToMathlib.LinearAlgebra.Submodule.Union` | The source assumed a field and a finite nontrivial module. The destination assumes only a finite division ring and an arbitrary module, and keeps the sharp bound of at most `Nat.card K` proper submodules, which Mathlib's strict-cardinality theorem does not cover. It replaces two identical private proofs in `ProximityGap/Errors.lean` and `ProximityGap/LineDecoding.lean`. Deferred: the interleaving projection and the other P12a transfers. |
| [P5 slice 1: agreement double counting after deletion (#919)](https://github.com/Verified-zkEVM/ArkLib/pull/919) | `AffineHilbert.finiteAgreementIncidence_lower_sharp` in `ToMathlib/AlgebraicGeometry/Incidence/SharpRatio.lean`; `AffineHilbert.finiteAgreementIncidence_lower` in `ToMathlib/Combinatorics/FiniteAgreementIncidence.lean` | `Finset.card_bipartiteAbove_sub_card_le_card_bipartiteAbove_sdiff`, `Finset.card_mul_sub_card_le_sum_card_bipartiteBelow_sdiff`, `Finset.card_mul_sub_add_one_le_sum_card_bipartiteBelow_sdiff`, `Finset.card_mul_sub_card_le_sum_compl_card_bipartiteBelow`, `Finset.card_mul_sub_add_one_le_sum_compl_card_bipartiteBelow` in `ArkLib.ToMathlib.Combinatorics.Enumerative.DoubleCounting` (Mathlib imports only) | The source indexed positions by `Fin n` and proved both bounds by separate copies of the same argument. The destination uses Mathlib's `bipartiteAbove`/`bipartiteBelow` vocabulary over an arbitrary type and an arbitrary position set `t`, with the `univ` versions summing over `uᶜ`; the `A - k + 1` form is a corollary of the sharp form and keeps `k ≤ A`, which a test shows is necessary. The source statements follow by `Finset.filter_notMem_eq_sdiff`. Deferred: `goodCuts_div_agreements_le` and the sharp-ratio layer; the consumer `affineAgreementIncidence_bound_aux` stays in the source. |
| [P2 slice 2: intrinsic-rank and primitive polynomial kernels (#914)](https://github.com/Verified-zkEVM/ArkLib/pull/914) | `Matrix.exists_ne_zero_mulVec_eq_zero_natDegree_le_of_rank_eq` in `ToMathlib/LinearAlgebra/PolynomialKernelHeight.lean`; `Matrix.exists_primitive_kernel_vector_preserving_zero`, `Matrix.exists_primitive_kernel_vector`, `Matrix.exists_primitive_ne_zero_mulVec_eq_zero_natDegree_le`, `Matrix.exists_primitive_ne_zero_mulVec_eq_zero_natDegree_le_of_rank_eq` in `PrimitivePolynomialKernel.lean`; `Matrix.exists_primitive_kernel_vector_degreeLT` in `ShiftedDegreeKernel.lean` | `Matrix.exists_rows_submatrix_mulVec_eq_zero_iff` in `ArkLib.ToMathlib.LinearAlgebra.Matrix.RowBasis`; `Finset.span_gcd`, `Ideal.span_range_eq_top_iff_univ_gcd_eq_one`, `Ideal.comp_ne_zero_of_span_range_eq_top`, `Matrix.exists_primitive_kernel_vector_eq_smul` in `ArkLib.ToMathlib.LinearAlgebra.Matrix.PrimitiveKernel` (no polynomial imports); `Polynomial.natDegree_le_of_mem_degreeLT_succ`, `Polynomial.mem_degreeLT_of_mul_left` in `ArkLib.ToMathlib.Polynomial.DegreeLT`; `Matrix.exists_ne_zero_mulVec_eq_zero_degreeLT_of_rank_le`, `Matrix.exists_ne_zero_mulVec_eq_zero_natDegree_le_of_rank_le`, `Matrix.exists_primitive_ne_zero_mulVec_eq_zero_degreeLT_of_rank_le` in `ArkLib.ToMathlib.LinearAlgebra.PolynomialKernelHeight` | The source fixed the rank exactly, measured it over `RatFunc F`, and used `Fin` indices. The destination takes an upper bound `rank ≤ s` over any field receiving an injective hom, with arbitrary finite indices, so consumers no longer prove monotonicity of `s * b / (c - s)` themselves. The row-kernel transfer holds over any semiring embedded in a field. Primitive normalization is a separate non-polynomial theorem over Bézout rings with a normalized gcd, stated as `v = g • u` with `g ≠ 0` and unit-ideal `u`. The source's zero-preservation, per-coordinate degree, and `eval₂` specialization clauses follow from this factorization, `Polynomial.mem_degreeLT_of_mul_left`, and `Ideal.comp_ne_zero_of_span_range_eq_top` (any nontrivial semiring target, any index type). Not kept as named wrappers: `exists_primitive_kernel_vector_preserving_zero`, `exists_primitive_kernel_vector`, `exists_primitive_kernel_vector_degreeLT`, and the row-count primitive theorem (the rank form with `Matrix.rank_le_card_height`); `ArkLibTest` derives each in a few lines. Deferred: the shifted family (`exists_ne_zero_mulVec_eq_zero_shifted_degreeLT`, `exists_primitive_mulVec_eq_zero_of_shifted_surplus`) and the column family (`exists_ne_zero_mulVec_eq_zero_column_degreeLT`, `_of_rank`, `exists_primitive_mulVec_eq_zero_of_column_surplus`). |
| [P2 slice 3: shifted and column-budget polynomial kernels (#920)](https://github.com/Verified-zkEVM/ArkLib/pull/920) | `Matrix.exists_ne_zero_mulVec_eq_zero_shifted_degreeLT`, `Matrix.exists_primitive_kernel_vector_degreeLT`, `Matrix.exists_primitive_mulVec_eq_zero_of_shifted_surplus` in `ToMathlib/LinearAlgebra/ShiftedDegreeKernel.lean`; `Matrix.exists_ne_zero_mulVec_eq_zero_column_degreeLT`, `Matrix.exists_ne_zero_mulVec_eq_zero_column_degreeLT_of_rank`, `Matrix.exists_primitive_mulVec_eq_zero_of_column_surplus` in `ColumnDegreeKernel.lean` | `Polynomial.mem_degreeLT_add_one_sub_iff`; `Matrix.exists_ne_zero_mulVec_eq_zero_shifted_degreeLT` and `_of_natDegree_le`, `Matrix.exists_primitive_ne_zero_mulVec_eq_zero_shifted_degreeLT` and `_of_natDegree_le`, `Matrix.exists_ne_zero_mulVec_eq_zero_column_degreeLT`, `Matrix.exists_ne_zero_mulVec_eq_zero_column_degreeLT_of_rank_le`, `Matrix.exists_primitive_ne_zero_mulVec_eq_zero_column_degreeLT_of_rank_le` in `ArkLib.ToMathlib.LinearAlgebra.ShiftedPolynomialKernelHeight`; `Matrix.exists_primitive_kernel_vector_degreeLT` in `ArkLib.ToMathlib.LinearAlgebra.PolynomialKernelHeight` | Arbitrary finite indices replace `Fin`. The source's `hdegree`/`hzero` pair becomes one entry hypothesis `M i j ∈ degreeLT F (columnWeight j + 1 - rowWeight i)` (equivalent by `mem_degreeLT_add_one_sub_iff`), required only for columns of weight at most `h`; source-shaped `_of_natDegree_le` forms remain. The column rank form takes `rank (M.map φ) ≤ s` for any injective `φ` into a field instead of the exact rank over `RatFunc F`. The primitive column form keeps each budget `degreeLT F (h + 1 - weight j)` where the source weakened it to `natDegree ≤ h`. `exists_primitive_kernel_vector_degreeLT`, listed as not kept in the #914 row, is restored as a public lemma because both shifted primitive forms and the uniform primitive form now use it. A shifted rank form is omitted: the row-slot sum depends on which rows are selected, so the rank alone does not determine the bound (explained in the module docstring). The `eval₂` specialization clauses follow from `Ideal.comp_ne_zero_of_span_range_eq_top`. Consumers in `HiddenDerivative/Interpolation/Symbolic/{ColumnHeight,CurveColumnHeight}.lean` stay in the source. |
| [P4 slice 1: resultant specialization and specialization avoidance (#915)](https://github.com/Verified-zkEVM/ArkLib/pull/915) | `eval_derivative_ne_zero_of_separableResultant_eval_ne_zero`, `eval_derivative_ne_zero_of_separableResultant_map_ne_zero`, `specialization_separable_of_separableResultant_eval_ne_zero`, `finite_polynomial_specializations_eq_zero_card_le`, `exists_map_evalRingHom_ne_zero_avoiding`, `exists_map_evalRingHom_ne_zero` in `ToMathlib/Polynomial/SeparableResultant.lean`; `paddedDerivativeResultant_map_eq_zero_of_common_root` in `PaddedDerivativeResultantCommonRoot.lean`; `separableResultant_map_eq_zero_of_common_root` in `DerivativeResultantDegree.lean` | `Polynomial.map_resultant_eq_zero_of_common_root`, `eval_derivative_map_ne_zero_of_resultant_derivative_padded_ne_zero`, `natDegree_map_eq_of_resultant_derivative_padded_ne_zero`, `isCoprime_map_of_resultant_padded_ne_zero`, `separable_map_of_resultant_derivative_padded_ne_zero`, `resultant_comm_sub_one` in `ArkLib.Data.Polynomial.ResultantSpecialization`; `Polynomial.card_le_natDegree_of_injOn_of_eval_eq_zero`, `exists_mem_eval_ne_zero_of_card_add_natDegree_lt_card`, `exists_mem_map_evalRingHom_ne_zero_of_card_add_natDegree_lt_card`, `exists_map_evalRingHom_ne_zero_avoiding`, `exists_map_evalRingHom_ne_zero` in `ArkLib.Data.Polynomial.SpecializationAvoidance` | The source proves the common-root argument separately for `R[X]` and `R[X][X]` and assumes `IsDomain`. The destination states it once for any ring hom between commutative rings and any declared degrees, and derives the simple-root, coprimality, and separability statements from it. The source's `separableResultant A b = resultant A.derivative A (b - 1) b` equals main's order `resultant A A.derivative b (b - 1)` with no sign (`resultant_comm_sub_one`). A nonzero padded derivative resultant forces `f` to keep degree `m`, so these statements cover a drop in the derivative's degree only. Avoidance is stated from a finite candidate set, which also covers finite fields; the source's infinite-domain theorems keep their statements. Main's `isCoprime_map_of_resultant_ne_zero`, the common-root argument in `KKH26SumSet`, and `resultant_fixed_degree_eq_zero_of_common_root_of_monic_right` now derive from `map_resultant_eq_zero_of_common_root`. Deferred: the total-degree bounds `natDegree_separableResultant_add_sq_le*` and `natDegree_separableResultant_le_totalDegree*`, `separableResultant_ne_zero_of_irreducible`, and the Ordinary root-presentation consumers. |
| [P4 slice 2: total-degree resultant bounds and irreducible derivative resultants (#929)](https://github.com/Verified-zkEVM/ArkLib/pull/929) | `natDegree_separableResultant_add_sq_le_of_le`, `natDegree_separableResultant_add_sq_le`, `natDegree_separableResultant_le_totalDegree_of_le`, `natDegree_separableResultant_le_totalDegree` in `ToMathlib/Polynomial/DerivativeResultantDegree.lean`; `separableResultant_ne_zero_of_irreducible` in `ToMathlib/Polynomial/SeparableResultant.lean` | `Polynomial.natDegree_resultant_add_mul_le_of_coeff_add_le`, `natDegree_resultant_le_of_coeff_add_le`, `natDegree_resultant_le_mul_of_coeff_add_le`, `natDegree_resultant_derivative_padded_add_sq_le`, `natDegree_resultant_derivative_padded_le_of_coeff_add_le` in `ArkLib.Data.Polynomial.ResultantDegree`; `Polynomial.separable_map_of_irreducible_of_derivative_ne_zero`, `resultant_derivative_ne_zero_of_irreducible` in `ArkLib.Data.Polynomial.FractionFieldResultant`; acceptance cases in the matching `ArkLibTest` files | The weighted Sylvester bound `deg_X (resultant P Q m n) + m n ≤ n dP + m dQ` is proved once for two polynomials over `R[X]` with declared degrees and coefficient triangles; the Bezout bound `dP dQ` and the source's derivative bound `(2b - 1) j - b²` are corollaries, the latter in main's argument order and without the source's `0 < b` and `A.natDegree = b`. The new bounds and main's `degreeX` bounds are incomparable. Irreducibility nonvanishing holds over any GCD domain via Gauss's lemma; Deferred: the Ordinary root-presentation, `SingularTail`, and content-exception consumers. |
| [P5 slice 2: retained minimal primes and zero-locus covers (#922)](https://github.com/Verified-zkEVM/ArkLib/pull/922) | `AffineHilbert.minimalPrimesFinset`, `mem_minimalPrimesFinset` in `ToMathlib/AlgebraicGeometry/PrincipalCut/ComponentCoefficient.lean`; `retainedMinimalPrimes`, `mem_retainedMinimalPrimes`, `exists_retainedMinimalPrime_of_mem_zeroLocus`, `mem_zeroLocus_and_eval_ne_zero_iff_retained`, `mem_zeroLocus_and_cut_iff_retained` in `PrincipalOpen/Cuts.lean` | `Ideal.minimalPrimesFinset`, `mem_minimalPrimesFinset`, `coe_minimalPrimesFinset`, `minimalPrimesFinset_eq_empty_iff`, `minimalPrimesFinset_of_isPrime`, `retainedMinimalPrimes`, `mem_retainedMinimalPrimes`, `retainedMinimalPrimes_subset`, `exists_mem_retainedMinimalPrimes_le`, `retainedMinimalPrimes_eq_empty_iff`, `retainedMinimalPrimes_of_isUnit` in `ArkLib.ToMathlib.RingTheory.Ideal.MinimalPrime.Noetherian`; `MvPolynomial.mem_zeroLocus_iff_le_ker_aeval`, `zeroLocus_sup`, `mem_zeroLocus_sup_span_singleton_iff`, `exists_retainedMinimalPrime_of_mem_zeroLocus`, `mem_zeroLocus_and_eval_ne_zero_iff_retained`, `mem_zeroLocus_and_cut_iff_retained` in `ArkLib.ToMathlib.RingTheory.Nullstellensatz` | The finite minimal-prime family and the retained filter hold for any Noetherian commutative semiring, not only `MvPolynomial σ F` with finite `σ`; there is one representation (the source had a second `toFinset` in `Cuts.lean`). The algebraic cover step `exists_mem_retainedMinimalPrimes_le` is public; emptiness is characterized by `s ∈ I.radical`. The point covers hold over any field extension, with no algebraic closure. `mem_retainedMinimalPrimes` now takes implicit arguments, so source call sites `(Ideal.mem_retainedMinimalPrimes _ _ _).mp` become `Ideal.mem_retainedMinimalPrimes.mp`. Deferred: `Ideal.lt_of_mem_minimalPrimes_sup_span` and the principal-cut Krull-dimension drop, plus the consumers `initialJetPrimeFamily_prime_open`, `exists_mem_initialJetPrimeFamily_of_regular`, and `card_filter_cut_le_sum_retained`. The finite-quotient results continue in P5 slice 3. |
| [P5 slice 3: finite coordinate quotients and point counts (#924)](https://github.com/Verified-zkEVM/ArkLib/pull/924) | `MvPolynomial.zeroLocusPointHom`, `zeroLocusPointHom_injective`, `finite_zeroLocus_of_finite_quotient`, `ncard_zeroLocus_le_finrank_quotient` in `ToMathlib/AlgebraicGeometry/ZeroLocus/ZeroDimensional.lean` | The same four declarations in `ArkLib.ToMathlib.RingTheory.Nullstellensatz.FiniteQuotient`; ordinary-import clients in `ArkLibTest.ToMathlib.RingTheory.Nullstellensatz` | Pinned Mathlib already owns the generic algebra layer: `Finite.algHom` and `card_algHom_le_finrank`. The ArkLib owner therefore adds only the injective map from the zero locus into algebra homomorphisms out of the actual coordinate quotient. The source assumptions are preserved: arbitrary variable type, arbitrary field extension, and only `Module.Finite` for the two conclusions; no properness, radicality, algebraic closure, or finite-variable assumption. The top-ideal zero quotient gives the empty/zero boundary, and the bottom ideal with no variables gives the singleton/one-dimensional boundary. Deferred: `finite_zeroLocus_and_ncard_le_of_krullDimLE_zero` and the Hilbert-polynomial/affine-degree wrappers; these need finite variables for the finite-type-to-module-finite implication and belong to a justified geometry consumer slice. |
| [P5 slice 4: principal-cut Krull-dimension drop (#931)](https://github.com/Verified-zkEVM/ArkLib/pull/931) | `Ideal.ringKrullDim_quotient_succ_le_of_lt`, `lt_of_mem_minimalPrimes_sup_span`, `retained_cut_krullDim_succ_le` in `ToMathlib/AlgebraicGeometry/PrincipalOpen/Cuts.lean` | The same three declarations in `ArkLib.ToMathlib.RingTheory.Ideal.PrincipalCut`; ordinary-import clients in `ArkLibTest.ToMathlib.RingTheory.Ideal` | The owner is ideal-theoretic rather than polynomial-geometric. Pinned Mathlib's `ringKrullDim_succ_le_of_surjective` supplies the generic dimension step after choosing an element in `J` but not `P`. The first two declarations need only a commutative ring; Noetherianity is retained only for the finite `retainedMinimalPrimes` corollary. The essential hypothesis `f ∉ P` is explicit. Membership of `J` in the minimal-prime family witnesses a proper cut, so there is no hidden nonemptiness assumption; no radicality, rational-point, or algebraic-closure hypothesis is introduced. Deferred: the source's relative-height-one principal-cut results and polynomial/Hilbert consumers, which require the separate height and affine-dimension infrastructure in `PrincipalCut/Dimension.lean`. |
| [P5 slice 5: affine Hilbert function and standard monomials (#944)](https://github.com/Verified-zkEVM/ArkLib/pull/944) | `AffineHilbert.quotientDegreeLE`, `hilbertFunction`, `exists_finset_generators_totalDegree_le`, `principalCut_hilbertFunction_add_le` in `ToMathlib/AlgebraicGeometry/Hilbert/Function.lean`; `quotientDegreeLE_eventually_top` and the existence half of `exists_hilbertPolynomial` in `Hilbert/Polynomial.lean`; `one_le_hilbertFunction` in `PrincipalCut/Degree.lean`; `standardExponents`, `standardSpace`, `standardQuotientMap_bijective`, `standardFilteredMap_bijective`, `hilbertFunction_eq_standard_count`, `standardExponents_lower`, `exists_finset_forbidden_standardExponents` in `Hilbert/StandardMonomials.lean`; `standardExponents_span_singleton` in `PrincipalCut/Polynomial.lean`; `MonomialHilbertCounting.degreeBall`, `card_degreeBall`, `card_upperConeInBall`, `countingPolynomial`, `countingPolynomial_eval_eq_card`, `exists_eventual_standardExponent_countingPolynomial` in `Hilbert/MonomialCounting.lean`; `finite_zeroLocus_and_ncard_le_of_krullDimLE_zero` in `ZeroLocus/ZeroDimensional.lean` | `MvPolynomial.quotientDegreeLE`, `affineHilbertFunction`, `affineHilbertFunction_mono`/`_anti`, `one_le_affineHilbertFunction`, `exists_affineHilbertFunction_eq_finrank`, `exists_finset_generators_totalDegree_le`, `principalCut_affineHilbertFunction_add_le`, `principalCut_affineHilbertFunction_add_le_of_isPrime` in `ArkLib.ToMathlib.RingTheory.MvPolynomial.AffineHilbert`; `MonomialOrder.standardExponents`, `standardSpace`, `standardQuotientEquiv`, `standardDegreeLEEquiv`, `affineHilbertFunction_eq_ncard_standardExponents`, `exists_finset_standardExponents_iff`, `MvPolynomial.affineHilbertFunction_eq_standard_count`, `affineHilbertFunction_bot`, `exists_eval_eq_affineHilbertFunction` in `ArkLib.ToMathlib.RingTheory.MvPolynomial.StandardMonomials`; `Finsupp.degreeLEFinset`, `card_degreeLEFinset`, `card_filter_le_degreeLEFinset`, `card_filter_forall_not_le_degreeLEFinset`, `coneAvoidancePoly`, `eval_coneAvoidancePoly`, `exists_eval_eq_ncard_forall_not_le` in `ArkLib.ToMathlib.Combinatorics.Enumerative.MonomialCount`; `MvPolynomial.finite_zeroLocus_and_ncard_le_of_krullDimLE_zero` in `ArkLib.ToMathlib.RingTheory.Nullstellensatz.FiniteQuotient`; classic acceptance files mirror each module | The source namespace `AffineHilbert` is dropped: the objects attach to an ideal of `MvPolynomial σ k`, and `affine` separates the filtered function from Mathlib's graded `Polynomial.hilbertPoly`. The principal-cut inequality assumes only that multiplication by `f` is injective on the quotient; the source's prime-ideal form is the `_of_isPrime` corollary. Standard monomials work for every monomial order and the filtered statements for every graded order, rather than for `degLex` with `LinearOrder σ` and `WellFoundedGT σ`; the source's `degLex` count is a specialization, and a deleted acceptance test illustrated why the graded hypothesis is needed (`I = (X₀ - X₁²)`: `degLex` count 3, `lex` count 2 at degree 1). The counting polynomial is over any characteristic-zero field with the single threshold `degree (B.sup id)`. Deferred: `hilbertPolynomial` and its uniqueness, constant, `⊥`, nonvanishing and degree-zero lemmas, the principal-cut degree/leading-coefficient statement, and the `ℚ[X]` growth lemmas of `PrincipalCut/Degree.lean`. |
| [P5 slice 6: affine Hilbert polynomial and principal-cut degree drop (#946)](https://github.com/Verified-zkEVM/ArkLib/pull/946) | `AffineHilbert.polynomial_eq_of_eval_nat_ge`, `hilbertPolynomial`, `hilbertPolynomial_natDegree_le`, `hilbertPolynomial_eventually`, `hilbertPolynomial_eventually_eval`, `hilbertPolynomial_unique`, `hilbertPolynomial_eq_constant`, `hilbertPolynomial_ne_zero`, `moduleFinite_of_hilbertPolynomial_natDegree_zero`, `hilbertPolynomial_degree_and_leadingCoeff_antitone`, `principalCut_hilbertPolynomial_zero_or_degree_and_coeff` in `ToMathlib/AlgebraicGeometry/Hilbert/Polynomial.lean`; `backwardDifference`, `natDegree_backwardDifference_le`, `coeff_backwardDifference_pred_natDegree`, `backwardDifference_natDegree_eq_and_leadingCoeff`, `natDegree_le_of_eventually_eval_nat_le`, `principalCut_eventualPolynomial_degree_and_coeff` in `PrincipalCut/Degree.lean`; `hilbertPolynomial_span_singleton`, `hilbertPolynomial_bot`, `hilbertPolynomial_bot_natDegree`, `totalDegree_pos_of_span_singleton_ne_top`, `hilbertPolynomial_span_singleton_natDegree_add_one` (and, via a different proof, `preHilbertPoly_eq_taylor`, `countingPolynomial_singleton`) in `PrincipalCut/Polynomial.lean` | `Polynomial.eq_of_eventually_eval_natCast_eq` (and filter form), `backwardDifference`, `eval_backwardDifference`, `natDegree_backwardDifference_le`, `coeff_backwardDifference_natDegree_sub_one`, `natDegree_backwardDifference_eq_and_leadingCoeff` (and `_of_ne_zero`), `leadingCoeff_nonneg_of_eventually_eval_natCast_nonneg`, `leadingCoeff_pos_of_eventually_eval_natCast_nonneg`, `natDegree_le_of_eventually_eval_natCast_le`, `natDegree_le_and_coeff_le_of_eventually_eval_natCast_le_backwardDifference` in `ArkLib.ToMathlib.Polynomial.EventualGrowth`; `MvPolynomial.affineHilbertPolynomial`, `natDegree_affineHilbertPolynomial_le`, `exists_eval_affineHilbertPolynomial`, `eventually_eval_affineHilbertPolynomial`, `eq_affineHilbertPolynomial_of_eval_eq`, `eq_map_affineHilbertPolynomial_of_eval_eq`, `affineHilbertPolynomial_eq_C_finrank`, `affineHilbertPolynomial_top`, `affineHilbertPolynomial_ne_zero`, `affineHilbertPolynomial_eq_zero_iff`, `leadingCoeff_affineHilbertPolynomial_pos`, `moduleFinite_of_natDegree_affineHilbertPolynomial_eq_zero`, `natDegree_affineHilbertPolynomial_eq_zero_iff`, `natDegree_affineHilbertPolynomial_le_of_le`, `leadingCoeff_affineHilbertPolynomial_le_of_le`, `principalCut_natDegree_affineHilbertPolynomial_le_and_coeff_le` (and `_of_isPrime`), `affineHilbertPolynomial_bot`, `natDegree_affineHilbertPolynomial_bot`, `affineHilbertFunction_span_singleton`, `affineHilbertPolynomial_span_singleton`, `totalDegree_pos_of_span_singleton_ne_top`, `natDegree_affineHilbertPolynomial_span_singleton_add_one` in `ArkLib.ToMathlib.RingTheory.MvPolynomial.AffineHilbertPolynomial`; classic acceptance files | The growth lemmas are about polynomials, not ideals, so they live in a `Polynomial` owner: the algebraic ones over any commutative ring, uniqueness over any characteristic-zero domain, and the order lemmas over an Archimedean ordered field, since the proofs use that the naturals tend to `+∞`. Redundant source hypotheses are dropped: `0 ≤ b` and eventual positivity of `P` in the backward-difference comparison, `Q ≠ 0` in the comparison, `J ≠ ⊤` in the inclusion comparison, and the `Q = 0 ∨ …` disjunction of the principal cut, whose bounds hold at `Q = 0`; the deleted acceptance tests recovered each source form. The principal cut keeps the parent's regularity hypothesis, with the prime-ideal form as a corollary; a deleted acceptance test with `I = (X₀²)`, `f = X₀` illustrated why regularity is needed. The earlier acceptance suite used examples over `ZMod 4` and `ZMod 2` to illustrate why characteristic zero is needed. The principal-ideal polynomial is computed by a direct count of exponents above the leading exponent of `f` instead of the source's Taylor route. `hilbertPolynomial_span_singleton_natDegree` is covered by the `_add_one` form. Deferred: `finite_zeroLocus_and_ncard_le_hilbertPolynomial`, `Hilbert/PolynomialGrowth{Rescaling,Affine}.lean`, finite-algebra growth, prime families, radical degree, Bezout and purity. |
| [P5 slice 7: affine Hilbert polynomials under algebra maps and zero-locus bound (#949)](https://github.com/Verified-zkEVM/ArkLib/pull/949) | `natDegree_comp_C_mul_X`, `natDegree_le_of_eventually_eval_nat_le_rescaled`, `natDegree_eq_of_eventually_eval_nat_sandwich` in `ToMathlib/AlgebraicGeometry/Hilbert/PolynomialGrowthRescaling.lean`; `natDegree_le_of_eventually_eval_nat_le_mul_affine` in `Hilbert/PolynomialGrowthAffine.lean`; `hilbertFunction_le_rescaled_of_injective_algHom`, `hilbertPolynomial_natDegree_le_of_injective_algHom`, `hilbertPolynomial_natDegree_le_of_surjective_algHom`, `hilbertFunction_le_mul_rescaled_of_finite` in `Hilbert/FiniteAlgebraGrowth.lean`; `hilbertPolynomial_natDegree_eq_of_finite_injective_algebraMap`, `hilbertPolynomial_natDegree_eq_of_finite_injective_algHom` in `Hilbert/FiniteExtensionDegree.lean`; `finite_zeroLocus_and_ncard_le_hilbertPolynomial` in `Hilbert/Polynomial.lean` | `Polynomial.natDegree_le_of_eventually_eval_natCast_le_mul_eval_comp`, `natDegree_le_of_eventually_eval_natCast_le_mul_eval_affine`, `natDegree_eq_of_eventually_eval_natCast_le_of_le_mul_eval_affine`, `natDegree_comp_C_mul_X_add_C` in `ArkLib.ToMathlib.Polynomial.EventualGrowth`; `MvPolynomial.exists_mem_quotientDegreeLE`, `algebraMap_mem_quotientDegreeLE`, `one_mem_quotientDegreeLE`, `mk_X_mem_quotientDegreeLE`, `mul_mem_quotientDegreeLE` in `ArkLib.ToMathlib.RingTheory.MvPolynomial.AffineHilbert`; `MvPolynomial.aeval_mem_of_forall_mul_mem`, `map_mem_quotientDegreeLE`, `affineHilbertFunction_le_of_injective`, `exists_affineHilbertFunction_le_of_injective`, `exists_affineHilbertFunction_le_mul_of_finite`, `natDegree_affineHilbertPolynomial_le_of_eventually_le_mul`, `natDegree_affineHilbertPolynomial_le_of_injective`, `natDegree_affineHilbertPolynomial_le_of_finite`, `natDegree_affineHilbertPolynomial_le_of_surjective`, `natDegree_affineHilbertPolynomial_eq_of_finite_of_injective` in `ArkLib.ToMathlib.RingTheory.MvPolynomial.AffineHilbertAlgHom`; `MvPolynomial.ncard_zeroLocus_le_coeff_zero_affineHilbertPolynomial`, `finite_zeroLocus_and_ncard_le_affineHilbertPolynomial` in `ArkLib.ToMathlib.RingTheory.Nullstellensatz.AffineHilbertPolynomial`; classic acceptance files | One composition growth lemma over any ordered normed field covers the source's rescaled, affine and sandwich `ℚ[X]` lemmas, without sign or nonvanishing conditions on the constants. Finite maps are `AlgHom`s with `g.Finite` rather than `Algebra` instances, so the two source equality theorems become one; a deleted acceptance test covered the instance form. The finite bound is `H(I, N) ≤ m * H(J, c * N)` instead of `c * (N + 1)`. `J ≠ ⊤` / `I ≠ ⊤` are dropped, and the surjective case is a corollary of the finite case. The point bound needs only a finite-dimensional quotient (`KrullDimLE 0` form as corollary) and lives in a Nullstellensatz file. Deleted acceptance tests illustrated the need for finiteness, injectivity, eventual nonnegativity and finite dimension. Deferred: radical degree, prime families, Bezout and purity. |
| [P5 slice 8: affine degree and radical Hilbert-polynomial degree (#950)](https://github.com/Verified-zkEVM/ArkLib/pull/950) | `AffineHilbert.affineDegree`, `affineDegree_nonneg`, `affineDegree_pos`, `affineDegree_bot`, `affineDegree_eq_finrank`, `finite_zeroLocus_and_ncard_le_affineDegree`, `affineDegree_span_singleton` in `ToMathlib/AlgebraicGeometry/Hilbert/Degree.lean`; `exponentDiv`, `exponentDiv_apply`, `exponentDiv_le`, `exponentDiv_mem_standardExponents`, `hilbertFunction_le_mul_of_pow_le`, `hilbertPolynomial_radical_natDegree` in `Hilbert/RadicalDegree.lean` | `MvPolynomial.affineDegree`, `affineDegree_nonneg`, `affineDegree_pos`, `affineDegree_top`, `affineDegree_pos_iff`, `affineDegree_eq_zero_iff`, `affineDegree_of_natDegree_eq_zero`, `affineDegree_eq_finrank`, `affineDegree_bot`, `affineDegree_span_singleton`, `affineDegree_le_of_le` in `ArkLib.ToMathlib.RingTheory.MvPolynomial.AffineDegree`; `Finsupp.floorDiv_le_self`, `Finsupp.smul_floorDiv_le_self`, `MonomialOrder.floorDiv_mem_standardExponents`, `MvPolynomial.affineHilbertFunction_le_pow_mul_of_pow_le`, `natDegree_affineHilbertPolynomial_le_of_pow_le`, `natDegree_affineHilbertPolynomial_eq_of_pow_le_of_le`, `natDegree_affineHilbertPolynomial_eq_of_le_of_le_radical`, `natDegree_affineHilbertPolynomial_radical`, `natDegree_affineHilbertPolynomial_eq_of_radical_eq` in `ArkLib.ToMathlib.RingTheory.MvPolynomial.AffineHilbertRadical`; `MvPolynomial.ncard_zeroLocus_le_affineDegree`, `finite_zeroLocus_and_ncard_le_affineDegree` in `ArkLib.ToMathlib.RingTheory.Nullstellensatz.AffineHilbertPolynomial`; classic acceptance files | `exponentDiv` is Mathlib's `Finsupp` floor division `e ⌊/⌋ t`, so `exponentDiv_apply` is `Finsupp.floorDiv_apply`. The standard-exponent step holds for every monomial order and drops `0 < t`; the Hilbert-function comparison assumes only `J ^ t ≤ I` (factor `t ^ Nat.card σ` on the left), and the radical statement is a corollary. `affineDegree_span_singleton` drops `span {f} ≠ ⊤`. The point bound holds for any finite-dimensional quotient; the source's `natDegree = 0` form is a corollary. Deferred: `principalCut_sum_minimalPrime_coeff_le` and `principalCut_sum_minimalPrime_factorial_le` (`PrincipalCut/ComponentCoefficient.lean`), which need `PrimeFamilyCoefficient` and `PrincipalCut/Dimension`. |
| [P5 slice 9: prime families, principal-cut components, and component coefficients (#954)](https://github.com/Verified-zkEVM/ArkLib/pull/954) | `coeff_nonneg_of_natDegree_le_of_eventually_eval_nat_nonneg`, `coeff_le_of_natDegree_le_of_eventually_eval_nat_le`, `coeff_taylor_eq_of_natDegree_le`, `familySeparatorLift`, `filteredFamilySeparatorLift`, `separatorFamilyMap`, `separatorFamilyMap_injective`, `sum_shifted_hilbertFunction_le_iInf`, `exists_separators_sum_shifted_hilbertFunction_le_iInf` in `ToMathlib/AlgebraicGeometry/Hilbert/PrimeFamily.lean`; `sum_hilbertPolynomial_coeff_le_iInf` in `Hilbert/PrimeFamilyCoefficient.lean`; `map_quotient_ne_bot_of_lt`, `principalCut_minimalPrime_relative_codimension_one`, `exists_principalCut_minimalPrime_relative_codimension_one` in `PrincipalCut/Dimension.lean`; `minimalPrime_pairwise_incomparable`, `principalCut_sum_minimalPrime_coeff_le`, `principalCut_sum_minimalPrime_factorial_le` in `PrincipalCut/ComponentCoefficient.lean` | `Polynomial.coeff_nonneg_of_natDegree_le_of_eventually_eval_natCast_nonneg`, `coeff_le_of_natDegree_le_of_eventually_eval_natCast_le`, `coeff_taylor_of_natDegree_le` in `ArkLib.ToMathlib.Polynomial.EventualGrowth`; `Ideal.exists_separators_of_pairwise_not_le`, `Ideal.not_le_of_mem_minimalPrimes` in `ArkLib.ToMathlib.RingTheory.Ideal.Separator`; `Ideal.map_quotient_ne_bot_of_lt`, `map_quotient_height_eq_one_of_mem_minimalPrimes_sup_span`, `exists_principalCut_component_relative_codimension_one` in `ArkLib.ToMathlib.RingTheory.Ideal.PrincipalCut`; `MvPolynomial.sum_affineHilbertFunction_le_iInf`, `exists_sum_affineHilbertFunction_le_iInf`, `sum_coeff_affineHilbertPolynomial_le_of_separators`, `sum_coeff_affineHilbertPolynomial_le_iInf`, `sum_coeff_affineHilbertPolynomial_minimalPrimes_le`, `principalCut_sum_coeff_affineHilbertPolynomial_minimalPrimes_le`, `principalCut_sum_factorial_mul_leadingCoeff_minimalPrimes_le` in `ArkLib.ToMathlib.RingTheory.MvPolynomial.AffineHilbertComponents`; acceptance cases in the matching `ArkLibTest` files | The coefficient comparisons hold over any Archimedean ordered field. The separator construction is a commutative-semiring theorem about incomparable primes; the separator maps are private. The Hilbert-function comparison assumes only separators that are regular modulo their own ideal, and the principal-cut component theorems assume only that `f` is regular modulo `I` (the source's prime `P` with `f ∉ P` is a test corollary). `sum_hilbertPolynomial_coeff_le_iInf` drops `hPdeg`, and `map_quotient_ne_bot_of_lt` drops primality. The relative-codimension conjunction is derived in the test file rather than restated. Purity remains a hypothesis of the factorial form; removing it needs Hilbert-polynomial degree equals Krull dimension. |
| [P5 slice 10: purity, Bezout bound, height-one contractions and finite zero loci (#956)](https://github.com/Verified-zkEVM/ArkLib/pull/956) | `principalCut_component_hilbertPolynomial_natDegree_add_one` in `ToMathlib/AlgebraicGeometry/PrincipalCut/Purity.lean`; `principalCut_sum_affineDegree_le` in `PrincipalCut/Bezout.lean`; `NormalizationHeightOneData`, `normalization_contraction_height_one` and the parent-degree step in `PrincipalCut/NoetherNormalizationHeightOne.lean`; `zeroLocusEvaluation_injective`, `finite_zeroLocus_iff_hilbertPolynomial_natDegree_zero`, the finite-quotient direction in `ZeroLocus/Finite.lean`; `retainedCutChildren`, `mem_retainedCutChildren`, `sum_retainedCutChildren_affineDegree_mul_pow_le`, `exists_mem_retainedCutChildren_of_mem_zeroLocus` in `CutFamily/Finite.lean` | `Ideal.height_under_le_height`, `height_under_eq_one`, `exists_prime_under_eq_span_singleton` in `ArkLib.ToMathlib.RingTheory.Ideal.HeightUnder`; `Ideal.of_mem_retainedMinimalPrimes_sup_span`, `retainedMinimalPrimes_sup_span_of_mem` in `...Ideal.PrincipalCut`; `MvPolynomial.natDegree_affineHilbertPolynomial_eq_card_of_finite_of_injective`, `natDegree_affineHilbertPolynomial_add_one_of_height_eq_one`, `principalCut_natDegree_affineHilbertPolynomial_add_one`, `principalCut_sum_affineDegree_minimalPrimes_le`, `sum_affineDegree_mul_pow_retainedMinimalPrimes_le` in `...MvPolynomial.AffineHilbertPurity`; `MvPolynomial.zeroLocus_radical`, `zeroLocusEvaluation_injective`, `moduleFinite_of_finite_zeroLocus`, `finite_zeroLocus_iff_moduleFinite`, `finite_zeroLocus_iff_natDegree_affineHilbertPolynomial_eq_zero` in `...Nullstellensatz.FiniteZeroLocus`; classic acceptance files | The height-one contraction holds for any integral extension of an integrally closed (resp. UFD) domain, without Noetherianity, through Mathlib's going-down; `NormalizationHeightOneData` is not introduced as a declaration; current tests cover the retained-component and zero-locus bounds. Purity is stated for any height-one prime `J ⊇ P`; the cut form is a corollary. `retainedCutChildren` is not defined: `retainedMinimalPrimes_sup_span_of_mem` shows it equals `(P ⊔ span {f}).retainedMinimalPrimes s`. The cut potential drops `s ∉ P` and `1 ≤ b`; the finite-zero-locus iff drops radicality, and evaluation injectivity allows points in any algebraically closed extension. `P` and `J` stay prime: the docstring gives the `(xy, xz)`, `f = x - 1` counterexample for non-prime `P`, and dropping primality of `J` needs the Hilbert degree to be a maximum over minimal primes. `exists_mem_retainedCutChildren_of_mem_zeroLocus` is the forward direction of `mem_zeroLocus_and_cut_iff_retained`. Deleted acceptance tests illustrated the roles of integrality, injectivity, radicality, `f ∉ P` and the degree bound; those necessity cases are not retained. Deferred: `CutFamily/Hypersurface.lean` (needs `CutFamily/Iteration` and `Incidence/Excluded`), `PrincipalOpen/{Finite,Presentation,Filtration}.lean`, and the forward finite-zero-locus direction for points in a proper extension. |
| [P5 slice 11: principal-localization presentation, dimension and finite principal opens (#962)](https://github.com/Verified-zkEVM/ArkLib/pull/962) | The presentation of `(k[σ] ⧸ I)_s` and its Hilbert bounds in `ToMathlib/AlgebraicGeometry/PrincipalOpen/{Presentation,Filtration,Dimension}.lean`; the finite-principal-open criterion in `PrincipalOpen/Finite.lean` | `IsLocalization.Away.algebraMap_injective_of_isLeftRegular`, `MvPolynomial.awayPresentation{,_X_none,_X_some,_rename_some,_surjective}`, `awayPresentationIdeal`, `mem_awayPresentationIdeal`, `rename_some_mem_awayPresentationIdeal`, `X_none_mul_rename_some_sub_one_mem_awayPresentationIdeal`, `awayPresentationEquiv{,_mk}`, `affineHilbertFunction_awayPresentationIdeal_le`, `affineHilbertFunction_le_awayPresentationIdeal`, `natDegree_affineHilbertPolynomial_le_awayPresentationIdeal`, `natDegree_affineHilbertPolynomial_awayPresentationIdeal{,_le}`, `natDegree_affineHilbertPolynomial_le_of_surjective_away{,_away}`, `natDegree_affineHilbertPolynomial_le_card_of_{surjective,adjoin_eq_top}_away` in `ArkLib.ToMathlib.RingTheory.MvPolynomial.AwayPresentation`; `image_comp_some_zeroLocus_awayPresentationIdeal`, `injOn_comp_some_zeroLocus_awayPresentationIdeal`, `finite_zeroLocus_awayPresentationIdeal_iff`, `finite_principalOpen_iff_natDegree_affineHilbertPolynomial_eq_zero` in `ArkLib.ToMathlib.RingTheory.Nullstellensatz.PrincipalOpen`; acceptance cases in the matching `ArkLibTest` files | The presentation is defined over any commutative ring. `H(K, N) ≤ H(I, (deg s + 1) N)` holds for every ideal `I` and every `s`. The reverse bound `H(I, N) ≤ H(K, N)` needs only that the class of `s` is regular on the quotient, which replaces the source's primality of `I` with `s ∉ I`, and strengthens the source's `H(I, N) ≤ H(K, 2N)`. The source's numerator–denominator filtration is therefore not ported as declarations. `principalOpenZeroLocus` is not defined; the criterion is stated for `{x ∈ zeroLocus I | eval x s ≠ 0}`. An explicit generating set for `awayPresentationIdeal` is deferred until a consumer needs it. |
| [P5 slice 12: iterated retained principal cuts (#965)](https://github.com/Verified-zkEVM/ArkLib/pull/965) | `retainedCutChildren`, `retainedCutFamily`, `iteratedRetainedCutFamily` and their membership, primality and containment lemmas, `exists_mem_{,iterated}RetainedCutFamily_of_mem_zeroLocus`, `iteratedRetainedCutFamily_singleton_spec`, `sum_{,iterated}RetainedCutFamily_affineDegree_mul_pow_le` in `ToMathlib/AlgebraicGeometry/CutFamily/{Finite,Iteration}.lean` | `Ideal.retainedCutFamily`, `mem_retainedCutFamily`, `of_mem_retainedCutFamily`, `retainedCutFamily_of_forall_mem`, `iteratedRetainedCutFamily{,_nil,_cons,_append}`, `isPrime_of_mem_iteratedRetainedCutFamily`, `notMem_of_mem_iteratedRetainedCutFamily`, `exists_le_of_mem_iteratedRetainedCutFamily`, `exists_mem_iteratedRetainedCutFamily_le`, `retainedMinimalPrimes_subset_iteratedRetainedCutFamily`, `sum_retainedCutFamily_le`, `sum_iteratedRetainedCutFamily_le` in `ArkLib.ToMathlib.RingTheory.Ideal.CutFamily`; `MvPolynomial.exists_mem_iteratedRetainedCutFamily_of_mem_zeroLocus` in `ArkLib.ToMathlib.RingTheory.Nullstellensatz.CutFamily`; `sum_affineDegree_mul_pow_iteratedRetainedCutFamily_le`, `sum_affineDegree_iteratedRetainedCutFamily_le` in `ArkLib.ToMathlib.RingTheory.MvPolynomial.AffineHilbertCutFamily`; acceptance cases in the matching `ArkLibTest` files | `Finite.lean` was already covered by P5 slice 10. A member's children are always its retained minimal primes; the source's `if f ∈ P then {P}` split agrees for prime `P` with `s ∉ P` (`retainedCutFamily_of_forall_mem`). The generic family lemmas hold over any Noetherian commutative semiring, and the weight bounds for any nonnegative weight in an ordered additive monoid. The source's `iteratedRetainedCutFamily_prime_open` splits into two lemmas, each with only its own hypothesis. The potential bound drops `hopen` and `1 ≤ b`; the point cover works for points in any field extension. The current suite keeps a fixed two-cut potential example for `[X₀, X₀²]`; one-cut and `singleton_spec` derivations were in deleted acceptance tests. Deferred: `CutFamily/Hypersurface.lean` and `Incidence/`. |
| [P5 slice 13: agreement incidence on principal opens (#966)](https://github.com/Verified-zkEVM/ArkLib/pull/966) | `sum_retained_affineDegree_le`, `cutsInIdeal`, `cutPoints`, `componentPoints`, `agreementIndices`, `card_filter_cut_le_sum_retained`, `sum_card_cutPoints_le_of_child_bounds`, `card_cuts_mem_prime_lt`, `affineAgreementIncidence_bound` in `ToMathlib/AlgebraicGeometry/Incidence/Agreement.lean`; `affineAgreementIncidence_bound_off_excluded` in `Incidence/Excluded.lean`; `finiteAgreementIncidence_lower` (already on main from #919) | `principalCut_sum_affineDegree_retainedMinimalPrimes_le` in `ArkLib.ToMathlib.RingTheory.MvPolynomial.AffineHilbertPurity`; `ncard_inter_cut_le_sum_retainedMinimalPrimes`, `ncard_inter_cut_le_of_retained_le`, `card_le_of_agreement_off_excluded`, `finite_and_ncard_le_of_agreement_off_excluded`, `ncard_setOf_mem_lt_of_subsingleton`, `card_le_of_agreement_of_subsingleton`, `finite_and_ncard_le_of_agreement_of_subsingleton` in `ArkLib.ToMathlib.RingTheory.Nullstellensatz.AgreementIncidence`; acceptance cases in the matching `ArkLibTest` files | `cutsInIdeal`, `cutPoints`, `componentPoints`, `agreementIndices` and `principalOpenZeroLocus` are not defined; counts are `Set.ncard` of set-builder sets, avoiding `DecidableEq K` and decidable ideal membership. Points may lie in any extension `K`, and the index type is any `Fintype`. The uniqueness bound is a corollary of the excluded-set bound (`excluded = ∅`, `L = m`), so the two source files share one module. Dropped: `s ∉ P`, `0 < b`, `0 < k`, `A ≤ n`. The cut-cover lemma holds for any ideal. `ncard_setOf_mem_lt_of_subsingleton` needs only `[Finite ι]`. Current tests retain fixed agreement-incidence and dimension-sensitive examples; deleted acceptance tests illustrated the need for `L ≤ A` and `s ≠ 0` on `S`; `IsAlgClosed` and `f ∉ P` are justified by docstring counterexamples only. Deferred: `Incidence/GraphPullback.lean`, `ProductBounds.lean` (needs `DimensionSensitive.lean`), non-prime `P`. |
| [P9 slice 3: continuous simplex volumes and Dirichlet integrals (#930)](https://github.com/Verified-zkEVM/ArkLib/pull/930) | `integral_pow_mul_sub_pow` in `ToMathlib/Analysis/Simplex/MonomialIntegral.lean` (with `monomialIntegral`, `monomialIntegral_eq`); `standardSimplex`, `simplexMonomial`, `isCompact_standardSimplex`, `integrableOn_simplexMonomial`, `integral_standardSimplex_succ`, `integral_standardSimplex_eq_monomialIntegral`, `integral_standardSimplex_eq`, `volume_standardSimplex` in `VolumeIntegral.lean`; `coordinateWeight`, `weightedSimplex`, `weightedToStandard`, `standardToWeighted`, `weightedStandardLinearEquiv`, `weightedToStandard_det`, `integral_weightedSimplex_eq_standardSimplex`, `volume_weightedSimplex` in `AffinePushforward.lean` | `integral_pow_mul_one_sub_pow`, `integral_pow_mul_sub_pow` in `ArkLib.ToMathlib.Analysis.Simplex.MonomialIntegral`; `Set.standardSimplex`, `MeasureTheory.setIntegral_standardSimplex_comp_equiv`, `setIntegral_standardSimplex_succ`, `integral_standardSimplex_prod_pow_mul_pow`, `volume_real_standardSimplex`, `volume_standardSimplex` in `...Simplex.VolumeIntegral`; `Set.weightedSimplex`, `MeasureTheory.setIntegral_weightedSimplex`, `integral_weightedSimplex_prod_pow_mul_pow`, `volume_real_weightedSimplex`, `volume_weightedSimplex`, `volume_real_weightedSimplex_succ` in `...Simplex.WeightedVolume`; acceptance cases in the matching `ArkLibTest` files | Arbitrary `Fintype` indices and arbitrary positive real weights replace `Fin n` and `i + 1`; the source's weighted volume is the specialization `volume_real_weightedSimplex_succ`. The beta integral comes from Mathlib's `Complex.betaIntegral` and needs no sign condition on `L`; the Fubini step holds for any integrable function. The list-indexed `monomialIntegral` is dropped because the Fubini recurrence evaluates the integral directly; the diagonal change-of-variables maps are private. Tests show that `0 ≤ L`, positive weights, and `0 ≤ W` are each needed. Deferred: `Simplex/Moments.lean` (linear-form moments, weighted radius, expectations), `RatePartition/{OrderedSimplex,Moment,SharpMoment}.lean`, and floor-cell transfers. |
| [P9 slice 4: moments of linear forms on simplices (#935)](https://github.com/Verified-zkEVM/ArkLib/pull/935) | `integral_standardSimplex_linearForm{,_sq,_cube}`, `integral_weightedSimplex_radius{,_sq,_cube}`, `weightedSimplexExpectation_radius{,_sq,_cube}`, `weightedSimplexFiniteMeasure`, `weightedSimplexProbabilityMeasure`, `weightedSimplexExpectation_eq_integral_probability`, and the private coordinate integrals and multiplicity counts in `ToMathlib/Analysis/Simplex/Moments.lean` | `MvPolynomial.eval_hsymm_eq_sum_piAntidiag`, `two_mul_eval_hsymm_two`, `six_mul_eval_hsymm_three` in `ArkLib.ToMathlib.MvPolynomial.CompleteHomogeneous`; `MeasureTheory.integral_standardSimplex_linearForm_pow`, `integral_weightedSimplex_linearForm_pow`, `setAverage_{standard,weighted}Simplex_linearForm_pow`, `setAverage_weightedSimplex_linearForm{,_sq,_cube}`, `setAverage_weightedSimplex_succ_sum{,_sq,_cube}`, `isProbabilityMeasure_cond_weightedSimplex` in `ArkLib.ToMathlib.Analysis.Simplex.Moments`; acceptance cases in the matching `ArkLibTest` files | The degree-1/2/3 source lemmas are cases of one `k`-th moment theorem over any `Fintype`, with arbitrary positive weights and coefficients; index-coincidence counting is replaced by the multinomial theorem and Mathlib's `hsymm`. The source's expectation and probability measure are Mathlib's set average and conditional measure `volume[|weightedSimplex w W]`, so no new definitions are introduced, and its expectation-as-integral lemma is Mathlib's `setAverage_eq'`. Named sums (`simplexLinearForm`, `weightedRadius`, `harmonicPowerSum`, …) are written out; `harmonicPowerSum_one` becomes Mathlib's `harmonic`. Deferred: continuity/integrability lemmas for the radius (to their consumer), centered moments, the discrete `DiscreteSimplex/{Moments,Variance}.lean`, and floor-cell transfers. |
| [P9 slice 5: exact discrete moments and weighted variance (#941)](https://github.com/Verified-zkEVM/ArkLib/pull/941) | `splitCoordinate`, `mergeCoordinate`, `simplexCoordinateSplitEquiv`, `ordinarySimplex_coordinate_le`, `sum_simplex_coordinate_succ`, `sum_simplex_mixed_succ`, `sum_simplex_factorial_succ`, `simplex_first_moment`, `simplex_mixed_moment`, `simplex_factorial_moment` in `ToMathlib/Combinatorics/DiscreteSimplex/Moments.lean`; `simplexAverage`, `simplexWeightedMean`, `simplexWeightedVariance`, `simplex_weighted_sum`, `simplex_weighted_square_sum`, `simplex_average_*`, `simplexWeightedVariance_nonneg`, `simplex_upper_tail_count` in `.../DiscreteSimplex/Variance.lean` | `Finset.natSimplexSplit`, `sum_natWeightedSimplex_one_sum_range_split`, the first/mixed/factorial and weighted moment identities (`card_add_one_mul_…`) in `ArkLib.Data.Finset.WeightedSimplex.Moments`; `natSimplexWeightedMean`, `natSimplexWeightedVariance`, `expect_natWeightedSimplex_one_weighted{,_sq,_sub_sq}`, `natSimplexWeightedVariance_nonneg`, `sq_mul_card_filter_le_abs_sub_le_card_mul_variance`, `sq_mul_card_filter_mean_add_le_le_card_mul_variance` in `ArkLib.Data.Finset.WeightedSimplex.Variance`; acceptance cases in the matching `ArkLibTest` files | Stated for the unit-weight `Finset.natWeightedSimplex` over any `Fintype`. The split/merge equivalence becomes `sum_natWeightedSimplex_one_sum_range_split`, a reindexing of Finset sums onto the simplex over `Option σ` rather than an equivalence of subtypes. The source's `simplexAverage` is Mathlib's `Finset.expect`. The counting identities are over `ℕ` and cast to any commutative ring, averages over characteristic-zero fields, and inequalities over ordered fields. The tail count covers the full filtered tail and follows from a new two-sided Chebyshev bound. The source's `Fin r` statements are test derivations. Deferred: non-unit weights, the one-sided Cantelli bound, and the discrete-versus-continuous variance comparison. |
| [P9 slice 6: floor-cell transfers and positive-part moment bounds (#943)](https://github.com/Verified-zkEVM/ArkLib/pull/943) | `integral_biUnion_le_sum_of_cell_measure_le_one`, `sum_le_integral_of_unit_cells` in `ToMathlib/MeasureTheory/Integral/FiniteCells.lean`; the cell API and `sum_natFloor_bounds` in `ToMathlib/MeasureTheory/Integral/NaturalFloorCells.lean`; `positivePart_pointwise` in `HiddenDerivative/Interpolation/WeightedSupport/PositivePart.lean`; `positivePart_mean_variance`, `weighted_residual_sum_le_integral` in `.../WeightedSupport/RankIntegral.lean`; `floor_higher_mem` in `.../RatePartition/Integral.lean`; `floorCell_subset_weightedSimplex`, `residual_le_on_floorCell` in `.../WeightedSupport/CubeTransfer.lean`; `floor_remaining`, `floor_cubic`, `weighted_floor_integral` in `.../WeightedSupport/FloorTransfer.lean`; `coordinateWeight_sum` in `.../PartitionSupport/RankBound.lean`; `weightedSupport_variance_factor_gt`, `weightedSupport_third_factor_le`, `weightedSupport_third_factor_numeric` in `.../WeightedSupport/MomentBounds.lean` | `MeasureTheory.setIntegral_biUnion_le_sum`, `sum_le_setIntegral_of_measure_eq_one`, `natFloorCell` with membership, measurability, unit volume, floor characterization, disjointness and the two weighted-sum bounds in `ArkLib.ToMathlib.MeasureTheory.Integral.NatFloorCells`; `max_sub_zero_le_sub_add_sq_div`, `Finset.expect_max_sub_zero_le`, `MeasureTheory.integral_max_sub_zero_le` in `ArkLib.ToMathlib.MeasureTheory.Integral.PositivePart`; `natFloor_mem_natWeightedSimplex`, `natFloorCell_subset_weightedSimplex`, `setIntegral_le_sum_natWeightedSimplex`, `sum_natWeightedSimplex_le_setIntegral` in `ArkLib.Data.Finset.WeightedSimplex.FloorTransfer`; the three moment-factor bounds in `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.WeightedSupport.MomentBounds`; `floor_remaining`, `floor_cubic`, `sum_fin_succ_eq_choose_two`, `weighted_floor_integral`, `weighted_residual_sum_le_integral` in `...WeightedSupport.FloorTransfer`; acceptance cases in the matching `ArkLibTest` files | The cell and positive-part lemmas move out of the RS namespace to measure-theory owners; the cellwise integral bound is one-sided instead of a norm bound, and the positive-part bound holds over any ordered field with a new finite-average form. The floor transfers are stated for arbitrary positive natural weights on `Finset.natWeightedSimplex` and `Set.weightedSimplex`; the hidden-derivative statements use `natWeightedSimplex (fun i : Fin n ↦ i + 1)` with `n` for `d - 1`, since `weightedHigherJetTuples` and `higherJetTupleDegree` are not yet ported. Dimension hypotheses weaken from `10000 ≤ d` to the sharp `150 < d` and from `48000 ≤ d` to `1 ≤ d`; `0 ≤ g * m` is dropped from `floor_cubic` and `weighted_floor_integral`, and integrability from `weighted_residual_sum_le_integral`. Deferred: the continuous centered moments of `WeightedSupport/Moments.lean` (`normalizedRadius` and its moments; they need P9 slice 4), `weighted_dimension_integral` (needs `weightedSupportSpace`), `Estimate.lean`, the rest of `RankIntegral.lean`, the `bounded_region_*` lemmas, the Cantelli bound and non-unit-weight moments. |
| [P9 slice 7: centered radius moments and the cubic contribution bound (#948)](https://github.com/Verified-zkEVM/ArkLib/pull/948) | `integrable_weighted_probability`, `normalizedRadius`, `continuous_normalizedRadius`, `integral_normalizedRadius{,_sq,_cube}` in `HiddenDerivative/Interpolation/WeightedSupport/Moments.lean`; `normalizedRadius_contribution_lower` in `.../WeightedSupport/Estimate.lean`; `positive_cube_moments`, `cubic_le_positive_cube`, `cubic_numeric`, `contribution_integral_lower` in `.../WeightedSupport/Cubic.lean` | `MeasureTheory.IntegrableOn.integrable_cond`, `setAverage_weightedSimplex_linearForm_sub_mean{,_sq,_cube}` in `ArkLib.ToMathlib.Analysis.Simplex.CenteredMoments`; `MeasureTheory.le_integral_max_sub_zero_pow_three` in `ArkLib.ToMathlib.MeasureTheory.Integral.PositivePart`; `normalizedRadius`, `continuous_normalizedRadius`, `setAverage_normalizedRadius{,_sq,_cube}`, `cubic_contribution_numeric`, `contribution_integral_lower`, `normalizedRadius_contribution_lower` in `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.WeightedSupport.Moments`; acceptance cases in the matching `ArkLibTest` files | The centered second and third moments hold for any linear form on any weighted simplex over a `Fintype`, in terms of the power sums of `c i / w i`; the harmonic-weight radius is a specialization. Moments are set averages, following P9 slice 4; the tests derive the source's `volume[|S]` integral forms. The source's `weightedSimplexProbabilityMeasure` wrapper is replaced by `IntegrableOn.integrable_cond`. The mean needs no hypothesis on `W`, and the higher moments need only `0 ≤ W` (a test shows `W = -1` breaks the variance formula). The positive-part cube bound is a general probability lemma with the source's two derived integrability hypotheses dropped. `normalizedRadius_contribution_lower` drops `48000 ≤ n + 1` (the harmonic hypotheses already force `n + 1 ≥ 225`) and weakens `0 < t` to `0 ≤ t`. `cubic_numeric` is renamed `cubic_contribution_numeric`; `s ≤ 10/27` is tested sharp. Deferred: `weighted_dimension_probability`, `weighted_dimension_lower` (need `weightedSupportSpace`), `Margin.lean`, `RankIntegral.lean`, `NormalizedRank.lean`, and `positive_cube_tangent`, `positive_cube_jensen`, `positive_cube_convex` (no consumer). |
| [P9 slice 8: weighted rank integrals and real-cutoff staircases (#957)](https://github.com/Verified-zkEVM/ArkLib/pull/957) | `weighted_residual_sum_le_volume_mul_mean_variance`, `weightedSimplex_centeredRadius_sq_le_harmonic`, `weighted_residual_sum_le_volume_mul_harmonic_variance`, `volume_weightedSimplex_add_choose_le_exp` in `HiddenDerivative/Interpolation/WeightedSupport/RankIntegral.lean`; `CubicStaircase.*` and `QuadraticStaircase.*` in `ToMathlib/Combinatorics/`; `Real.sum_staircase_mul_exp_le` in `ToMathlib/Analysis/ExponentialStaircase.lean` | `MeasureTheory.setIntegral_max_sub_zero_le` in `ArkLib.ToMathlib.MeasureTheory.Integral.PositivePart`; `setAverage_weightedSimplex_linearForm_sub_mean_sq_le` in `ArkLib.ToMathlib.Analysis.Simplex.CenteredMoments`; `volume_real_weightedSimplex_add_le_mul_exp` in `ArkLib.ToMathlib.Analysis.Simplex.WeightedVolume`; `Finset.sum_natWeightedSimplex_max_sub_add_one_le{,_of_setAverage_sq_le}` in `ArkLib.Data.Finset.WeightedSimplex.RankIntegral`; `CubicStaircase.{count,Slot,card_slot,Slot.exponents,Slot.exponents_injective,Slot.weighted_degree_lt,six_mul_sum,cube_div_six_le_sum,count_ge_cubic}` in `ArkLib.ToMathlib.Combinatorics.CubicStaircase`; `QuadraticStaircase.{count,Slot,card_slot,Slot.exponents,Slot.exponents_injective,Slot.weighted_degree_lt,two_mul_sum,square_div_two_le_sum,count_ge_quadratic}` in `ArkLib.ToMathlib.Combinatorics.QuadraticStaircase`; `Real.sum_staircase_mul_exp_le` in `ArkLib.ToMathlib.Analysis.ExponentialStaircase`; the four source-named specializations in `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.WeightedSupport.RankIntegral`; acceptance cases in the matching `ArkLibTest` files | The residual-sum bound holds for any finite index type, positive natural weights and nonnegative coefficients; the source's weights `i + 1` and coefficients `1` are a specialization, and its hypotheses `1 ≤ d` and `0 < W'` are dropped. The positive-part step is a set-integral lemma for any measure and set, including sets of infinite measure. The variance bound needs no hypothesis on `W`. The exp volume bound holds for any real enlargement `r`; `0 < W` is tested necessary. `cube_div_six_le_sum` drops `0 < L`; `square_div_two_le_sum` weakens it to `0 ≤ L`, tested necessary. The real-cutoff staircases stay under their source paths because `ArkLib/Data/Finset/Staircase.lean` is P3's natural-number staircase. Deferred: `NormalizedRank.lean`, `Margin.lean`, `weighted_dimension_probability`, `weighted_dimension_lower` (need `WeightedSupport/Basic`, `WeightedSupport/Dimension` and the harmonic numeric lemmas), and the orphan `CubicStaircase.Slot.exists_of_weighted_degree_lt`. |
| [P9 slice 9: weighted support space, its dimension, and the probability estimate (#964)](https://github.com/Verified-zkEVM/ArkLib/pull/964) | `WeightedSupportEligible`, `weightedSupportBasis`, the jet-degree and decoder bounds in `HiddenDerivative/Interpolation/WeightedSupport/Basic.lean`; `WeightedSupportSlot`, `weightedSupportSlotExponent`, `card_weightedSupportSlot_{le,eq}`, `weightedSupport_dimension_ge_cubic_sum` in `WeightedSupport/Dimension.lean`; `weighted_dimension_integral` in `WeightedSupport/FloorTransfer.lean`; `weighted_dimension_probability` in `WeightedSupport/Estimate.lean`; `exactInterpolationMonomialWeight_le_coarse` in `Interpolation/Index.lean` | `WeightedSupportEligible`, `degree_lt_ceil_of_weightedSupportEligible`, `weightedSupportEligible_finite`, `weightedSupportExponents`, `mem_weightedSupportExponents`, `weightedSupportSpace`, `mem_weightedSupportSpace_iff`, `weightedSupportSpaceBasis`, `finrank_weightedSupportSpace_eq_card`, `totalJetDegree_lt_of_weightedSupportEligible`, `totalJetDegree_le_pred_of_weightedSupportEligible`, `weight_differentialWeight_lt_of_weightedSupportEligible`, `weightedSupportSpace_le_exactInterpolationSpace`, `differentialWeightedDegree_lt_of_mem_weightedSupportSpace`, `jetTotalDegree_lt_of_mem_weightedSupportSpace`, `decoder_bounds_of_mem_weightedSupportSpace` in `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.WeightedSupport.Basic`; `weightedSupportEligible_coordinates_iff`, `weightedSupportSlotExponent{,_eligible,_injective}`, `sum_count_le_card_weightedSupportExponents`, `sum_count_le_finrank_weightedSupportSpace`, `weightedSupport_dimension_ge_cubic_sum` in `...WeightedSupport.Dimension`; `weighted_dimension_integral`, `weighted_dimension_probability` in `...WeightedSupport.Estimate`; `weight_le_add_mul_totalJetDegree` in `...Interpolation.Index`; acceptance cases in the matching `ArkLibTest` files | The weighted support space is `MvPolynomial.restrictSupport` on the eligible exponents. `DimensionBridge.lean` is covered by main's `jetExponentCoordinatesEquiv`; `TotalJetDegreeRootCount` is covered by main's `jetTotalDegree`. The dependent `WeightedSupportSlot` type becomes a `Finset.sigma` inside the proof. `Field` is weakened to `CommSemiring` in the jet-degree and decoder bounds; `0 ≤ g * m` is dropped from the integral form and `0 < W`, `0 < g * m` from the probability form; `0 < D` is tested necessary. The probability-measure integral is a set average and `harmonicPowerSum (d - 1) 1` is `harmonic (d - 1)`. Deferred: `weighted_dimension_lower` (needs four harmonic-number estimates). |
| [P9 slice 1: discrete weighted-simplex counts (#917)](https://github.com/Verified-zkEVM/ArkLib/pull/917) | `OrdinarySimplex`, `ExactSimplex`, `ordinaryToExact`, `ordinarySimplexEquivSym`, `card_ordinarySimplex` in `ToMathlib/Combinatorics/DiscreteSimplex/Basic.lean`; `ScaledResidue`, `card_scaledResidue`, `ordinaryToScaledWithResidue` and its injectivity theorem, `scaledWithResidueToOrdinary` and its injectivity theorem, `pow_le_factorial_mul_card_ordinarySimplex`, `factorial_mul_card_ordinarySimplex_le_pow`, `scaledExponentCount_factorial_sq_sandwich` in `HiddenDerivative/Parameters/Lattice/ScaledLattice.lean`; `weightedHigherJetTuples`, `higherJetTupleWeight`, `higherJetTuple_apply_le_weight`, `mem_weightedHigherJetTuples` in `HiddenDerivative/Interpolation/Counting.lean`; `ratePartitionTupleCount_factorial_sandwich` and `ratePartitionTupleCount_le_volume` in `RatePartition/RankEstimate.lean` are motivating consumers | `Finset.natWeightedSimplex` and its membership, unit-weight stars-and-bars, binomial comparisons, factorial sandwich, ordered-field upper bound, and `1, …, n` specialization in `ArkLib.Data.Finset.WeightedSimplex`; acceptance cases in `ArkLibTest.Data.Finset.WeightedSimplex` | Arbitrary finite indices and positive natural weights replace `Fin (d - 1)` and weights `i + 1`. Quotient/remainder maps stay private; their public cardinal comparisons retain the sharp binomial bounds. The lower power bound strengthens `W^n` to `(W+1)^n`. The upper bound also handles zero weights, while the finite box keeps the set computable. The ordered-field bound is the form needed by `ratePartitionTupleCount_le_volume`, and the specialization uses `(n+1).choose 2` instead of natural division. The exact-simplex, `Finsupp`, and shell interfaces are delivered by P9 slice 2. Deferred: continuous simplex volumes, floor-cell transfers, and moments. |
| [P9 slice 2: exact-simplex, `Finsupp`, and shell-count bridges (#925)](https://github.com/Verified-zkEVM/ArkLib/pull/925) | `OrdinarySimplex`, `ExactSimplex`, `ordinaryToExact`, `exactToOrdinary` in `ToMathlib/Combinatorics/DiscreteSimplex/Basic.lean`; `weightedHigherJetShell`, `mem_weightedHigherJetShell`, `higherJetTupleWeight_equivFunOnFinite`, `weightedHigherJetExponentEquiv`, `weightedHigherJetShellEquiv`, `card_weightedHigherJetExponent_eq_count`, `card_weightedHigherJetShellExponent_eq_count`, `weightedHigherJetCount_eq_sum_shellCount` in `HiddenDerivative/Interpolation/Counting.lean` | `Finset.natWeightedSimplexShell`, its positive-weight membership theorem, `Finset.weightedSum_equivFunOnFinite`, bounded and exact-weight `Finsupp` equivalences and cardinality theorems, `Finset.card_natWeightedSimplex_eq_sum_card_shell`, and `Finset.natWeightedSimplexOneEquivExact` in `ArkLib.Data.Finset.WeightedSimplex`; ordinary-import clients in `ArkLibTest.Data.Finset.WeightedSimplex` | The generic owner works over any finite index type and arbitrary positive natural weights. Both tuple finsets remain executable for zero weights, but the `Finsupp` equivalences and shell decomposition require nonzero weights because an unconstrained coordinate makes the canonical subtype infinite; acceptance cases preserve this boundary. The exact-simplex equivalence exposes the slack coordinate directly. Deferred: the thin higher-jet aliases and `goodHigherExponents_self_eq_weighted_count` until the hidden-derivative interpolation space lands; continuous simplex volumes, floor cells, and moments remain later P9 slices. |
| [P12a slice 2: generic interleaving transfer (#918)](https://github.com/Verified-zkEVM/ArkLib/pull/918) | Source-private `interleaved_powerProjectionBad_card_le` in `ReedSolomon/Interleaved/PowerAgreement.lean`, `interleaved_powerProjectionBadArbitrary_finset_card_le` in `PowerAgreementArbitrary.lean`, and `interleaved_lineProjectionBad_card_le` in `TensorFoldAgreement.lean`, all at `a5aa2677`; main's `ProximityGap.mcaError_interleaved_le` | Seed-free `Code.projectedWord_rowCombination_mem`, `Code.goodRowFunctionals`, and `Code.exists_rowFunctional_forall_notMem` in `ArkLib.Data.CodingTheory.InterleavedCode.Projection`; seedwise and numeric transfer in `ArkLib.Data.CodingTheory.ProximityGenerator.Interleaving` | Depends on #912 for sharp finite-field avoidance. An arbitrary generator with at most `|F|` seeds has `mcaError G (C^⋈κ) δ ≤ mcaError G C δ` for finite `κ` and any real `δ`; the reverse holds for every generator and nonempty `κ`, even infinite. The equality needs both bounds. Existing affine-line inequalities in `ProximityGap.Errors` and `TensorMCA.isMCAGenerator_of_moduleInterleavedCode` now consume these theorems without changing signatures; ordinary-import clients cover powers, empty and nonempty rows, and the seed-free lemma over `ℚ`. Semiring row helpers work at `Type*`; the unified finite/infinite avoidance lemma uses `Field` because pinned Mathlib's infinite-submodule avoidance requires it, while #912's finite lemma works over a division ring. Deferred: the source's scalar exception-set counting and exact-agreement wrappers, Jo26's field-size-weighted case `|S| > |F|`, and the separate tensor-tight theorem. |
| [P12a slice 3: exact agreement transfer (#932)](https://github.com/Verified-zkEVM/ArkLib/pull/932) | Private `powerProjectionBad`, `scalar_powerProjectionBad_card_le`, `interleaved_powerProjectionBad_card_le`, public `uniformExactInterleavedPowerAgreement_of_scalar`, `interleavedCodeword_eq_of_agree_on`, `exactNestedPowerAgreement_of_interleaved`, `nestedPowerAgreement_sharedInner` in `ReedSolomon/Interleaved/PowerAgreement.lean`; `powerProjectionBadArbitrary` and `uniformExactInterleavedPowerAgreement_of_scalar_arbitrary` in `PowerAgreementArbitrary.lean` | `Code.IsProjectionBad`, `HasExactAgreement`, `UniformExactAgreement`, `DeterminedByAgreement`, `DeterminedByAgreement.moduleInterleavedCode`, `forall_hasExactAgreement_iff_not_isProjectionBad`, `uniformExactAgreement_iff_encard_le`, `encard_setOf_isProjectionBad_moduleInterleavedCode_le`, `uniformExactAgreement_moduleInterleavedCode` in `ArkLib.Data.CodingTheory.InterleavedCode.ExactAgreement`; power batching, `HasExactPowerAgreement`, `UniformExactPowerAgreement`, `determinedByAgreement_code` and the code-level identifications in `ArkLib.Data.CodingTheory.ReedSolomon.PowerAgreement`; `uniformExactInterleavedPowerAgreement_of_scalar`, `exactNestedPowerAgreement_of_interleaved`, `nestedPowerAgreement_sharedInner` in `ArkLib.Data.CodingTheory.ReedSolomon.Interleaved.PowerAgreement`; `ProximityGap.isMCA_iff_isProjectionBad` | The counting transfer holds for any module code, batching map and field, with hypothesis `ENat.card S ≤ ENat.card F ∨ e < ENat.card F` in place of `[Finite F]`; the Reed–Solomon hypothesis `k ≤ agreement` becomes `DeterminedByAgreement`. The source's finite-field and arbitrary-field files collapse to one theorem. The seedwise MCA transfer of #918 is now derived from the generic lemma. Deferred: `TensorFoldAgreement.lean` (shared-level fold, tensor-tight), `Anchored*`, `nestedPowerAgreement_probability_bound`, and the P12b endpoints. |
| [P12a slice 4: shared-level binary tensor fold (#937)](https://github.com/Verified-zkEVM/ArkLib/pull/937) | `fullAgreementSet`, `binaryLineFold`, `binaryEqualityGenerator`, `binaryTensorFold`, `familyAgreementSet`, `FullSetLevelWitness`, `HasFullTensorDecomposition`, `levelExceptional`, `TensorFoldFamilyGood`, `TensorFoldGood`, `tensorFoldFamilyBad`, `tensorFoldBad`, `binaryTensorFold_eq_tensorGeneratorPi`, `tensorFoldBad_card_le`, `hasFullTensorDecomposition_of_not_mem_bad` and their helpers in `ProximityGenerator/BinaryTensorFoldAgreement.lean`; private `lineProjectionBad`, `interleaved_lineProjectionBad_card_le`, `scalar_lineProjectionBad_card_le`, `exists_exceptional_fullSetLine_interleaved_of_exactAgreement`, and `fullSetLevelWitness_interleaved_of_exactAgreement` in `ReedSolomon/Interleaved/TensorFoldAgreement.lean` | `TensorMCA.FullSetLevelWitness`, `fullSetLevelWitness_of_uniformExactAgreement`, `fullSetLevelWitness_iff`, `FullSetLevelWitness.moduleInterleavedCode`, `isProjectionBad_binaryEqualityGenerator_iff`, `binaryTensorFold_eq_tensorGeneratorPi`, `tensorFoldFamilyBad_card_le`, `tensorFoldBad_card_le`, `hasFullTensorDecomposition_of_not_mem_bad` and the fold definitions in `ArkLib.Data.CodingTheory.ProximityGenerator.BinaryTensorFoldAgreement`; the Reed–Solomon specializations `fullSetLevelWitness_code`, `fullSetLevelWitness_interleaved_of_exactAgreement`, `interleavedRS_tensorFoldBad_card_le_heightThree` in `ArkLib.Data.CodingTheory.ReedSolomon.Interleaved.TensorFoldAgreement`; acceptance cases in the matching `ArkLibTest` files | The level witness is proved for an arbitrary module code determined by `a` agreements, and is equivalent there to the single-line uniform exact-agreement guarantee; interleaving preserves it with the same count and no hypothesis. The Reed–Solomon file only supplies `k ≤ agreement` as `Code.DeterminedByAgreement`, and the source width hypothesis is dropped. Universes are arbitrary and the field is weakened to a ring outside the transfer section. `FullSetLevelWitness` no longer requires a nonempty family type, and the source-private family decomposition and family count are public. Deferred: the probability form of the count (`BinaryTensorFoldProbability.lean`) and the anchored-agreement consumers. |
| [P12a slice 5: tensor-fold probability, interleaved agreement bounds, anchored reconstruction (#938)](https://github.com/Verified-zkEVM/ArkLib/pull/938) | `tensorFoldBad_probability_le` and `tensorFoldBad_probability_height_three` in `ProximityGenerator/BinaryTensorFoldProbability.lean`; `packRowPolynomials*`, `tupleRatFunc`, `tupleRatFunc_injective`, `ringChar_ratFunc`, `lambda_interleaved_rs_le_of_ratFunc_polynomial_agreement_bound` in `ReedSolomon/Interleaved/AgreementBounds.lean`; `cubicAnchorDivisor`, `cubicAnchorReconstruct` and their degree/evaluation lemmas, the `X ^ T - 1` trace-remainder lemmas in `ReedSolomon/Interleaved/AnchoredReconstruction.lean` | `TensorMCA.tensorFoldFamilyBad_probability_le`, `tensorFoldBad_probability_le`, `prob_not_hasFullTensorDecomposition_le`, `tensorFoldBad_probability_height_three` in `ArkLib.Data.CodingTheory.ProximityGenerator.BinaryTensorFoldProbability`; `Code.encard_closeCodewordsRel_le_of_injective_comp`, `Code.Lambda_le_of_injective_comp` in `ArkLib.Data.CodingTheory.ListDecodability.SymbolMap`; `Polynomial.degree_mul_add_lt`, `eval_mul_add_of_eval_eq_zero`, `eval_mul_add_of_eval_mul_eq_sub`, `degree_nodal_mul_add_lt`, `eval_nodal_mul_add_at_node` in `ArkLib.Data.Polynomial.DivisorReconstruction`; `ReedSolomon.comp_mem_code_map`, `pack_comp_mem_code_map`, `Lambda_interleaved_le_of_injective_pack`, `tupleRatFunc`, `tupleRatFunc_injective`, `Lambda_interleaved_le_ratFunc` in `...ReedSolomon.Interleaved.AgreementBounds`; `cubicAnchorDivisor{,_eq_nodal}`, `cubicAnchorReconstruct` and its laws, `traceRemainder_degree_lt`, `traceRemainder_eval_eq` in `...ReedSolomon.Interleaved.AnchoredReconstruction`; acceptance cases in the matching `ArkLibTest` files | The probability bound holds for finite rings and for families; the injection step behind the source's rational-function bound is a generic Lambda comparison under any injective symbol map, and the Reed–Solomon statement becomes a Lambda-to-Lambda inequality at every radius for any injective packing, removing the dependency on `capacityRadius`. The cubic-anchor lemmas are instances of generic divisor reconstruction with `Lagrange.nodal`, over commutative rings, without `0 < k`; trace remainders use `X ^ T - C c` for any `c`. `ringChar_ratFunc` is Mathlib's `CharP` instance and `tuplePolynomial` is unneeded. Deferred: `mcaError_interleaved_le_of_exactAgreement` (needs `MutualCorrelatedAgreement/LineToAffine`'s exact-agreement bounds), `lambda_rs_le_of_finite_polynomial_agreement_bound` (needs `ListDecodability/Capacity/CodewordBound`), and `AnchoredAgreement.lean`. |
| [P12a slice 6: anchored agreement, point-collision bound, nested probability (#945)](https://github.com/Verified-zkEVM/ArkLib/pull/945) | `tupleEvaluation`, `tuple_eq_of_evaluation_eq`, `candidateSet_injective`, `CandidateSet`, `candidateSet_finite`, `candidateFamily`, `mem_candidateFamily`, `candidateFamily_card_le`, `badAnchorPairs`, `badAnchorRate`, `badAnchorRate_eq`, `not_mem_collisionSet_of_sampled_of_not_mem_badAnchorPairs`, `badAnchorRate_le`, `candidateFamily_badAnchorRate_le`, `cubicReconstructedTuple_agreement`, `successfulCubicReconstruction_mem_and_values`, `LaterCubicReconstruction`, `SuccessfulCubicReconstruction`, `exists_selectedCandidate_before_later`, `traceRemainderTuple*`, `exists_selectedTrace_before_later`, `twoAnchorValues` in `ReedSolomon/Interleaved/AnchoredAgreement.lean`; `card_twoPointRootPairs_le`, `card_collisionSet_le`, `collisionRate_le` and the outside-domain/ordered-pair counts in `Data/Probability/TwoPointPolynomialCollision.lean`; `relHammingDist_le_capacityRadius_iff_agreementThreshold_le` as used there; `nestedPowerAgreement_probability_bound` in `ReedSolomon/MutualCorrelatedAgreement/NestedPowerAgreement.lean` | `Polynomial.evalTuple`, `evalTuple_mul_add_of_eval_eq_zero`, `exists_option_eq_some_iff_of_injOn_evalTuple`, `card_le_of_evalTuple_eq`, `card_le_of_not_injOn_evalTuple` in `ArkLib.Data.Polynomial.PointCollision`; `prob_not_injOn_evalTuple_le`, `prob_not_injOn_evalTuple_le_of_encard_le` in `ArkLib.Data.Polynomial.PointCollisionProbability`; `Code.relHammingDist_le_one_sub_div_of_le_agree`, `mem_closeCodewordsRel_of_le_agree`, `encard_setOf_le_agree_le_Lambda`, `encard_setOf_le_agree_encode_le_Lambda` in `ArkLib.Data.CodingTheory.ListDecodability.AgreementRadius`; `ReedSolomon.AnchoredAgreement.candidateSet`, `injOn_evalTuple_of_degree_lt`, `encard_candidateSet_le_Lambda`, `finite_candidateSet_of_Lambda_le`, `prob_not_injOn_candidateSet_le`, `card_offDiag_compl_map`, `prob_not_injOn_candidateSet_offDiag_le`, `agree_evalTuple_mul_add`, `mul_add_mem_candidateSet`, `exists_selected_before_reconstruction`, `exists_selectedCandidate_before_later` in `ArkLib.Data.CodingTheory.ReedSolomon.Interleaved.AnchoredAgreement`; `ReedSolomon.nestedPowerAgreement_probability_le` in `…Interleaved.PowerAgreement`; retained concrete acceptance examples in `ArkLibTest/Data/CodingTheory/ReedSolomon/Interleaved.lean`, including the trace-remainder degree and evaluation properties | The collision event is `¬ Set.InjOn (evalTuple x) S` for any number of anchors and any finite sample space mapped injectively to anchor tuples; the two-anchor outside-domain case is the `offDiag` specialization, and its bound `choose L 2 (K-1)² / ((q-n)(q-n-1))` is at most the source's rate `choose L 2 ((T+2)/(q-n-1))²`. The list-size radius is `1 - A/n`, which is at most `capacityRadius` whenever `agreementThreshold ≤ A`, so the source form follows by `Lambda_mono`; `0 ≤ delta` and `0 < n` are dropped. The degree bound `K` is a parameter instead of `T + 3`, and `0 < k` is dropped from evaluation injectivity. The finiteness-built `candidateFamily` is replaced by the set with an `encard` bound. The bundled reconstruction structures become unbundled hypotheses; `s₁ ≠ s₂`, `z ≠ sᵢ`, `claimedZ` and `0 < T` are unused and dropped, and the selection theorem takes separation directly. Reconstruction holds for any divisor vanishing at the anchors, with the cubic divisor as a specialization. The nested-agreement arithmetic lemma is replaced by the probability of the failure event itself. Deferred: application-level consumers that supply a concrete `Lambda` bound. |
| [P3 slice 1: local constraint maps and rank bounds (#934)](https://github.com/Verified-zkEVM/ArkLib/pull/934) | `HiddenDerivative/Variables.lean` (local variables and weights), `Substitution.lean` (unscaled local substitution), `Interpolation/Index.lean` (exact interpolation space) with the pieces of `Interpolation/Space.lean` it uses, `Interpolation/Local/ConstraintMap.lean`, `Interpolation/Local/Rank.lean` including `finrank_range_le_sub_finrank_of_injective_to_ker` and `finrank_range_comp_le_outer` | `MvPolynomial.restrictWeightedOrder`, `filterSupport`, `weightedTruncation`, `bind₁_mem_restrictWeightedOrder`, `weightedTruncation_bind₁_weightedTruncation` in `ArkLib.Data.MvPolynomial.WeightedOrder`; `LinearMap.finrank_range_le_sub_of_injective_ker`, `LinearMap.finrank_range_comp_le_left` in `ArkLib.ToMathlib.LinearAlgebra.FiniteDimensional`; `exactInterpolationSpace`, `exactInterpolationSpaceBasis`, `finrank_exactInterpolationSpace_eq_card` in `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Index`; the constraint maps and `SatisfiesLocalConstraints` in `.Interpolation.Local.ConstraintMap`; `finrank_range_exactLocalConstraintAt_le_enlarged`, `finrank_range_exactLocalConstraintAt_le_sub` in `.Interpolation.Local.Rank` | Hidden-derivative modules live under `ArkLib/Data/CodingTheory/HiddenDerivative/` instead of the source's `ReedSolomon/HiddenDerivative/`, because generated `public import` lines for the source paths exceed 100 columns; the Lean namespace stays `ReedSolomon.HiddenDerivative`. The source's exponent-level jet weights are `Finsupp.weight` of pointwise weights and its `totalJetDegree` is P1 slice 2's, so no second jet-weight representation exists. The truncation step is the general `weightedTruncation_bind₁_weightedTruncation` for any weighted substitution. No characteristic guard appears. Deferred: the support-first rectangular space, `IntermediateSpace`, `ConstraintKernel`, `KernelSliceIndependence`, `Counting`, the `certifiedEnlargedRankBound` theorems (P3 slice 2), and the local identity layer (`localPolynomialEvaluation`, `shiftedJetSubstitution`, backward-error identity). |
| [P3 slice 2: exhibited kernel and certified local rank bound (#940)](https://github.com/Verified-zkEVM/ArkLib/pull/940) | `truncateLocalT_sum_exhibitedKernelFactor_mul_eq_zero_iff` induction and the private `support_weight_*_le` lemmas; `weightedHigherJetCount`, `contactThreshold`, `ambientContactCount`, `exhibitedKernelContactCount`, `certifiedContactRankBudget`, `certifiedEnlargedRankBound` in `HiddenDerivative/Interpolation/Counting.lean`; `hiddenErrorFactor`, `exhibitedKernelFactor`, `exhibitedKernelMultiplier` and kernel membership in `Local/ConstraintKernel.lean`; the intermediate space, kernel-slice source space and their dimensions in `Local/IntermediateSpace.lean`; `localTCoefficient` and family independence in `Local/KernelSliceIndependence.lean`; the certified bound in `Local/Rank.lean` | `LinearMap.injective_sum_comp_proj_of_triangular` in `ArkLib.ToMathlib.LinearAlgebra.TriangularInjective`; `MvPolynomial.restrictWeightAtMost` with its algebra and substitution laws, `restrictSupport_finite`, `finrank_restrictSupport_finset` in `ArkLib.Data.MvPolynomial.WeightAtMost`; the counting definitions and `ambient_sub_exhibitedKernel_eq_certifiedEnlargedRankBound` in `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Counting`; `exhibitedKernelMultiplier_mem_ker`, `exhibitedKernelMultiplier_injective`, `finrank_localIntermediateSpace`, `finrank_kernelSliceSourceSpace`, `exhibitedKernelFamilyKernelMap_injective`, `finrank_intermediateConstraintMap_le_certifiedEnlargedRankBound`, `finrank_exactLocalConstraintAt_le_certifiedEnlargedRankBound` in `...HiddenDerivative.Interpolation.Local.{ConstraintKernel,IntermediateSpace,KernelSliceIndependence,CertifiedRankBound}`; acceptance cases in the matching `ArkLibTest` files | The certified bound is an instance of P3 slice 1's `finrank_range_exactLocalConstraintAt_le_sub`, with the exhibited-kernel family as the injection. The triangular-injectivity step is a generic linear-algebra lemma over rings and finite linear orders; weight-at-most supports take values in any ordered monoid. The spaces are defined for every `d`; `0 < d` is needed only for the dimension formulas and kernel membership, and a test shows the formula fails at `d = 0`. The contact-threshold lemmas drop `r < m`; kernel-multiplier injectivity holds over any domain. Deferred: the public coordinate API of `IntermediateSpace.lean`, shell/staircase counts, `exactInterpolationDimensionCount`, `ExactFiniteCertificate`, and the free-order dimension count and rate rounding. |
| [P3 slice 3: local backward-error identity, normalized substitution and point translation (#942)](https://github.com/Verified-zkEVM/ArkLib/pull/942) | `shiftedJetSubstitution` and its generator lemmas, `taylorAlgHom_comp_differentialSpecializationHom`, `taylor_differentialSpecialization`, `localPolynomialEvaluation`, `localPolynomialEvaluation_localCorrection`, `localPolynomialEvaluation_comp_unscaled_of_reconstruction`, `localPolynomialEvaluation_comp_normalized_of_reconstruction`, `localPolynomialEvaluation_comp_unscaled_backwardError`, `reducedHiddenTaylorError`, `X_pow_mul_reducedHiddenTaylorError`, `X_pow_succ_mul_reducedHiddenTaylorError`, `localPolynomialEvaluation_comp_normalized_reducedError` in `HiddenDerivative/Interpolation/Local/Identity.lean`; `normalizeError`, `normalizedLocalSubstitution`, `normalizedLocalSubstitution_eq_normalize_comp_unscaled` in `HiddenDerivative/Substitution.lean`; `globalPointTranslation`, `globalPointTranslation_neg_comp`, `globalPointTranslation_support_weight_le`, `unscaledLocalSubstitution_zero_comp_globalPointTranslation`, `localConstraintAt_eq_zero_comp_globalPointTranslation` in `HiddenDerivative/Interpolation/Local/Translation.lean` | `PolynomialDifferential.shiftedJetSubstitution`, `taylorAlgHom_comp_differentialSpecializationHom`, `taylor_differentialSpecialization` in `ArkLib.Data.Polynomial.Differential.ShiftedJet`; `normalizeErrorImage`, `normalizeError`, `normalizedLocalImage`, `normalizedLocalSubstitution`, `normalizedLocalSubstitution_eq_normalize_comp_unscaled` in `ArkLib.Data.CodingTheory.HiddenDerivative.NormalizedSubstitution`; `aeval_localCorrection`, `aeval_comp_unscaledLocalSubstitution_eq_aeval_iff`, `aeval_comp_normalizedLocalSubstitution_eq_aeval_iff`, `localPolynomialEvaluation`, `localPolynomialEvaluation_comp_{unscaled,normalized}_eq_iff`, `localPolynomialEvaluation_comp_unscaled_eq_iff_error_eq`, `localPolynomialEvaluation_{comp_,}unscaled_backwardError`, `reducedHiddenTaylorError` with its two `X`-power laws, `localPolynomialEvaluation_{comp_,}normalized_reducedError` in `...HiddenDerivative.Interpolation.Local.Identity`; `globalPointTranslation`, `globalPointTranslation_{zero,comp,neg_comp}`, `globalPointTranslation_mem_restrictWeightAtMost`, `{unscaled,normalized}LocalSubstitution_comp_globalPointTranslation`, `localConstraintAt_globalPointTranslation`, `localConstraintAt_eq_zero_globalPointTranslation` in `...HiddenDerivative.Interpolation.Local.Translation`; acceptance cases in the matching `ArkLibTest` files | The shifted-jet substitution mentions no Reed–Solomon object, so it moves to the differential-polynomial owner over a commutative semiring. The source's reconstruction-hypothesis lemmas become the reverse directions of exact iff criteria, and a new uniqueness theorem says the backward Taylor error is the only error satisfying the identity. The substitution criteria hold for evaluation in any `R`-algebra. Translation composes additively and moves the constraint map between centers at arbitrary offsets, not only from the origin; its weight lemma is the signed-weight `restrictWeightAtMost` closure, with nonnegative weights on `X` and `Y₀` shown necessary. The source's `X_pow_dvd_hiddenTaylorError` duplicates main's `Polynomial.X_pow_dvd_normalizedBackwardTaylorError` and is derived in a test. Deferred: all of `Local/Coordinates.lean` (it needs the parent's private coordinate equivalence, `localDerivativeJetWeight`, `fullDerivativeJetWeight`, and the budget indices), `Interpolation/Space.lean`, `Matrix.rank_map_algebraMap_le`, and the normalized substitution's membership lemmas, which go with `Symbolic/LocalRank`. |
| [P3 slice 4: local exponent coordinates and the rectangular interpolation space (#947)](https://github.com/Verified-zkEVM/ArkLib/pull/947) | `HiddenDerivative/Interpolation/Space.lean` (higher-jet exponents, good exponents, `GlobalEligibleExponent`, `interpolationSpace` and its basis); from `Interpolation/Local/Coordinates.lean`: `localCoordinateBudget`, `localResidualCoordinateBudget`, `unscaled_support_weight_le`, `unscaledLocal_error_le_t`, `unscaledLocal_higher_weight_le`, `unscaledLocal_derivativeJetWeight_le`, `unscaledLocal_jet_degree_le`, `unscaled_jet_degree_lt_of_support`, `localConstraint_support_of_derivative_weight`, `localConstraint_support_of_weight_bounds`, `reachableLocalJetDegree_eq_coordinates`, `localContact_eq_coordinates`, `localResidualExponents`, `card_localResidualExponents_le`, `mem_localResidualExponents_of_bounds`; the public coordinate equivalence of `Local/IntermediateSpace.lean` | `Finsupp.weight_le_weight` in `ArkLib.ToMathlib.Finsupp.Weight`; `localDerivativeJetWeight`, `localJetDegreeWeight`, `localContactOrder_eq`, `localExponentCoordinatesEquiv`, `weight_localFirstJetWeight`, `weight_localHigherJetWeight`, `weight_localJetDegreeWeight` in `ArkLib.Data.CodingTheory.HiddenDerivative.Variables`; `localResidualCoordinateBudget`, `localCoordinateBudget`, `localResidualCoordinateBudget_le_localCoordinateBudget` in `...Interpolation.Counting`; `HigherJetExponent`, `goodHigherExponents`, `card_goodHigherExponents_of_le`, `fullDerivativeJetWeight`, `fullHigherJetDegree`, `globalEligibleExponents`, `interpolationSpace`, `interpolationSpaceBasis`, `finrank_interpolationSpace_eq_card`, `interpolationSpace_le_exactInterpolationSpace`, `finrank_interpolationSpace_le_exactInterpolationSpace` in `...Interpolation.Space`; `unscaledLocal{Image,Substitution}_mem_restrictWeightAtMost`, `localE_le_localT_of_mem_support`, `local{HigherJetWeight,DerivativeJetWeight,JetDegree}_le_of_mem_support`, `localJetDegree_lt_of_mem_support`, `localConstraintAt_support_of_{derivative_weight,weight_bounds}`, `localResidualExponents`, `card_localResidualExponents_le`, `mem_localResidualExponents_of_bounds`, `finrank_range_localConstraintAt_domRestrict_le`, `finrank_range_exactLocalConstraintAt_le_localResidualCoordinateBudget` in `...Interpolation.Local.Coordinates`; acceptance cases in the matching `ArkLibTest` files | The source's fixed-shape integer weight lemma becomes a transport lemma for any weights in an ordered additive monoid, with nonnegativity on `X` and `Y₀` shown necessary. The real jet-degree cutoff `T` becomes a natural cutoff `B`; the tests recover the source statements with `B = ⌈T⌉₊`. The source's dependent index types `LocalResidualCoordinateIndex`, `localResidualExponent` and `LocalCoordinateBudgetIndex` are replaced by a `Finset.sigma` image, so the budget is an upper bound on a card rather than an index-type cardinality. The comparison with the exact space (`D < K`) and the rank bound on the exact space are new; tests show `D < K` and `W ≤ C` (for the good-exponent count) are needed. `LocalIntermediateIndex`, `localIntermediateExponents`, `localIntermediateSpaceBasis` and the kernel-slice index API are not ported: nothing in the source uses them. Deferred: `Matrix.rank_map_algebraMap_le` and the normalized substitution's membership lemmas (`Symbolic/LocalRank`), the shell/staircase counts, `exactInterpolationDimensionCount`, `ExactFiniteCertificate`, `finrank_interpolationSpace_lowerBound` and the free-order count. |
| [P3 slice 5: exact interpolation dimension count and rectangular lower bound (#951)](https://github.com/Verified-zkEVM/ArkLib/pull/951) | `staircaseCount`, `StaircaseIndex`, `card_staircaseIndex`, `staircaseIndexEquiv`, `card_staircasePairs`, `higherJetTupleSpecializationCost`, `exactDimensionResidual`, `exactInterpolationDimensionCount`, `ExactDimensionIndex`, `card_exactDimensionIndex` in `Interpolation/Counting.lean`; all of `Interpolation/DimensionBridge.lean` (`jetIndexSplitEquiv`, `exactExponentCoordinatesEquiv` and its `_x`/`_y₀`/`_y₁`/`_higher` lemmas, `sum_jet_eq_y₀_add_y₁_add_higher`, `firstJetExponent_eq_coordinate`, `fullHigherJetWeight_eq_coordinate`, `exactInterpolationMonomialWeight_eq_coordinates`, `ExactDimensionCoordinatesEligible`, `exactInterpolationEligibleExponent_iff_coordinates`, the index equivalences, `card_exactInterpolationExponents_eq_exactInterpolationDimensionCount`, `finrank_exactInterpolationSpace_eq_exactInterpolationDimensionCount`, `totalJetDegree_eq_coordinates`, `fullHigherJetDegree_eq_coordinates`); all of `Interpolation/Global/Dimension.lean` (rectangle index embeddings, `globalRectangleExponent` and its lemmas, `globalRectangleEmbedding_injective`, `card_globalRectangleIndex`, `card_globalEligibleExponents_lowerBound`, `finrank_interpolationSpace_lowerBound`) | `Nat.staircaseCount`, `staircaseCount_zero_right`, `Finset.staircase`, `mem_staircase`, `mem_staircase_of_pos`, `card_staircase`, `Nat.card_staircasePairs` in `ArkLib.Data.Finset.Staircase`; `higherJetTupleSpecializationCost`, `exactDimensionResidual`, `exactInterpolationDimensionCount`, `exactDimensionCoordinates`, `card_exactDimensionCoordinates` in `HiddenDerivative/Interpolation/Counting.lean`; `jetExponentCoordinatesEquiv`, `jetExponentCoordinatesEquiv_apply`, `weight_eq_coordinates`, `firstJetExponent_eq_coordinates`, `fullHigherJetWeight_eq_coordinates`, `fullHigherJetDegree_eq_coordinates`, `totalJetDegree_eq_coordinates`, `weight_differentialWeight_eq_coordinates`, `exactInterpolationEligibleExponent_iff_coordinates`, `globalEligibleExponent_iff_coordinates`, `card_exactInterpolationExponents`, `finrank_exactInterpolationSpace_eq_exactInterpolationDimensionCount`, `card_mul_le_card_globalEligibleExponents`, `le_finrank_interpolationSpace`, `finrank_interpolationSpace_lowerBound` in `HiddenDerivative/Interpolation/Dimension.lean`; classic acceptance files | The two source modules share one coordinate equivalence and become one module. Index types and equivalence chains become `Finset`s, `Finset.sigma` and one `Finset.card_equiv`; the staircase count is general combinatorics in `Data/Finset`, with the card identity for every `D`. The rectangle lower bound takes independent sides `N`, `H₀`, `H₁`; the source statement is the case `N = (K - 1) H`, `H₀ = H₁ = H`. Tests show `0 < D` and `0 < d` are needed. Not ported: the `Finsupp` form `higherJetSpecializationCost` (no consumer yet); `finrank_interpolationSpace_eq_card` is in P3 slice 4. Deferred: shell counts, `CertifiedEnlargedRankBudgetIndex`, `ExactFiniteCertificate`, `FreeOrderDimension`. |
| [P6 slice 2: radical factor splitting, ordinary factor assembly, Frobenius–Taylor and sparse contraction (#972)](https://github.com/Verified-zkEVM/ArkLib/pull/972) | `ToMathlib/MvPolynomial/OrdinaryFactors.lean` (`ordinaryFactorRepresentative`, `ordinarySquarefreeProduct`, `ordinaryRootFactorClasses`, `ordinaryContent`, `ordinaryRootProduct`, `ordinary_split_product`, `ordinary_split_zero_iff`, `ordinary_degree_sum_le`, `ordinary_root_degree_sum_le` and companions); `MutualCorrelatedAgreement/Ordinary/Factors/FactorAssembly.lean` (`exists_exceptional_ordinaryFactorAssembly`); `ToMathlib/Polynomial/FrobeniusTaylor.lean` (`taylor_expand_primePow`, `coeff_taylor_expand_primePow_mul`, `coeff_taylor_expand_primePow_eq_zero`); `ToMathlib/Polynomial/SparseContraction.lean` | `Associates.{rep, mk_rep, rep_injective, mk_prod_rep}`, `UniqueFactorizationMonoid.{radicalRep, mk_radicalRep, radicalRep_dvd_self, radicalRep_ne_zero, radicalRep_eq_of_associated, irreducible_rep_of_mem_primeFactors, primeFactors_mk_pow_of_prime, map_eq_zero_iff_exists_primeFactors, map_radicalRep_eq_zero_iff, map_prod_rep_eq_sum, map_radicalRep_eq_sum, map_le_of_dvd, sum_primeFactors_le}` in `ArkLib.ToMathlib.RingTheory.Radical.Representative`; `MvPolynomial.{positiveDegreeFactorClasses, radicalContent, radicalPrimPart, radicalContent_mul_radicalPrimPart, map_eq_zero_iff_radicalContent_or_exists, exists_exceptional_of_factor_exceptional, degreeOf_radicalContent, add_sum_positiveDegreeFactorClasses_le, sum_degreeOf_positiveDegreeFactorClasses_le}` and the degree-sum corollaries in `ArkLib.ToMathlib.MvPolynomial.RadicalSplit`; `exists_exceptional_ordinaryFactorAssembly`, `exists_exceptional_ordinaryUnifiedPowerFactorAssembly` in `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.Ordinary.FactorAssembly`; `Polynomial.{taylor_expand_expChar_pow, coeff_taylor_expand_expChar_pow_mul, coeff_taylor_expand_expChar_pow_eq_zero}` in `ArkLib.ToMathlib.Polynomial.FrobeniusTaylor`; `Polynomial.{expand_contract_eq_self_iff, expand_contract_of_sparse, degree_contract_lt_of_degree_lt, existsUnique_expand_of_sparse, existsUnique_expand_of_sparse_taylor}` in `ArkLib.ToMathlib.Polynomial.SparseContraction`; acceptance cases in the matching `ArkLibTest` files | `OrdinaryFactors` is split into a monoid-level representative file and a multivariate content/primitive-part file and renamed, since nothing in it is RS-specific. Factor classes are `primeFactors` in `Associates` (pinned Mathlib has no normalization monoid on `MvPolynomial`). Field → UFD coefficients, `Option σ`/`none` → any variable, `RingHom` → `MonoidWithZeroHomClass`. The three source assemblies share the generic `exists_exceptional_of_factor_exceptional`; `Q ≠ 0` tested necessary. FrobeniusTaylor and SparseContraction hold over commutative semirings, and `0 < s` is dropped where unused. Deferred: `GraphLine.lean`, `Ordinary/Frobenius/PointRecognition.lean`, and the consumers `SharpEquation`, `UnifiedCurve`, `PolynomialCurve/Assembly`. |
| [P6 slice 1: ordinary MCA degree budgets (#968)](https://github.com/Verified-zkEVM/ArkLib/pull/968) | `ordinaryFrobeniusMixedDegree{,_eq,_le,_le_sharp,_le_unified}`, `ordinaryFrobenius_sharp_difference`, `ordinaryFrobenius_sharp_factor`, `ordinaryFrobenius_unified_factor`, `ordinaryPsi{,_eq_sharp,_mono,_le_four_mul}`, `ordinaryFactorRaw`, `ordinaryFrobenius_charge_le`, `ordinaryFactorRaw_sum_le`, `ordinaryUnifiedPowerFactor{RawAt,At,AtOrHeight,Raw}`, `ordinaryUnifiedPowerFactorAtOrHeight_{zero,of_pos}`, `ordinaryUnifiedPowerFactorRawAt_succ_eq`, `ordinaryUnifiedPowerFactorAt_succ_eq`, `ordinaryUnifiedPowerFactorRawAt_sum_le`, `ordinaryUnifiedPowerFactorRaw_sum_le` in `MutualCorrelatedAgreement/Ordinary/Factors/{FactorBounds,AggregationBounds,UnifiedBudget}.lean` | `Finset.add_sum_le_mul_add_mul_of_le` in `ArkLib.ToMathlib.BigOperators.LinearBudget`; all source declarations plus `ordinaryFactorRaw_{le,eq}_linear`, `ordinaryUnifiedPowerFactorRawAt_{le,eq}_linear`, `ordinaryUnifiedPowerFactorRaw_eq_rawAt` in `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.Ordinary.FactorBudget`; acceptance cases in the matching `ArkLibTest` files | The three source files are merged into one module because source-shaped names exceed the module-name length limit. The shared summation step is a generic ordered-semiring lemma, and the three summation theorems specialize it through new linear bounds. `1 ≤ b`, `1 ≤ D` and `1 ≤ s` are dropped wherever truncated subtraction makes them unnecessary; the sharp bounds keep only `b ≤ D`, tested necessary at `D = h = 1`, `s = b = 2`. `ordinaryFrobenius_sharp_difference` holds in any commutative ring. Deferred: `Factors/FactorAssembly` and `ToMathlib/MvPolynomial/OrdinaryFactors`, the Frobenius and polynomial-curve incidence bounds, and the unified curve theorem. |
| [P9 slice 10: harmonic bounds and the explicit weighted dimension bound (#970)](https://github.com/Verified-zkEVM/ArkLib/pull/970) | `Real.harmonic_pred_le_log_add_three_fifths`, `Real.harmonic_le_log_add_three_fifths`, `Real.log_add_three_fifths_le_sqrt_div_ten`, `Real.reciprocal_square_sum_lt`, `Real.reciprocal_square_sum_gt`, `Real.reciprocal_cube_sum_lt` in `ToMathlib/NumberTheory/Harmonic/Bounds.lean`; `weighted_dimension_lower` in `HiddenDerivative/Interpolation/WeightedSupport/Estimate.lean`; `positive_cube_convex`, `positive_cube_tangent`, `positive_cube_jensen` in `.../WeightedSupport/Cubic.lean` | `Real.{eulerMascheroniConstant_lt_three_fifths, harmonic_lt_log_succ_add_three_fifths, harmonic_pred_lt_log_add_three_fifths, harmonic_lt_log_add_three_fifths, log_add_three_fifths_le_sqrt_div_ten, harmonic_sq_le_succ_div_hundred, sum_range_one_div_succ_sq_lt_pi_sq_div_six, reciprocal_square_sum_lt, reciprocal_square_sum_gt, reciprocal_cube_sum_lt}` in `ArkLib.ToMathlib.NumberTheory.Harmonic.Bounds`; `convexOn_max_sub_zero_pow`, `pow_three_sub_mul_le_max_sub_zero_pow_three`, `MeasureTheory.pow_three_le_integral_max_sub_zero_pow_three` in `ArkLib.ToMathlib.MeasureTheory.Integral.PositivePart`; `weighted_dimension_lower` in `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.WeightedSupport.Estimate`; acceptance cases in the matching `ArkLibTest` files | The harmonic bounds go through `γ < 3/5` and Mathlib's Euler–Mascheroni sequences; `harmonic_pred_…` becomes strict and drops `33 ≤ d`, `harmonic_…` becomes strict with the same threshold `32` (not sharp: first holds at `22`). `harmonic_sq_le_succ_div_hundred` is new and generalizes `WeightedSupportParameters.harmonic_square_bound` for `H = harmonic (d - 1)`. The square-sum bound comes from `hasSum_zeta_two`; `8 ≤ n` in `reciprocal_square_sum_gt` is tested sharp. Summands are `1 / (i + 1) ^ k`; source forms derived in tests. `weighted_dimension_lower` weakens `48000 ≤ d` to `10000 ≤ d`, drops `0 < W` and `0 < g * m`, and uses Mathlib's `harmonic`. The positive-cube convexity holds for every exponent over any ordered field; the Jensen bound drops `0 ≤ b`; `0 ≤ b` and the mean-zero hypothesis are tested necessary where kept. Deferred: `WeightedSupport/Margin.lean`, `WeightedSupport/RankBound.lean` (need the local-rank layer), `Real.reciprocal_fourth_sum_lt` (no consumer). |
| [P7a slice 1: finite-field root counting via regular jets and witnesses (#969)](https://github.com/Verified-zkEVM/ArkLib/pull/969) | `witness_counting_le_of_bad`, `witness_counting_pow_le_of_bad`, `lower_mul_card_le_card_mul_of_fiber_bounds` in `HiddenDerivative/RootFinding/FiniteField/Counting.lean`; `univariateSpecialization`, `jetAssignment`, `natCard_regularJetAt_le`, `natCard_regularJet_le`, `natCard_regularJet_le_degree_mul_pow` in `.../FiniteField/RegularJetCounting.lean`; `boundedSolution_counting_pow_le_of_bad`, `boundedSolution_counting_pow_le` in `.../FiniteField/WitnessCounting.lean`; `regularBranch_counting_pow_le`, `polynomialJet_injOn_regularWitness` in `.../FiniteField/RegularCounting.lean` | `MvPolynomial.{univariateSpecialization, eval_univariateSpecialization, univariateSpecialization_update, natDegree_univariateSpecialization_le, derivative_optionEquivLeft, derivative_univariateSpecialization, univariateSpecialization_ne_zero_of_eval_pderiv_ne_zero, card_le_degreeOf_mul_prod_of_univariateSpecialization_ne_zero, card_le_degreeOf_mul_prod_of_eval_pderiv_ne_zero}` in `ArkLib.ToMathlib.MvPolynomial.UnivariateSpecialization`; `Finset.card_mul_sub_le_card_mul_of_card_bad_le` in `ArkLib.ToMathlib.Combinatorics.Enumerative.DoubleCounting`; `jetAssignment{,_none,_some,_injective}`, `jetEvaluation_eq_eval`, `decidableIsRegularJet`, `card_filter_isRegularJet_le`, `card_filter_isRegularJet_product_le`, `natCard_regularJetAt_le`, `natCard_regularJet_le` in `ArkLib.Data.Polynomial.Differential.RegularJetCount`; `card_mul_sub_le_of_card_bad_le`, `card_mul_sub_le_of_natDegree_separant_le`, `natCast_choose_ne_zero_of_ringChar`, `card_mul_sub_le_of_isHighestActiveJet`, `BoundedSolution.card_mul_sub_le_of_isHighestActiveJet` in `ArkLib.Data.Polynomial.Differential.WitnessCount`; acceptance cases in the matching `ArkLibTest` files | Fields become domains; `IsDomain` is tested necessary. The specialization takes a full assignment and ignores the selected coordinate, replacing `OtherVariable`/`insertVariableValue`. The source's sigma-type encodings and `IsRegularWitness`/`regularJetAt`/`separantBadPoints`/`RegularJetAt` are not ported; the counts go through the grid zero count and a generic double-counting lemma. `D < ringChar F` is replaced by a binomial-cast hypothesis `hbinom` (also covers characteristic zero), tested necessary over `ZMod 2`; `natCast_choose_ne_zero_of_ringChar` recovers it. Roots are a `Finset F[X]` with a solution hypothesis; `Δ` is replaced by `jetDegree`; the weighted-degree hypothesis is weakened. `lower_mul_card_le_card_mul_of_fiber_bounds` is Mathlib's `Finset.card_mul_le_card_mul`. Source-shaped statements are derived in the tests. Deferred: extension-field and total-jet-degree counts, and the recursive and singular-recursion consumers. |
| [P5 slice 14: principal-open parametrizations and hypersurface cut families (#971)](https://github.com/Verified-zkEVM/ArkLib/pull/971) | `ToMathlib/AlgebraicGeometry/Incidence/GraphPullback.lean` (`eval_polynomialGraphPullback`, `eval_affineGraphPullback`, `polynomialGraphPullback_eq_zero_of_infinite`, `affineGraphPullback_eq_zero_of_infinite`, `hilbertPolynomial_natDegree_le_one_of_principalOpen_subset_{polynomialGraph,affineGraph}`, `polynomialGraphPullback_vanishes_of_principalOpen`, `graphPullback_vanishes_of_principalOpen`); `ToMathlib/AlgebraicGeometry/CutFamily/Hypersurface.lean` (`hypersurfaceCutFamily_dimension_le`, `hypersurfacePrimeFamily_dimension`, `hypersurfacePrimeFamily_potential_le`, `hypersurfaceCutFamily_potential_le`, `hypersurfaceCutFamily_incidence_off_excluded`, `hypersurfacePrimeFamily_prime_open`, `hypersurfaceCutFamily_spec`, `hypersurfaceCutFamily_covers`) | `MvPolynomial.{polynomial_eval_aeval, aeval_eq_zero_of_infinite, mem_radical_of_forall_principalOpen, ker_aeval_le_radical_of_principalOpen_subset_range, natDegree_affineHilbertPolynomial_le_of_principalOpen_subset_range, natDegree_affineHilbertPolynomial_le_one_of_principalOpen_subset_range, aeval_eq_zero_of_principalOpen_subset_range}` in `ArkLib.ToMathlib.RingTheory.Nullstellensatz.PrincipalOpenParametrization`; `natDegree_affineHilbertPolynomial_le_of_mem` in `...MvPolynomial.AffineHilbertPolynomial`; `natDegree_affineHilbertPolynomial_add_one_of_mem_minimalPrimes_span_singleton` in `...MvPolynomial.AffineHilbertPurity`; `sum_affineDegree_mul_pow_{retainedMinimalPrimes,iteratedRetainedCutFamily}_span_singleton_le` in `...MvPolynomial.AffineHilbertCutFamily`; `card_le_sum_of_agreement_off_excluded`, `card_le_of_agreement_off_excluded_of_hypersurface` in `...Nullstellensatz.AgreementIncidence`; acceptance cases in the matching `ArkLibTest` files | Graph pullbacks are `aeval w` for any `w`; no graph or hypersurface-family definitions are introduced. The dimension bound holds for any parametrization `w : σ → k[τ]` of the principal open (bound `Nat.card τ`), drops the positive-dimension hypothesis, weakens primality with `s ∉ P` to left-regularity of `s` modulo `I` (tested necessary), and allows points in an algebraically closed extension. Hypersurface incidence replaces `0 < L`, `A ≤ n` by `A - L + 1 ≤ Fintype.card ι` (tested necessary) and drops `0 < B`. `hypersurfacePrimeFamily_prime_open`, `hypersurfaceCutFamily_spec`, `hypersurfaceCutFamily_covers` are derived in tests. Deferred: `Incidence/DimensionSensitive.lean` and the remaining `Incidence/` files. |
| [P3 slice 6: free-order parameters and the interpolation dimension comparison (#955)](https://github.com/Verified-zkEVM/ArkLib/pull/955) | `multiplicity`, `agreementThreshold`, `ambientDimension`, `interpolationDegreeBudget`, `interpolationWeightBudget`, `higherJetDegreeBudget`, `interpolationBoxWidth`, `coarseListBound` in `ReedSolomon/HiddenDerivative/Parameters/Basic.lean`; the rounding, slack, rank-saving and rounded-rate lemmas of `Parameters/FreeOrder.lean` (`agreementThreshold_le_iff`, `ambientDimension_lt_blockLength`, `le_ambientDimension_iff`, `interpolationDegreeBudget_pos`, `boxFamily_weightedBudget_lt`, `freeGlobalDimensionSlacks`, `exists_orderThreshold_for_boxWidth`, `rankSavingExponent`, `exists_freeOrderRankThreshold`, `exists_freeOrderElementaryThreshold`, `half_rate_le_ambientDimension_sub_one_div`, `freeOrder_rank_comparison`, and the rest); `certifiedEnlargedRankBound_le_four_mul_d_pow_eight`, `shellExponent`, `shellExponent_add_rankSavingExponent`, `rankShellBound_lt_interpolationBox`, `n_mul_certifiedEnlargedRankBound_lt_finrank_exactInterpolationSpace`, `localRankBound_lt_interpolationSpace_of_shell_bounds`, and the private `rectangleResidual_le`, `contactThreshold_cube_le_sq`, `certifiedContactRankBudget_cube_le` in `Interpolation/FreeOrderDimension.lean` | The same names in `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.{Basic,FreeOrder}` and `...Interpolation.FreeOrderDimension`, plus `interpolationDegreeBudget_le_iff`, `one_le_of_ambientDimension_pos`, `lt_one_of_ambientDimension_pos`, `tendsto_interpolationBoxWidthTarget`, `tendsto_const_mul_rpow_rankSavingExponent`, `eventually_freeOrderRankComparison`, `eventually_freeOrderElementary`; `Finset.natWeightedSimplex_mono` in `ArkLib.Data.Finset.WeightedSimplex`; `weightedHigherJetCount_mono`, `contactThreshold_le_of_le_mul`, `exhibitedKernelResidualCount_le`, `certifiedEnlargedRankBound_le_of_le_mul` in `...Interpolation.Counting`; `card_goodHigherExponents_mul_le_finrank_exactInterpolationSpace` in `...Interpolation.Dimension`; acceptance cases in the matching `ArkLibTest` files | Definitions keep the source formulas and constants. Order thresholds are `Filter.Eventually` statements with the source's existential forms as corollaries; `exists_orderThreshold_for_boxWidth` takes any target instead of the explicit `⌈32/θ⌉`. Range hypotheses on `ε`, `θ`, `n` and `d` are dropped where the conclusion does not need them. The three private cube lemmas become general `k`-multiple lemmas in `Counting.lean`. The dimension comparison uses the exact interpolation space, so it drops the jet-degree budget `B` and `C + 2H ≤ B`, and `0 < θ`, `0 < d`, `0 < n` follow from the rank comparison. `_of_mul_denominator_lt` becomes the non-strict `_of_mul_denominator_le`. Deferred: the shell estimate and the choice of `W`, which the source also leaves as hypotheses. |
| [P3 slice 7: local graded rank, remainder map and the local rank budget (#958)](https://github.com/Verified-zkEVM/ArkLib/pull/958) | `sourceMonomial` and its weighted-homogeneity lemmas in `HiddenDerivative/Interpolation/SourceMonomial.lean`; `localCorrection_isWeightedHomogeneous`, `unscaledLocalImage_isWeightedHomogeneous`, `unscaledLocalSubstitution_isWeightedHomogeneous`, `localConstraintAt_isWeightedHomogeneous`, `sourceJetGrade`, `localJetGrade`, `gradedLocalConstraintAt`, `coeff_localConstraintAt_eq_zero_of_weight_ne`, `coeff_localConstraintAt_zero_sourceMonomial_eq_zero` in `.../Interpolation/Local/GradedRank.lean`; `normalizeLocalExponent` and the remainder-map lemmas in `.../Local/RemainderMap.lean`; `totalJetDegree_le_of_mem_globalPointTranslation_support`, `localRank_ceilDiv_le`, `weightedHigherJetCount_le_exp`, `sum_contactThreshold_mul_exp_le`, `localCoordinateBudget_le_geometric`, `localCoordinateBudget_le_kappa`, `localCoordinateBudget_div_volume_mul_cube_le` in `.../Local/RankBudget.lean` | `MvPolynomial.mapExponents{,_monomial,_eq_bind₁,_injective}`, `filterSupport_monomial`, `filterSupport_mapExponents`, `weightedTruncation_mapExponents` in `ArkLib.Data.MvPolynomial.MapExponents`; `IsWeightedHomogeneous.{bind₁,filterSupport,weightedTruncation}`, `bind₁_mem_weightedHomogeneousSubmodule` in `ArkLib.Data.MvPolynomial.WeightedHomogeneous`; `sum_range_natCast_succ_mul_pow_succ_mul_one_sub_sq`, `sum_range_pow_succ_le_div_one_sub`, `sum_range_natCast_succ_mul_pow_succ_le`, `sum_range_linear_mul_pow_succ_le`, `Nat.cast_ceilDiv_le_div_add_one`, `exp_neg_div_one_sub_exp_neg{,_sq}_le`, `sum_range_linear_mul_exp_neg_pow_succ_le`, `add_pow_le_pow_mul_exp` in `ArkLib.ToMathlib.Analysis.SpecificLimits.GeometricBounds`; `LinearMap.rangeCoordinates{,_eq_zero_iff,_surjective}`, `LinearMap.ker_rangeCoordinates` in `ArkLib.ToMathlib.LinearAlgebra.FiniteDimensional`; the RS declarations in `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.{SourceMonomial,Local.GradedRank,Local.RemainderMap,Local.RankBudget}`; acceptance cases in the matching `ArkLibTest` files | Weighted homogeneity is preserved by `bind₁` with homogeneous images and by any support filter, stated for any weight monoid; the local grading is a corollary. The grading lemmas take an arbitrary center (the source fixes `0`); the received value must be `0`, and a test shows received value `1` breaks the grading. Exponent relabelling is `AddMonoidAlgebra.mapDomainAlgHom` along an additive map of exponents. The geometric and exponential tail bounds are generic real analysis; the two exponential ratio bounds drop `0 < x`. The budget bound uses `Finset.natWeightedSimplex_succ_sandwich` instead of the source's ScaledLattice count. `0 < x`, `0 < W`, `0 ≤ W + y`, `0 < d` and `2 ≤ d` are each tested necessary. Source-shaped statements `localConstraintAt_zero_sourceMonomial_isWeightedHomogeneous`, `localRank_linear_geometric_sum_le`, `localRank_linear_exp_sum_le` and `localRank_contact_exp_sum_le` are derived in the tests. Deferred: `X_sub_C_pow_dvd_differentialSpecialization_of_normalizedLocalConstraint` (needs `Interpolation/Local/Contact.lean`), `WeightedSupport/LocalRank.lean`, `PartitionSupport/LocalRank.lean`. |
| [P9 slice 15: finite Johnson parameter bounds (#987)](https://github.com/Verified-zkEVM/ArkLib/pull/987) | `johnsonRhoMinus`, `johnsonAgreement`, `johnsonM`, `johnsonT`, `johnsonMu`, `johnsonH`, `johnsonTheta`, `johnsonExceptionCount`, `johnsonRefinedExceptionCount`, `johnsonComparison{Estimate,Multiplicity,Shift}`, `johnsonGamma`, `johnson{Normalized,Pre}Envelope` and all 29 source bounds (including `johnsonExceptionCount_lt_closed`, `johnsonExceptionCount_lt_comparisonEstimate`, `johnson_half_gap`) in `HiddenDerivative/Parameters/Johnson/FiniteBounds.lean` | `Nat.cast_ceil_sub_one_lt`, `Nat.one_le_ceil_sub_one` in `ArkLib.ToMathlib.Algebra.Order.Floor.Semiring`; acceptance cases in `ArkLibTest/Data/CodingTheory/HiddenDerivative/Parameters/Johnson.lean` | Definitions unchanged. `D ≤ n - 2` weakened to `D < n` in 14 lemmas that do not use `n - D ≥ 2` (kept in `johnson_inv_length_le_min` and the `E₀` comparisons); unused `A ≤ n` and `johnsonAgreement ≤ 1` dropped. Nothing deferred from the file. Renamed from the source's `johnsonE0`, `johnsonESharp`, `johnsonBCHKS{,_M,_T}` (and the lemma names built on them). |
| [P9 slice 16: fixed-rate gate and first-order rate bound (#988)](https://github.com/Verified-zkEVM/ArkLib/pull/988) | `rateGamma`, `fixedRateCoefficient`, `rateGamma_pos`, `log_rateGamma`, `complementary_rate_logs`, `fixed_rate_log_identity`, `fixedRateCoefficient_pos`, `exists_small_gap_rate_gate` in `HiddenDerivative/Parameters/RatePartition/Gate.lean`; `firstOrderRateThreshold`, `firstOrderRateBeta`, `firstOrderSourceDensity`, `firstOrderRankDensity`, `firstOrderRankCubicEnvelope`, `firstOrderCleanExpression` and their bounds, including `firstOrderRate_surplus_pos`, in `.../Parameters/FirstOrder/RateBound.lean` | acceptance cases in the matching `ArkLibTest` files | Definitions unchanged; hypotheses weakened (`rate ≠ 0` for the log identities, `rate < 40/9` for `fixedRateCoefficient_pos`, `R < 2` for the β and threshold lemmas). `RateBound` imports only Mathlib (the source's `Parameters/Basic` import was unused). Deferred: the constructions using the gate and the threshold. |
| [P9 slice 17: weighted Johnson certificate parameters and low-rate stationary root (#989)](https://github.com/Verified-zkEVM/ArkLib/pull/989) | `johnsonWeighted{U,N,W,R,T,Slope,Moment,HeightInt,Height,SourceSlots,RowSlots,RefinedExceptionCount,RefinedExceptionCountFloor}`, `johnsonPairwiseListFloor`, `IsJohnsonWeightedCertificate` and its lemmas in `HiddenDerivative/Parameters/Johnson/WeightedCertificate.lean`; `firstOrderStationaryCubic`, `firstOrderLowRateScale`, `firstOrderRateSwitch`, `FirstOrderLowRateRegime`, `firstOrderLowRateStationaryU`, `firstOrderLowRateBeta`, `firstOrderLowRateThreshold` and their lemmas in `.../Parameters/FirstOrder/LowRateStationary.lean` | acceptance cases in `ArkLibTest/Data/CodingTheory/HiddenDerivative/Parameters/{Johnson,FirstOrder}.lean`; the former `n = 2¹⁶` table-row computations were removed, leaving the small negative-moment certificate in the Johnson aggregate file | All statements identical to the source except `firstOrderLowRateRegime_iff_lt_rateSwitch` (drops `0 ≤ ρ`) and `rate_lt_firstOrderLowRateThreshold` (drops `ρ < 1`), with source forms tested. Both files import only Mathlib (the source's parameter imports were unused). Deferred: the weighted Johnson interpolation chain and the first-order interpolation construction. Renamed from the source's `johnsonWeightedSharpException{,Floor}`. |
| [P9 slice 18: Johnson interpolation slot bounds, branchwise first-order rate, pair-agreement list bound (#990)](https://github.com/Verified-zkEVM/ArkLib/pull/990) | `johnsonXCutoff`, `johnsonSourceSlotCount`, `johnsonRowSlotCount`, `johnsonMu_add_one`, `johnsonH_add_one`, `johnsonMu_le_H`, `johnsonM_le_H_add_one`, `johnson_D_mul_mu_lt_XCutoff`, `johnsonXCutoff_le_mul_agreement`, `johnson_interpolation_slot_surplus` in `HiddenDerivative/Parameters/Johnson/InterpolationBounds.lean`; `firstOrderBranchThreshold`, `firstOrderBranchBeta` and their lemmas, including `firstOrderBranch_surplus_pos`, in `.../Parameters/FirstOrder/BranchwiseRate.lean` | `Code.card_mul_sq_minAgreement_sub_pairAgreement_le`, `Code.card_le_one_of_pairwise_agree_le`, `Code.card_mul_minAgreement_le_of_pairwise_agree_eq_zero` in `ArkLib.Data.CodingTheory.ListDecodability.PairAgreementBound`; acceptance cases in `ArkLibTest/Data/CodingTheory/HiddenDerivative/Parameters/{Johnson,FirstOrder}.lean` | Definitions and conclusions unchanged. `D ≤ n - 2` weakened to `D < n` (matching `FiniteBounds`), unused guards dropped from `johnsonXCutoff_le_mul_agreement`; `firstOrderBranchBeta_pos` needs `ρ < 2`, and `firstOrderBranchBeta_lt_agreement_div_rate` drops `a < 1`. Nothing deferred. |
| [P6 slice 6: divisibility and primitive-part degree bounds for the radical split (#1010)](https://github.com/Verified-zkEVM/ArkLib/pull/1010) | `ToMathlib/MvPolynomial/OrdinaryFactorDegrees.lean`; `MutualCorrelatedAgreement/FirstOrder/Squarefree/Factorization.lean` | Seven theorems in `ArkLib.ToMathlib.MvPolynomial.RadicalSplit`; acceptance cases in `ArkLibTest/ToMathlib/MvPolynomial/RadicalSplit.lean` | The source's root-first coordinate swap (`rootFirstEquiv`, `rootFirst`) is not ported: the split applies at any variable, e.g. `some 1` in `DifferentialPolynomial F 1`. No bound needs `Q ≠ 0`. |
| [P9 slice 15: finite Johnson parameter bounds (#987)](https://github.com/Verified-zkEVM/ArkLib/pull/987) | `johnsonRhoMinus`, `johnsonAgreement`, `johnsonM`, `johnsonT`, `johnsonMu`, `johnsonH`, `johnsonTheta`, `johnsonExceptionCount`, `johnsonRefinedExceptionCount`, `johnsonComparison{Estimate,Multiplicity,Shift}`, `johnsonGamma`, `johnson{Normalized,Pre}Envelope` and all 29 source bounds (including `johnsonExceptionCount_lt_closed`, `johnsonExceptionCount_lt_comparisonEstimate`, `johnson_half_gap`) in `HiddenDerivative/Parameters/Johnson/FiniteBounds.lean` | `Nat.cast_ceil_sub_one_lt`, `Nat.one_le_ceil_sub_one` in `ArkLib.ToMathlib.Algebra.Order.Floor.Semiring`; acceptance cases in the matching `ArkLibTest` files | Definitions unchanged. `D ≤ n - 2` weakened to `D < n` in 14 lemmas that do not use `n - D ≥ 2` (kept, and tested necessary, in `johnson_inv_length_le_min` and the `E₀` comparisons); unused `A ≤ n` and `johnsonAgreement ≤ 1` dropped. Nothing deferred from the file. Renamed from the source's `johnsonE0`, `johnsonESharp`, `johnsonBCHKS{,_M,_T}` (and the lemma names built on them). |
| [P9 slice 16: fixed-rate gate and first-order rate bound (#988)](https://github.com/Verified-zkEVM/ArkLib/pull/988) | `rateGamma`, `fixedRateCoefficient`, `rateGamma_pos`, `log_rateGamma`, `complementary_rate_logs`, `fixed_rate_log_identity`, `fixedRateCoefficient_pos`, `exists_small_gap_rate_gate` in `HiddenDerivative/Parameters/RatePartition/Gate.lean`; `firstOrderRateThreshold`, `firstOrderRateBeta`, `firstOrderSourceDensity`, `firstOrderRankDensity`, `firstOrderRankCubicEnvelope`, `firstOrderCleanExpression` and their bounds, including `firstOrderRate_surplus_pos`, in `.../Parameters/FirstOrder/RateBound.lean` | acceptance cases in the matching `ArkLibTest` files | Definitions unchanged; hypotheses weakened (`rate ≠ 0` for the log identities, `rate < 40/9` for `fixedRateCoefficient_pos`, `R < 2` for the β and threshold lemmas), each with the source-shaped form tested. `RateBound` imports only Mathlib (the source's `Parameters/Basic` import was unused). Deferred: the constructions using the gate and the threshold. |
| [P9 slice 17: weighted Johnson certificate parameters and low-rate stationary root (#989)](https://github.com/Verified-zkEVM/ArkLib/pull/989) | `johnsonWeighted{U,N,W,R,T,Slope,Moment,HeightInt,Height,SourceSlots,RowSlots,RefinedExceptionCount,RefinedExceptionCountFloor}`, `johnsonPairwiseListFloor`, `IsJohnsonWeightedCertificate` and its lemmas in `HiddenDerivative/Parameters/Johnson/WeightedCertificate.lean`; `firstOrderStationaryCubic`, `firstOrderLowRateScale`, `firstOrderRateSwitch`, `FirstOrderLowRateRegime`, `firstOrderLowRateStationaryU`, `firstOrderLowRateBeta`, `firstOrderLowRateThreshold` and their lemmas in `.../Parameters/FirstOrder/LowRateStationary.lean` | acceptance cases in the matching `ArkLibTest` files; the three `n = 2¹⁶` table rows (`johnsonWeightedCertificate_rate_one_{sixteenth,fourth,half}` and their agreement ceilings) are in `ArkLibTest/.../Johnson/WeightedCertificate.lean` | All statements identical to the source except `firstOrderLowRateRegime_iff_lt_rateSwitch` (drops `0 ≤ ρ`) and `rate_lt_firstOrderLowRateThreshold` (drops `ρ < 1`), with source forms tested. Both files import only Mathlib (the source's parameter imports were unused). Deferred: the weighted Johnson interpolation chain and the first-order interpolation construction. Renamed from the source's `johnsonWeightedSharpException{,Floor}`. |
| [P9 slice 18: Johnson interpolation slot bounds, branchwise first-order rate, pair-agreement list bound (#990)](https://github.com/Verified-zkEVM/ArkLib/pull/990) | `johnsonXCutoff`, `johnsonSourceSlotCount`, `johnsonRowSlotCount`, `johnsonMu_add_one`, `johnsonH_add_one`, `johnsonMu_le_H`, `johnsonM_le_H_add_one`, `johnson_D_mul_mu_lt_XCutoff`, `johnsonXCutoff_le_mul_agreement`, `johnson_interpolation_slot_surplus` in `HiddenDerivative/Parameters/Johnson/InterpolationBounds.lean`; `firstOrderBranchThreshold`, `firstOrderBranchBeta` and their lemmas, including `firstOrderBranch_surplus_pos`, in `.../Parameters/FirstOrder/BranchwiseRate.lean` | `Code.card_mul_sq_minAgreement_sub_pairAgreement_le`, `Code.card_le_one_of_pairwise_agree_le`, `Code.card_mul_minAgreement_le_of_pairwise_agree_eq_zero` in `ArkLib.Data.CodingTheory.ListDecodability.PairAgreementBound`; acceptance cases in the matching `ArkLibTest` files | Definitions and conclusions unchanged. `D ≤ n - 2` weakened to `D < n` (matching `FiniteBounds`), unused guards dropped from `johnsonXCutoff_le_mul_agreement`; `firstOrderBranchBeta_pos` needs `ρ < 2`, and `firstOrderBranchBeta_lt_agreement_div_rate` drops `a < 1`. Nothing deferred. |
| [P6 slice 6: divisibility and primitive-part degree bounds for the radical split (#1010)](https://github.com/Verified-zkEVM/ArkLib/pull/1010) | `ToMathlib/MvPolynomial/OrdinaryFactorDegrees.lean`; `MutualCorrelatedAgreement/FirstOrder/Squarefree/Factorization.lean` | Seven theorems in `ArkLib.ToMathlib.MvPolynomial.RadicalSplit`; acceptance cases consolidated in `ArkLibTest/ToMathlib/MvPolynomial.lean` | The source's root-first coordinate swap (`rootFirstEquiv`, `rootFirst`) is not ported: the split applies at any variable, e.g. `some 1` in `DifferentialPolynomial F 1`. No bound needs `Q ≠ 0`. |
| [P6 slice 3: graph-line recognition, Frobenius point recognition and full dimension (#980)](https://github.com/Verified-zkEVM/ArkLib/pull/980) | `exists_graphLine_polynomials_of_sample`, `exists_exceptional_graphLine_challenges{,_le_disagreement,_of_sample}`, `exists_graphLine_polynomials_and_exceptional_challenges`, `exists_frobeniusGraphLine_polynomials_of_sample` in `ReedSolomon/MutualCorrelatedAgreement/GraphLine.lean`; `exists_exactPair_fullDimension`, `exists_exceptional_fullDimension_lineMCA` in `.../MutualCorrelatedAgreement/FullDimension.lean` | `Lagrange.{map_basisDivisor,map_basis,map_interpolate,eq_map_interpolate_add_C_mul_of_eval_eq,eq_expand_map_interpolate_add_C_mul_of_eval_eq}` in `ArkLib.ToMathlib.LinearAlgebra.LagrangeLine`; `Finset.exists_card_le_forall_add_mul_eq_add_mul_iff` in `ArkLib.ToMathlib.Finset.LineAgreement`; acceptance cases in the matching `ArkLibTest` files | Graph-line recognition is Lagrange interpolation transported along a ring hom; Frobenius point recognition adds `expand (p ^ e)` for Taylor-sparse polynomials at `p ^ e`-th roots. Index type is any `Fintype ι` (source `Fin n`); `mappedDomain` is `domain.trans ⟨φ, φ.injective⟩` as in `PowerAgreement.lean`. Exceptional-challenge bounds are sharpened from `n` to `card ι - #agreement`; the Frobenius root hypothesis is needed only on the sample. `Ordinary/Frobenius/PointRecognition` is ported inside `GraphLine.lean` (its module name exceeds 86 characters). The corresponding field, degree-bound, node-injectivity, Taylor-sparsity and full-dimension conditions remain in the ported results; the consolidated tests make no boundary-necessity claim for them. Deferred: SharpEquation, UnifiedCurve, PolynomialCurve/Assembly, Pairs/*, LineToAffine, TaylorChart/*, other Ordinary/Frobenius/* files. |
| [P6 slice 4: correlated pair families, exceptional sets, line-to-affine transfer and capacity midpoint (#982)](https://github.com/Verified-zkEVM/ArkLib/pull/982) | `correlatedPairFamily`, `mem_correlatedPairFamily_iff`, `mem_correlatedPairFamily_of_commonAgreement`, `correlatedPairFamily_card_le`, `finite_commonAgreement_pairs`, `correlatedPairSpecialization{,_eq_add_smul}`, `injective_map_pair`, `pair_eq_of_correlatedPairSpecialization_eq_at_two`, `subsingleton_correlatedPair_collisions`, `finite_correlatedPair_collision_challenges`, `exists_correlatedPairSpecialization_injOn_avoiding{,_roots}` in `ReedSolomon/MutualCorrelatedAgreement/Pairs/Family.lean`; `exists_exceptional_correlatedPairFamily{,_le_sum}` in `.../Pairs/ExceptionalSet.lean`; `LineExactAgreementBound{,.mono,.exists_forall_not_isProjectionBad}`, `affineLine_bad_set_card_le_of_exactAgreement`, `mcaError_affineLine_le{,_min_one}_of_exactAgreement`, `affineSpace_bad_density_le_of_exactAgreement`, `mcaError_affineSpace_le_of_exactAgreement`, `exists_affine_exceptionalSet_full_agreement_of_exactLine` in `.../LineToAffine.lean`; `correlatedMidpoint{,_bounds}` in `.../Capacity/Midpoint.lean` | `eq_and_eq_of_add_smul_eq_add_smul_of_ne`, `subsingleton_setOf_add_smul_eq_add_smul`, `Set.Finite.finite_setOf_not_injOn_add_smul`, `Set.Finite.exists_notMem_injOn_add_smul` in `ArkLib.ToMathlib.LinearAlgebra.LineInjectivity`; `Nat.add_floor_half_bounds` in `ArkLib.ToMathlib.Order.FloorHalf`; `CoreDefinitions.mcaError_le_ofReal_of_forall_card_le`, `AffineMCAMain.card_filter_isMCA_affineSpaceGenerator_div_le`, `AffineMCAMain.mcaError_affineSpaceGenerator_le_of_forall_card_le` in `ArkLib.Data.CodingTheory.ProximityGenerator.AffineGenerator`; acceptance cases in the matching `ArkLibTest` files | Pair-specialization collisions are line injectivity in a torsion-free module over a domain. The count-to-`mcaError` and line-to-affine-space transfer are generic generator lemmas. Index type is any `Fintype ι` (source `Fin n`); `1 ≤ s` is dropped (dimension 0 has no bad seeds); `LineExactAgreementBound` drops the unused `[Fintype F]`. Torsion-freeness, `Infinite E` and `0 ≤ x` are tested necessary; source-shaped `Fin n`/`hs` forms derived in tests. Deferred: providers of `LineExactAgreementBound`, `mcaError_interleaved_le_of_exactAgreement`. |
| [P6 slice 5: half-gap line MCA and full-rate RS codes (#986)](https://github.com/Verified-zkEVM/ArkLib/pull/986) | `code_eq_top_of_card_le`, `fullRate_code_eq_top`, `mcaError_eq_zero_of_card_le`, `mcaError_affineLine_fullRate_eq_zero` in `ReedSolomon/MutualCorrelatedAgreement/Johnson/FullCode.lean`; `HalfGapCertificate`, `exists_halfGapCertificate`, `exists_exactPair_of_halfGapCertificate`, `exists_exactPair_of_messageDim_add_half_blockLength_le`, `exists_exceptionalSet_exactAgreement_of_messageDim_add_half_blockLength_le` in `.../HalfGap/Line.lean`; the source's `Ordinary/Factors/FactorBounds` is covered by `Ordinary/FactorBudget.lean` (P6 slice 1) | `CoreDefinitions.not_isMCA_of_forall_mem`, `CoreDefinitions.mcaError_top_eq_zero` in `ArkLib.Data.CodingTheory.ProximityGenerator.AffineGenerator`; `Polynomial.natDegree_eval_C_le` in `ArkLib.ToMathlib.Polynomial.NatDegreeOfSum`; acceptance cases in the matching `ArkLibTest` files | `Fin n` generalized to `Fintype ι`. The half-gap theorem drops `0 < k` and `A ≤ n`, and a new one-pair form fixes `(F₀, G₀)` before the challenge with at most `2 * card ι - k` exceptions. The certificate's numerator bound is `degree < A`. Interpolation and recognition reuse the `GraphLine` lemmas (#980). `mcaError_affineLine_fullRate_eq_zero` needs `[SampleableType F]` for the current `mcaError`. Deferred: the rest of `HalfGap/` and `Johnson/`; a field-extension form. |
| [P3 slice 10: global interpolant from a rank comparison (#976)](https://github.com/Verified-zkEVM/ArkLib/pull/976) | `range_exact_coefficient_local_constraint_at_eq` (as `range_exactCoefficientLocalConstraintAt`), `finrank_range_global_exact_coefficient_constraint_map_le_sum_local` (as `finrank_range_globalExactCoefficientConstraintMap_le_sum`), `exists_nonzero_global_interpolation_coefficients_of_rank_lt`, `exists_nonzero_global_interpolant_of_rank_lt`, `exists_nonzero_global_interpolant_of_rank_le`, `exists_nonzero_global_interpolant_of_local_rank_sum_lt`, `exists_nonzero_global_interpolant_of_local_rank_bounds`, `exists_nonzero_global_interpolant_of_uniform_local_rank_bound` in `HiddenDerivative/Interpolation/Global/Interpolation.lean`; `Global/Dimension.lean` was already on main as `Interpolation/Dimension.lean` (P3 slice 5) | `LinearMap.ker_ne_bot_of_finrank_range_lt`, `LinearMap.exists_ne_zero_map_eq_zero_of_finrank_range_lt`, `LinearMap.finrank_range_pi_le_sum` in `ArkLib.ToMathlib.LinearAlgebra.FiniteDimensional`; the RS declarations in `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Global.Interpolation`; acceptance cases in the matching `ArkLibTest` files | Rank–nullity and the product-rank bound are stated over a division ring with no finite-dimensionality hypothesis; the product-rank bound replaces the source's `includeRange`. `[Fintype ι]` is dropped from the three existence theorems stated in terms of the global rank (tested with centers indexed by `ℕ`). The strict rank inequality is tested necessary. Deferred: `Interpolation/Global/Multiplicity.lean` (needs `Interpolation/SpecializationDegree` and the `RootFinding/FiniteField/WitnessCounting` chain). |
| [P3 slice 11: rate-partition support at real cutoffs, interpolant and area bound (#977)](https://github.com/Verified-zkEVM/ArkLib/pull/977) | `RatePartitionEligible`, `ratePartitionExponents`, `ratePartitionSpace`, `ratePartitionRankBound` (covered by `PartitionSupportEligible`, `partitionSupportExponents`, `partitionSupportSpace`, `localDerivativeCoordinateBudget`); `card_ratePartitionExponents_eq_dimensionCount` (as `finrank_partitionSupportSpace_eq_partitionSourceCount_natCeil`), `card_ratePartitionExponents_pos` (as `card_partitionSupportExponents_pos_iff`), `totalJetDegree_lt_of_ratePartitionEligible` (as `totalJetDegree_lt_of_partitionSupportEligible`) in `HiddenDerivative/Interpolation/RatePartition/Basic.lean`; `exists_nonzero_ratePartition_interpolant` (as `exists_nonzero_partitionSupport_interpolant`, from `exists_ne_zero_satisfiesLocalConstraints_of_derivative_weight`) in `.../RatePartition/Interpolation.lean`; `ratePartition_dimension_ge_quadratic_sum` (as `partitionSupport_dimension_ge_quadratic_sum_real`) and `partitionSupport_dimension_ge_rate_sum_real` in `.../RatePartition/Area.lean`; the source's `Dimension.lean` and `Rank.lean` are covered by `PartitionSupport/Counting.lean` and `Local/Coordinates.lean` | `LinearMap.exists_ne_zero_of_sum_finrank_range_lt` in `ArkLib.ToMathlib.LinearAlgebra.FiniteDimensional`; the real-cutoff, nonemptiness and degree lemmas in `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.PartitionSupport.Basic`, `finrank_partitionSupportSpace_eq_partitionSourceCount_natCeil` in `...PartitionSupport.Counting`, the `_real` dimension bounds in `...PartitionSupport.Dimension`, and the interpolants in `...PartitionSupport.LocalRank`; acceptance cases in the matching `ArkLibTest` files | The source's rate-partition eligibility is definitionally the partition-support eligibility, so no second space is defined; each source declaration is ported or mapped to an existing one (see the PR description). The source's `RatePartition/{Basic,Area,Interpolation}.lean` have no `RatePartition` module here; their results live with the partition support owners. Real cutoffs reduce to `⌈L⌉₊`. The aggregate retains concrete partition-dimension, rate-bound, moment and surplus cases; generic applications and hypothesis-necessity examples were removed. The joint-kernel lemma is over a division ring with no finite-dimensionality hypothesis; this slice's copy of `finrank_range_pi_le_sum` was dropped for #976's more general one. Deferred: `RatePartition/{Adapter,Bound,Certificate,Integral,RankEstimate,Ratio,SharpRatio,SourceEstimate}`. |
| [P3 slice 12: symbolic local coordinate rank, exact-characteristic budget, curve height (#984)](https://github.com/Verified-zkEVM/ArkLib/pull/984) | `weightedSupportPointTranslation`, `weightedSupportLocalCoordinateMatrix`, `rank_weightedSupportLocalCoordinateMatrix{,_eq_zero,_le_actual,_le_base,_le_base_actual}`, `weightedSupportLocalCoordinateMatrix_map` in `HiddenDerivative/Interpolation/Symbolic/LocalRank.lean`; `exactInterpolationJetDegreeFloorAt` and its bounds in `.../Parameters/ExactCharacteristicBudget.lean`; `curveInterpolationHeight` and `curveInterpolationHeight_preserves_certificate` in `.../Interpolation/Symbolic/CurveHeight.lean` | `Matrix.rank_of_basis`, `Matrix.rank_map_le` in `ArkLib.ToMathlib.LinearAlgebra.Matrix.Rank`; `coe_basisRestrictSupport_apply`, `degreeOf_le_div_of_mem_restrictWeightAtMost` in `ArkLib.Data.MvPolynomial.WeightAtMost`; `Finsupp.apply_smul_le_weight`; acceptance cases in the matching `ArkLibTest` files | `m` is explicit in `LocalRank`. `WeightedSupportIndex` is not ported; columns use the `weightedSupportExponents` subtype. The characteristic lemmas state `jetDegree Q j < ringChar F` in place of the unported `IsBelowCharacteristic`. `0 < ℓ` dropped from the certificate lemma. Deferred: `IsBelowCharacteristic` forms; `Symbolic/ReceivedLine` (needs `PrimitivePolynomialKernel`) and `Symbolic/SpecializationDegree` (needs `RootFinding.DegreeBounds`). |
| [P3 slice 8: normalized contact corollary and weighted/partition-support local ranks (#973)](https://github.com/Verified-zkEVM/ArkLib/pull/973) | `X_sub_C_pow_dvd_differentialSpecialization_of_normalizedLocalConstraint` in `HiddenDerivative/Interpolation/Local/RemainderMap.lean`; the derivative-weight coordinates of `.../Local/Coordinates.lean`; `.../WeightedSupport/LocalRank.lean`; `.../PartitionSupport/Basic.lean` and `.../PartitionSupport/LocalRank.lean` (including `partitionLocalRankBound`) | `X_sub_C_pow_dvd_differentialSpecialization_of_normalizedLocalConstraint` in `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Local.RemainderMap`; `localDerivativeCoordinateBudget`, `localDerivativeExponent`, `localDerivativeExponents`, `card_localDerivativeExponents_le`, `weight_localDerivativeJetWeight`, `mem_localDerivativeExponents_of_bounds`, `finrank_range_localConstraintAt_domRestrict_le_of_derivative_weight` in `...Interpolation.Local.Coordinates`; `weightedSupport_localConstraint_support`, `weightedSupport_localConstraint_mem_exponents`, `weightedSupportLocalConstraint`, `finrank_weightedSupportLocalConstraint_le` in `...Interpolation.WeightedSupport.LocalRank`; `PartitionSupportEligible`, `partitionSupportSpace`, `partitionSupportSpace_le_weightedSupportSpace` in `...Interpolation.PartitionSupport.Basic`; `finrank_partitionSupportLocalConstraint_le` in `...Interpolation.PartitionSupport.LocalRank`; acceptance cases in the matching `ArkLibTest` files | The weighted-support local rank budget is `localResidualCoordinateBudget d m W ⌈L / D⌉₊`, with the real cutoff entering only through the natural ceiling. The partition-support budget is `localDerivativeCoordinateBudget d m W`, equal to the source's `partitionLocalRankBound`. Uses #964's `WeightedSupport/Basic` (with `hD : 0 < D` on the spaces) unchanged. `weightedSupportBasis` and `decoder_bounds` are not restated. The aggregate retains concrete local-rank and weighted-support dimension cases at positive `D`; the `D = 0` necessity example was removed. Deferred: `WeightedSupport/{NormalizedRank,Margin,RankBound,Interpolation}` and `PartitionSupport/{Counting,Dimension,FiniteSurplus,FloorTransfer,MomentSource,RankBound}`. |
| [P9 slice 11: rank rounding, weighted-support rank bound and normalized rank (#975)](https://github.com/Verified-zkEVM/ArkLib/pull/975) | `radius_lower`, `radius_ge_twice_order`, `kappa_floor_bounds`, `kappa_interval`, `binomial_error_le`, `kappa_exponent_le`, `kappa_multiplicity_error_le`, `kappa_reciprocal_factor_le`, `prescribed_kappa_bounds` in `HiddenDerivative/Parameters/RankRounding.lean`; `ceil_residual_le` (as `Nat.cast_ceil_le_max_add_one`), `localResidualCoordinateBudget_le_positivePart_sum`, `finrank_weightedSupportLocalConstraint_le_positivePart_sum`, `localResidualCoordinateBudget_le_geometric` in `.../Interpolation/WeightedSupport/RankBound.lean`; `localResidualCoordinateBudget_le_weighted_integral_geometric`, `localResidualCoordinateBudget_div_volume_mul_cube_le`, `finrank_weightedSupportLocalConstraint_div_volume_mul_cube_le`, `normalized_rank_lt_of_rounding_bounds`, `finrank_weightedSupportLocalConstraint_lt_of_harmonic_error_and_rounding`, `finrank_weightedSupportLocalConstraint_lt_prescribed_rounding_of_harmonic_error` in `.../Interpolation/WeightedSupport/NormalizedRank.lean`; `floor_reciprocal_le`, `floor_ratio_bounds` (as `Nat.one_div_floor_le`, `Nat.div_floor_bounds`); `exp_le_rpow` and `endpoint_exp_upper` (as the `Real` lemmas below) | `Nat.cast_ceil_le_max_add_one`, `Nat.cast_ceil_sub_le_max_sub_add_one`, `Nat.one_div_floor_le`, `Nat.div_floor_bounds` in `ArkLib.ToMathlib.Algebra.Order.Floor.Ratio`; `Real.exp_le_exp_mul_rpow_of_le_log_add`, `Real.exp_le_exp_add_mul_rpow_of_le_log_add`, `Real.exp_sixtyOne_div_hundred_lt` in `ArkLib.ToMathlib.Analysis.SpecialFunctions.ExpLogRpow`; the RS declarations in `ArkLib.Data.CodingTheory.HiddenDerivative.{Parameters.RankRounding,Interpolation.WeightedSupport.RankBound,Interpolation.WeightedSupport.NormalizedRank}`; acceptance cases in the matching `ArkLibTest` files | The source's fixed constants (`C = 100`, `999/1000`, `1/1000`, `101/100`, `448/625`, `3/5`, `1/100`, `37/20`) are hypotheses or arguments; the prescribed-rounding theorem keeps `m = ⌈100 d² H⌉₊`, `W = ⌊a d m / H⌋₊` and `1000 ≤ d`, and the test derives the source statement (`a = 1 + 3g/8`, `L = m D (1 + g)`, `48000 ≤ d`). The budget uses P3 slice 8's natural cutoff `⌈T⌉₊`, hence the new `Nat.cast_ceil_sub_le_max_sub_add_one`. The floor-ratio bounds need `0 < R` and `1 ≤ R` instead of the source's `2 ≤ R`. `harmonicPowerSum` is written as `harmonic` and an explicit sum of inverse squares. Unported and unused at the source revision: `multiplicity_lower`, `radius_error_le`, `rounded_mean_bounds`, `normalized_radius_le_quarter`. Deferred: `finrank_weightedSupportLocalConstraint_lt_prescribed` (needs `Parameters/WeightedSupport/{Rounding,ScalarParameters}`), `Parameters/WeightedSupport/{Surplus,DimensionInputs,EndpointComparison}`, `Interpolation/WeightedSupport/{Margin,Interpolation}`. |
| [P9 slice 12: weighted-support scalar parameters, endpoint comparison, surplus and interpolant (#978)](https://github.com/Verified-zkEVM/ArkLib/pull/978) | `xi`, `theta`, `residualFraction`, `rateGap` and the rate-gap lemmas, `le_rateGap_mul`, `xi_le_rateGap_mul`, `one_add_mul_rateGap_div_le`, `max_add_theta_mul_le_one`, `le_inv_of_le_one_add_mul_rateGap_div`, `normalizedRadius_le_ten_twentySeven`, `prescribed_order_lower` in `HiddenDerivative/Parameters/WeightedSupport/ScalarParameters.lean`; `dimensionBaselineFactor`, `dimensionVarianceFactor`, `normalizedDimensionRankSurplus`, `highRate_denominator_sq_le_rate`, `highRate_baseline_lower`, `highRate_variance_lower`, `lowRate_baseline_lower`, `normalizedDimensionRankSurplus_gt` in `.../Endpoints.lean` (source `EndpointComparison.lean`); `normalized_surplus_product`, `multiplicative_margin_from_bounds` in `.../Surplus.lean`; `weightedSupportGlobalConstraint`, `finrank_weightedSupportGlobalConstraint_le`, `exists_nonzero_weightedSupport_interpolant`, `exists_nonzero_exact_interpolant_of_weightedSupport_surplus` in `HiddenDerivative/Interpolation/WeightedSupport/Interpolation.lean` | `Real.exp_one_mul_le_mul_exp_div`, `Real.rpow_one_div_div_self`, `Real.fortyEightThousand_lt_exp_fiftyFour_div_five`, `Real.elevenFourths_lt_exp_eightyOne_div_eighty`, `Real.log_add_le_mul_sqrt_of_le`, `Real.log_add_three_fifths_le_nineteen_div_365_mul_sqrt` in `ArkLib.ToMathlib.Analysis.SpecialFunctions.ExpLogRpow`; `Real.log_le_harmonic_pred`, `Real.sq_le_div_hundred_of_le_log_add_three_fifths` in `ArkLib.ToMathlib.NumberTheory.Harmonic.Bounds`; `Module.Finite` instance for `weightedSupportSpace`; acceptance cases in the matching `ArkLibTest` files | The comparison lemmas take `θ`, `ξ` and the endpoint as parameters; `le_rateGap_mul` generalizes `clippedGap_mul_harmonic_ge_xi` to any `ξ ≥ 0`. `rateGap` moves from EndpointComparison to ScalarParameters. The interpolant budget uses the natural cutoff `⌈L / D⌉₊`, matching `finrank_weightedSupportLocalConstraint_le`. The interpolant lemmas use `LinearMap.finrank_range_pi_le_sum` and `LinearMap.exists_ne_zero_map_eq_zero_of_finrank_range_lt` from P3 slice 10. `exp_one_gt` is replaced by `Real.exp_one_gt_d9`, `lowRate_taylorEight_le_exp` by `Real.sum_le_exp_of_nonneg`. Not ported (no consumer at the source revision): `lowRate_taylorEight_ratio_gt`, `exact_surplus_identity`, `exact_surplus_gt`, `exact_surplus_challenge_ratio_lt`, `targetSurplus_challenge_ratio_eq`, `challenge_ratio_lt_twelve`; `harmonic_le_nineteen_over_365_sqrt` is derived in the test. |
| [P9 slice 13: weighted-support block and rounding parameters, dimension inputs and margin (#981)](https://github.com/Verified-zkEVM/ArkLib/pull/981) | `blockDegree_mul_one_add_rateGap_le`, `blockDegree_bounds`, `prescribedBlockBounds` in `HiddenDerivative/Parameters/WeightedSupport/Block.lean`; `floorRadius_{mul_div_le,normalized_le,sq_ge}`, `remainingDegree_{lower,upper}`, `enlargedRadius_{upper,normalized}`, `residualVariance_le`, `centeringErrorBounds`, `averageResidualError_le`, `residualMeanVariance_le`, `prescribedFiberMeanVariance_le` in `.../Rounding.lean`; `prescribed_dimension_inputs` in `.../DimensionInputs.lean`; `finrank_weightedSupportLocalConstraint_lt_prescribed`, `weightedSupport_margin_of_normalized_rank`, `prescribed_weightedSupport_margin` in `HiddenDerivative/Interpolation/WeightedSupport/Margin.lean` | `Nat.one_sub_one_div_mul_lt_floor` in `ArkLib.ToMathlib.Algebra.Order.Floor.Ratio`; acceptance cases in the matching `ArkLibTest` files | Rounding lemmas take the tilt `θ`, tolerance `ε` and floor threshold `N` as parameters (source: `theta`, `2/1000`/`1/1000`, `2000`). `prescribedBlockBounds` drops the unused `0 < k` and weakens `δ < 1/4` to `≤`; `prescribed_dimension_inputs` needs `3 ≤ d` (source `48000 ≤ d`); `weightedSupport_margin_of_normalized_rank` needs `10000 ≤ d`; `finrank_weightedSupportLocalConstraint_lt_prescribed` (deferred from P9 slice 11) drops `hHlog`, proved by `Real.harmonic_pred_lt_log_add_three_fifths`, and lives in `Margin.lean` to keep NormalizedRank's imports small. `floorRadius_pos` is `Nat.floor_pos`. `12 ≤ δ n`, `δ ≤ 2/3`, `N ≤ R`, `0 ≤ a`, `1 ≤ N` are tested necessary; source-shaped statements derived in tests. |
| [P7a slice 2: specialization degree, global multiplicity and coefficient base change (#979)](https://github.com/Verified-zkEVM/ArkLib/pull/979) | `natDegree_differentialSpecialization_lt_of_mem_exactInterpolationSpace`, `natDegree_differentialSpecialization_exactInterpolationPolynomial_lt`, `eq_zero_of_mem_exactInterpolationSpace_of_mul_eq_zero` in `HiddenDerivative/Interpolation/SpecializationDegree.lean`; `differentialSpecialization_eq_zero_of_mem_exactInterpolationSpace_of_agreements` in `.../Interpolation/Global/Multiplicity.lean` | `Polynomial.eq_zero_of_{degree,natDegree}_lt_mul_of_pow_X_sub_C_dvd_at_injOn` (and `sum_rootMultiplicity_le_natDegree` generalized to domains) in `ArkLib.ToMathlib.Polynomial.RootMultiplicity`; `map_differentialSpecialization`, `map_separant`, `jetDegree_map_eq`, `jetDegreeCastsNeZero_map_iff`, `BoundedSolution.{instFinite,map,map_injective,natCard_le_natCard_map}` in `ArkLib.Data.Polynomial.Differential.BaseChange`; `card_mul_sub_le_of_degree_le` in `ArkLib.Data.Polynomial.Differential.WitnessCount`; acceptance cases in the matching `ArkLibTest` files | Root-multiplicity vanishing and the global multiplicity statement hold over a domain; the global statement drops `0 < m * A` (the exact space is `{0}` when `m * A = 0`). The aggregate retains positive-multiplicity vanishing cases with a nonzero polynomial, distinct points, and computed agreement and constraint hypotheses. `RootFinding/DegreeBounds/SpecializationDegree` was already covered by `JetDegree` and `MvPolynomial/WeightedDegree` except the counting wrapper `card_mul_sub_le_of_degree_le`; `RootFinding/FiniteField/RegularCounting` is `BoundedSolution.card_mul_sub_le_of_isHighestActiveJet` (P7a slice 1); `ToMathlib/FieldTheory/FiniteExtension` is covered by Mathlib's `FiniteField.Extension` and not ported. `RootFinding/FiniteField/Extension` is ported as coefficient base change for any injective ring hom (`isBelowCharacteristic_map_iff` → `jetDegreeCastsNeZero_map_iff`, `algebraMapEmbedding` → `BoundedSolution.map` and `BoundedSolution.map_injective`, `natCard_le_extension` → `natCard_le_natCard_map`). Derived in tests: `BoundedSolution.natDegree_separant_{le,lt}`, `differentialSpecialization_eq_zero_of_global_multiplicity`, `natCard_le_of_extension_bound`, `finset_card_le_extension`. Deferred: `ExtensionRootCount`, `TotalDegreeExtension`, transport of `IsHighestActiveJet`. |
| [P3 slice 8: normalized contact corollary and weighted/partition-support local ranks (#973)](https://github.com/Verified-zkEVM/ArkLib/pull/973) | `X_sub_C_pow_dvd_differentialSpecialization_of_normalizedLocalConstraint` in `HiddenDerivative/Interpolation/Local/RemainderMap.lean`; the derivative-weight coordinates of `.../Local/Coordinates.lean`; `.../WeightedSupport/LocalRank.lean`; `.../PartitionSupport/Basic.lean` and `.../PartitionSupport/LocalRank.lean` (including `partitionLocalRankBound`) | `X_sub_C_pow_dvd_differentialSpecialization_of_normalizedLocalConstraint` in `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Local.RemainderMap`; `localDerivativeCoordinateBudget`, `localDerivativeExponent`, `localDerivativeExponents`, `card_localDerivativeExponents_le`, `weight_localDerivativeJetWeight`, `mem_localDerivativeExponents_of_bounds`, `finrank_range_localConstraintAt_domRestrict_le_of_derivative_weight` in `...Interpolation.Local.Coordinates`; `weightedSupport_localConstraint_support`, `weightedSupport_localConstraint_mem_exponents`, `weightedSupportLocalConstraint`, `finrank_weightedSupportLocalConstraint_le` in `...Interpolation.WeightedSupport.LocalRank`; `PartitionSupportEligible`, `partitionSupportSpace`, `partitionSupportSpace_le_weightedSupportSpace` in `...Interpolation.PartitionSupport.Basic`; `finrank_partitionSupportLocalConstraint_le` in `...Interpolation.PartitionSupport.LocalRank`; acceptance cases in the matching `ArkLibTest` files | The weighted-support local rank budget is `localResidualCoordinateBudget d m W ⌈L / D⌉₊`, with the real cutoff entering only through the natural ceiling. The partition-support budget is `localDerivativeCoordinateBudget d m W`, equal to the source's `partitionLocalRankBound`. Uses #964's `WeightedSupport/Basic` (with `hD : 0 < D` on the spaces) unchanged. `weightedSupportBasis` and `decoder_bounds` are not restated. `D = 0` is tested to break the jet-degree conclusion. Deferred: `WeightedSupport/{NormalizedRank,Margin,RankBound,Interpolation}` and `PartitionSupport/{Counting,Dimension,FiniteSurplus,FloorTransfer,MomentSource,RankBound}`. |
| [P9 slice 11: rank rounding, weighted-support rank bound and normalized rank (#975)](https://github.com/Verified-zkEVM/ArkLib/pull/975) | `radius_lower`, `radius_ge_twice_order`, `kappa_floor_bounds`, `kappa_interval`, `binomial_error_le`, `kappa_exponent_le`, `kappa_multiplicity_error_le`, `kappa_reciprocal_factor_le`, `prescribed_kappa_bounds` in `HiddenDerivative/Parameters/RankRounding.lean`; `ceil_residual_le` (as `Nat.cast_ceil_le_max_add_one`), `localResidualCoordinateBudget_le_positivePart_sum`, `finrank_weightedSupportLocalConstraint_le_positivePart_sum`, `localResidualCoordinateBudget_le_geometric` in `.../Interpolation/WeightedSupport/RankBound.lean`; `localResidualCoordinateBudget_le_weighted_integral_geometric`, `localResidualCoordinateBudget_div_volume_mul_cube_le`, `finrank_weightedSupportLocalConstraint_div_volume_mul_cube_le`, `normalized_rank_lt_of_rounding_bounds`, `finrank_weightedSupportLocalConstraint_lt_of_harmonic_error_and_rounding`, `finrank_weightedSupportLocalConstraint_lt_prescribed_rounding_of_harmonic_error` in `.../Interpolation/WeightedSupport/NormalizedRank.lean`; `floor_reciprocal_le`, `floor_ratio_bounds` (as `Nat.one_div_floor_le`, `Nat.div_floor_bounds`); `exp_le_rpow` and `endpoint_exp_upper` (as the `Real` lemmas below) | `Nat.cast_ceil_le_max_add_one`, `Nat.cast_ceil_sub_le_max_sub_add_one`, `Nat.one_div_floor_le`, `Nat.div_floor_bounds` in `ArkLib.ToMathlib.Algebra.Order.Floor.Ratio`; `Real.exp_le_exp_mul_rpow_of_le_log_add`, `Real.exp_le_exp_add_mul_rpow_of_le_log_add`, `Real.exp_sixtyOne_div_hundred_lt` in `ArkLib.ToMathlib.Analysis.SpecialFunctions.ExpLogRpow`; the RS declarations in `ArkLib.Data.CodingTheory.HiddenDerivative.{Parameters.RankRounding,Interpolation.WeightedSupport.RankBound,Interpolation.WeightedSupport.NormalizedRank}`; acceptance cases in the matching `ArkLibTest` files | The source's fixed constants (`C = 100`, `999/1000`, `1/1000`, `101/100`, `448/625`, `3/5`, `1/100`, `37/20`) are hypotheses or arguments; the prescribed-rounding theorem keeps `m = ⌈100 d² H⌉₊`, `W = ⌊a d m / H⌋₊` and `1000 ≤ d`. The budget uses P3 slice 8's natural cutoff `⌈T⌉₊`, hence the new `Nat.cast_ceil_sub_le_max_sub_add_one`. The floor-ratio bounds need `0 < R` and `1 ≤ R` instead of the source's `2 ≤ R`. `harmonicPowerSum` is written as `harmonic` and an explicit sum of inverse squares. Unported and unused at the source revision: `multiplicity_lower`, `radius_error_le`, `rounded_mean_bounds`, `normalized_radius_le_quarter`. Deferred: `finrank_weightedSupportLocalConstraint_lt_prescribed` (needs `Parameters/WeightedSupport/{Rounding,ScalarParameters}`), `Parameters/WeightedSupport/{Surplus,DimensionInputs,EndpointComparison}`, `Interpolation/WeightedSupport/{Margin,Interpolation}`. |
| [P9 slice 12: weighted-support scalar parameters, endpoint comparison, surplus and interpolant (#978)](https://github.com/Verified-zkEVM/ArkLib/pull/978) | `xi`, `theta`, `residualFraction`, `rateGap` and the rate-gap lemmas, `le_rateGap_mul`, `xi_le_rateGap_mul`, `one_add_mul_rateGap_div_le`, `max_add_theta_mul_le_one`, `le_inv_of_le_one_add_mul_rateGap_div`, `normalizedRadius_le_ten_twentySeven`, `prescribed_order_lower` in `HiddenDerivative/Parameters/WeightedSupport/ScalarParameters.lean`; `dimensionBaselineFactor`, `dimensionVarianceFactor`, `normalizedDimensionRankSurplus`, `highRate_denominator_sq_le_rate`, `highRate_baseline_lower`, `highRate_variance_lower`, `lowRate_baseline_lower`, `normalizedDimensionRankSurplus_gt` in `.../Endpoints.lean` (source `EndpointComparison.lean`); `normalized_surplus_product`, `multiplicative_margin_from_bounds` in `.../Surplus.lean`; `weightedSupportGlobalConstraint`, `finrank_weightedSupportGlobalConstraint_le`, `exists_nonzero_weightedSupport_interpolant`, `exists_nonzero_exact_interpolant_of_weightedSupport_surplus` in `HiddenDerivative/Interpolation/WeightedSupport/Interpolation.lean` | `Real.exp_one_mul_le_mul_exp_div`, `Real.rpow_one_div_div_self`, `Real.fortyEightThousand_lt_exp_fiftyFour_div_five`, `Real.elevenFourths_lt_exp_eightyOne_div_eighty`, `Real.log_add_le_mul_sqrt_of_le`, `Real.log_add_three_fifths_le_nineteen_div_365_mul_sqrt` in `ArkLib.ToMathlib.Analysis.SpecialFunctions.ExpLogRpow`; `Real.log_le_harmonic_pred`, `Real.sq_le_div_hundred_of_le_log_add_three_fifths` in `ArkLib.ToMathlib.NumberTheory.Harmonic.Bounds`; `Module.Finite` instance for `weightedSupportSpace`; acceptance cases in the matching `ArkLibTest` files | The comparison lemmas take `θ`, `ξ` and the endpoint as parameters; `le_rateGap_mul` generalizes `clippedGap_mul_harmonic_ge_xi` to any `ξ ≥ 0`. `rateGap` moves from EndpointComparison to ScalarParameters. The interpolant budget uses the natural cutoff `⌈L / D⌉₊`, matching `finrank_weightedSupportLocalConstraint_le`. The interpolant lemmas use `LinearMap.finrank_range_pi_le_sum` and `LinearMap.exists_ne_zero_map_eq_zero_of_finrank_range_lt` from P3 slice 10. `exp_one_gt` is replaced by `Real.exp_one_gt_d9`, `lowRate_taylorEight_le_exp` by `Real.sum_le_exp_of_nonneg`. Not ported (no consumer at the source revision): `lowRate_taylorEight_ratio_gt`, `exact_surplus_identity`, `exact_surplus_gt`, `exact_surplus_challenge_ratio_lt`, `targetSurplus_challenge_ratio_eq`, `challenge_ratio_lt_twelve`. |
| [P9 slice 13: weighted-support block and rounding parameters, dimension inputs and margin (#981)](https://github.com/Verified-zkEVM/ArkLib/pull/981) | `blockDegree_mul_one_add_rateGap_le`, `blockDegree_bounds`, `prescribedBlockBounds` in `HiddenDerivative/Parameters/WeightedSupport/Block.lean`; `floorRadius_{mul_div_le,normalized_le,sq_ge}`, `remainingDegree_{lower,upper}`, `enlargedRadius_{upper,normalized}`, `residualVariance_le`, `centeringErrorBounds`, `averageResidualError_le`, `residualMeanVariance_le`, `prescribedFiberMeanVariance_le` in `.../Rounding.lean`; `prescribed_dimension_inputs` in `.../DimensionInputs.lean`; `finrank_weightedSupportLocalConstraint_lt_prescribed`, `weightedSupport_margin_of_normalized_rank`, `prescribed_weightedSupport_margin` in `HiddenDerivative/Interpolation/WeightedSupport/Margin.lean` | `Nat.one_sub_one_div_mul_lt_floor` in `ArkLib.ToMathlib.Algebra.Order.Floor.Ratio`; acceptance cases in the matching `ArkLibTest` files | Rounding lemmas take the tilt `θ`, tolerance `ε` and floor threshold `N` as parameters (source: `theta`, `2/1000`/`1/1000`, `2000`). `prescribedBlockBounds` drops the unused `0 < k` and weakens `δ < 1/4` to `≤`; `prescribed_dimension_inputs` needs `3 ≤ d` (source `48000 ≤ d`); `weightedSupport_margin_of_normalized_rank` needs `10000 ≤ d`; `finrank_weightedSupportLocalConstraint_lt_prescribed` (deferred from P9 slice 11) drops `hHlog`, proved by `Real.harmonic_pred_lt_log_add_three_fifths`, and lives in `Margin.lean` to keep NormalizedRank's imports small. `floorRadius_pos` is `Nat.floor_pos`.  |
| [P7a slice 2: specialization degree, global multiplicity and coefficient base change (#979)](https://github.com/Verified-zkEVM/ArkLib/pull/979) | `natDegree_differentialSpecialization_lt_of_mem_exactInterpolationSpace`, `natDegree_differentialSpecialization_exactInterpolationPolynomial_lt`, `eq_zero_of_mem_exactInterpolationSpace_of_mul_eq_zero` in `HiddenDerivative/Interpolation/SpecializationDegree.lean`; `differentialSpecialization_eq_zero_of_mem_exactInterpolationSpace_of_agreements` in `.../Interpolation/Global/Multiplicity.lean` | `Polynomial.eq_zero_of_{degree,natDegree}_lt_mul_of_pow_X_sub_C_dvd_at_injOn` (and `sum_rootMultiplicity_le_natDegree` generalized to domains) in `ArkLib.ToMathlib.Polynomial.RootMultiplicity`; `map_differentialSpecialization`, `map_separant`, `jetDegree_map_eq`, `jetDegreeCastsNeZero_map_iff`, `BoundedSolution.{instFinite,map,map_injective,natCard_le_natCard_map}` in `ArkLib.Data.Polynomial.Differential.BaseChange`; `card_mul_sub_le_of_degree_le` in `ArkLib.Data.Polynomial.Differential.WitnessCount`; acceptance cases in the matching `ArkLibTest` files | Root-multiplicity vanishing and the global multiplicity statement hold over a domain; the global statement drops `0 < m * A` (the exact space is `{0}` when `m * A = 0`). The domain, distinct points and strict degree bound are tested necessary. `RootFinding/DegreeBounds/SpecializationDegree` was already covered by `JetDegree` and `MvPolynomial/WeightedDegree` except the counting wrapper `card_mul_sub_le_of_degree_le`; `RootFinding/FiniteField/RegularCounting` is `BoundedSolution.card_mul_sub_le_of_isHighestActiveJet` (P7a slice 1); `ToMathlib/FieldTheory/FiniteExtension` is covered by Mathlib's `FiniteField.Extension` and not ported. `RootFinding/FiniteField/Extension` is ported as coefficient base change for any injective ring hom (`isBelowCharacteristic_map_iff` → `jetDegreeCastsNeZero_map_iff`, `algebraMapEmbedding` → `BoundedSolution.map` and `BoundedSolution.map_injective`, `natCard_le_extension` → `natCard_le_natCard_map`). Derived in tests: `BoundedSolution.natDegree_separant_{le,lt}`, `differentialSpecialization_eq_zero_of_global_multiplicity`, `natCard_le_of_extension_bound`, `finset_card_le_extension`. Deferred: `ExtensionRootCount`, `TotalDegreeExtension`, transport of `IsHighestActiveJet`. |
| [P9 slice 14: capacity parameters, interpolation certificates, solution embedding (#985)](https://github.com/Verified-zkEVM/ArkLib/pull/985) | `capacityDerivativeOrder`, `capacityDerivativeOrder_lower`, `weightedSupportMultiplicity`, `weightedSupportAmbientDimension`, `LargeFieldCondition`, `capacity_block_bounds` in `HiddenDerivative/Parameters/WeightedSupport/Capacity.lean`; `InterpolationCertificate`, `HiddenDerivativeInterpolationCertificate`, `specializes_to_zero`, `below_characteristic`, `UniformHiddenDerivativeInterpolation`, `WeightedSupportConstruction` in `.../Interpolation/Certificates.lean`; `solutionEmbedding`, `exists_solution`, `encard_agreeingPolynomials_le` in `.../Interpolation/SolutionEmbedding.lean`; `differentialSpecialization_eq_zero_of_differentialWeightedDegree_lt` in `.../Interpolation/Global/Multiplicity.lean`; `agreeingPolynomials` in `ReedSolomon/AgreementList.lean` | `harmonic` from Mathlib covers the source's `Parameters/Harmonic`; acceptance cases in the matching `ArkLibTest` files | `weightedSupportMultiplicity` takes `d` rather than `δ`. The certificate is split into a generic part over any commutative ring and a `ZMod q` extension; the `IsBelowCharacteristic` field becomes `castsNeZero`, and `below_characteristic` is derived. `agreementThreshold δ n k` is written out as `k + ⌈δ * n⌉₊`. Deferred: `agreeingPolynomials_antitone`, `exists_finset_polynomial_list`, `agreeingPolynomials_eq_empty_of_card_lt`; `ListDecodability/Capacity/WeightedSupportInterpolant` (blocked on the `Symbolic`, `TotalDegreeExtension` and `HiddenDerivativeBound` ports). |
| [P3 slice 9: partition-support dimension, moment lower bound and finite surplus (#974)](https://github.com/Verified-zkEVM/ArkLib/pull/974) | `HiddenDerivative/Interpolation/PartitionSupport/{Counting,Dimension,FloorTransfer,MomentSource,FiniteSurplus,RankBound}.lean` (`PartitionSourceIndex` and its lemmas, `partitionSourceCount`, `finrank_partitionSupportSpace_eq_sourceCount`, `partition_residual_ceil`, `quadraticStaircase_le_partition_slice`, `partitionSupport_dimension_ge_quadratic_sum`, `partition_quadratic_rate_lower`, `partition_floor_square_integral`, `partition_square_rescale`, `partition_integral_ge_normalized_moment`, `partition_integral_eq_volume_mul_expectation`, `partition_contact_exp_sum_le`, `partitionLocalRankBound_le_geometric`, `partition_choose_two_real`, `partition_count_le_volume`) | `QuadraticStaircase.{ceil_mul_div_sub_sub, count_div_sub_eq_sum}` in `ArkLib.ToMathlib.Combinatorics.QuadraticStaircase`; `sum_contactThreshold_mul_exp_le_slots`, `localDerivativeCoordinateBudget_le_geometric` in `...Interpolation.Local.RankBudget`; `partitionSourceExponent{,_none,_zero,_succ,_eta}`, `fullDerivativeJetWeight_eq_sum_succ`, `totalJetDegree_eq_zero_add_sum_succ`, `partitionSupportEligible_partitionSourceExponent_iff`, `partitionSourceCount`, `card_partitionSupportExponents`, `finrank_partitionSupportSpace_eq_partitionSourceCount` in `...PartitionSupport.Counting`; `finrank_partitionSupportSpace_eq_sum_count`, `partitionSupport_dimension_ge_quadratic_sum`, `partition_quadratic_rate_lower`, `partitionSupport_dimension_ge_rate_sum` in `...PartitionSupport.Dimension`; `partition_floor_square_integral`, `partitionSupport_dimension_ge_rate_integral` in `...PartitionSupport.FloorTransfer`; `partition_square_rescale`, `partition_integral_ge_normalized_moment`, `partitionSupport_dimension_gt_moment` in `...PartitionSupport.MomentSource`; `finrank_partitionSupportLocalConstraint_le_geometric`, `partitionSupport_surplus`, `partitionSupport_localConstraint_surplus` in `...PartitionSupport.FiniteSurplus`; acceptance cases in the matching `ArkLibTest` files | The dimension is counted exactly (`finrank = partitionSourceCount`), replacing the source's injective index type. The cutoff `m * A` is a natural `L`, `m * agreement` a real `level`, and `27/10` an arbitrary `μ`; `hn`, `hrate`, strict positivity in the rescaling, `0 < d`, `0 < budget`, `0 < s` and `0 < m` are dropped where unused. The aggregate retains a concrete `d = 500` moment case and positive-margin finite-surplus cases with numeric hypotheses; the former hypothesis-necessity examples were removed. `localDerivativeCoordinateBudget` moves to `Interpolation/Counting.lean`. `RankBound.lean` is split between `Local/RankBudget.lean` and `FiniteSurplus.lean`. `partition_integral_eq_volume_mul_expectation` is Mathlib's `setAverage_eq`; `partition_choose_two_real` is `Nat.cast_choose_two`. Deferred: `finiteGamma_mul_rankEnvelope` and the source-shaped `partitionSupport_finiteGamma_surplus` (need `Parameters/RatePartition/FiniteRatio.lean`). |
| [P7a slice 3: recursive and total-jet-degree counts of bounded solutions (#983)](https://github.com/Verified-zkEVM/ArkLib/pull/983) | — (all content is generic) | `differentialWeightedDegree_le_of_{singularStep,reflTransGen_singularStep}`, `jetDegree_le_of_{singularStep,reflTransGen_singularStep}`, `jetTotalDegree_le_of_{singularStep,reflTransGen_singularStep}`, `jetTotalDegree_le_sum_jetDegree`, `jetTotalDegree_le_mul`, `card_mul_le_jetTotalDegree_mul` in `ArkLib.Data.Polynomial.Differential.RecursiveCount`; `ChainWitness{,.solves,.jetEvaluation_eq_zero,.initialJetEquation_ne_zero,.eq_of_polynomialJet_eq}` and `exists_chainWitness` in `ArkLib.Data.Polynomial.Differential.ChainWitness`; `initialJetEquation`, `map_initialJetEquation`, `aeval_initialJetEquation`, `aeval_map_initialJetEquation`, `totalDegree_initialJetEquation_le`, `pderiv_initialJetEquation` in `ArkLib.Data.Polynomial.Differential.JetDegree` (replacing `jetFiberHom`, `eval_jetFiberHom`, `jetFiberHom_separant`, and `totalDegree_jetFiberHom_le`); `card_filter_chainWitness_le`, `card_mul_sub_le_jetTotalDegree_mul`, `BoundedSolution.natCard_mul_sub_le_jetTotalDegree_mul`, `BoundedSolution.natCard_le_two_mul_jetTotalDegree_mul{,_of_injective,_extension}` in `ArkLib.Data.Polynomial.Differential.TotalJetDegreeCount`; `jetTotalDegree_map_eq` in `ArkLib.Data.Polynomial.Differential.BaseChange`; `MvPolynomial.card_filter_eval_eq_zero_le` in `ArkLib.ToMathlib.MvPolynomial.SchwartzZippel`; acceptance cases in the matching `ArkLibTest` files | Ports `RootFinding/FiniteField/{RecursiveCounting,TotalDegreeWitness,TotalDegreeExtension}` and `RootFinding/Counting/TotalJetDegreeRootCount`; `FiniteField/ExtensionRootCount` is derived in tests from the sharper total-jet-degree count. `jetTotalDegree` replaces `jetDegreeMeasure` (it is at most `(d + 1) t`, the source measure `(d + 1) t²`); `RegularBranchBudget` is the inline `hregular` hypothesis; `IsBelowCharacteristic` becomes `JetDegreeCastsNeZero` plus binomial nonvanishing. New: the count over any finite domain receiving an injective ring hom. The total count and the Schwartz–Zippel count are tested attained. |
| [P7a slice 6: symbolic separant chains, jet-prefix presentations and first-order stage sums (#1007)](https://github.com/Verified-zkEVM/ArkLib/pull/1007) | `ReedSolomon/HiddenDerivative/RootFinding/Symbolic/{SeparantChain,JetPrefix,FirstOrderStageSum}.lean` | `ArkLib.Data.Polynomial.Differential.{SeparantChain,JetPrefixPresentation,FirstOrderStageSum}`, two lemmas in `ArkLib.Data.Polynomial.Differential.BaseChange`; acceptance cases in the matching `ArkLibTest` files | Moved to `Data/Polynomial/Differential`; `Chain`/`Stage`/`Presentation` → `SeparantChain`/`SeparantStage`/`JetPrefixPresentation`, `jetWeight` → `jetTotalDegree`; per-jet cast hypotheses without `Nontrivial`; specialization into any commutative semiring (a domain for the exceptional set); charges in any additive commutative monoid. `exists_symbolic_chain` and the conjunction forms are derived in tests. |
| [P1 slice 8: option-variable weighted degree, offset support weights (#991)](https://github.com/Verified-zkEVM/ArkLib/pull/991) | `Finsupp.weight_optionElim`, `optionEquivRight_monomial`, `optionEquivRight_coeff_coeff`, `weight_optionElim_zero_one`, `weightedTotalDegree_optionEquivRight`, `totalDegree_optionEquivRight` in `ToMathlib/MvPolynomial/OptionWeightedDegree.lean`; `SupportWeightOffset` with `mono`, `monomial`, `C`, `add`, `mul`, `pow`, `sum`, `prod`, `supportWeightOffset_aeval`, `supportWeightOffset_zero_iff` in `ToMathlib/MvPolynomial/SupportWeightOffset.lean` | acceptance cases in the matching `ArkLibTest` files | `SupportWeightOffset` unchanged. `weightedTotalDegree_option_zero_one_mul` and `_le_of_dvd` not ported: main's `weightedTotalDegree_mul` and `weightedTotalDegree_le_of_dvd` cover them, and the tests derive both. |
| [P5 slice 15: rectangle differences and bidegree Hilbert bounds (#1003)](https://github.com/Verified-zkEVM/ArkLib/pull/1003) | `ToMathlib/Polynomial/RectangleDifference{,General}.lean`; `ToMathlib/AlgebraicGeometry/Hilbert/Bidegree.lean` | `ArkLib.ToMathlib.Polynomial.RectangleDifference`, `ArkLib.ToMathlib.RingTheory.MvPolynomial.Bidegree`, `ArkLib.ToMathlib.RingTheory.MvPolynomial.AffineHilbertBidegree`; general lemmas in `AffineDegree`, `AffineHilbertPurity`, `AffineHilbertAlgHom` and `OptionWeightedDegree`; acceptance cases in the matching `ArkLibTest` files | General `s` and finite `σ` in place of the `One`/`Two` and `Fin (r + 1)` forms; the bidegree ideal is the kernel of `bidegreeMap` and the hypersurface is a comap; properness and nonvanishing hypotheses dropped. Not ported: `bidegreeCutMap_surjective`. Downstream `BidegreePoints`, `BidegreeExcluded`, `DerivativeBidegree` and `TaylorBidegree` build on this API. |
| [P5 slice 16: monomial maps, bidegree points and capped-bidegree bounds (#1004)](https://github.com/Verified-zkEVM/ArkLib/pull/1004) | `ToMathlib/AlgebraicGeometry/Hilbert/{BidegreePoints,DerivativeBidegree}.lean` | `ArkLib.ToMathlib.RingTheory.MvPolynomial.{MonomialMap,AffineHilbertComap,CappedBidegree,AffineHilbertCappedBidegree}`; acceptance cases in the matching `ArkLibTest` files | Bidegree and derivative-bidegree maps and points are instances of `monomialMap` and `monomialPoint`; pullback bounds for any surjective algebra map; the cap is on any `some i` of any finite `σ`; the affine-degree bound drops `r ≤ j` and properness and needs only `c ≤ b`. Inlined: `derivativeWeight`, the image-degree helpers and `cappedRectangleDifferenceTwo`. Downstream `TwoJetDegree`, `DerivativeBidegreePoints`, `BidegreeExcluded` and `DerivativeBidegreeExcluded` use the capped API. |
| [P5 slice 17: capped-degree monomial maps and curve affine-degree bounds (#1005)](https://github.com/Verified-zkEVM/ArkLib/pull/1005) | `ToMathlib/AlgebraicGeometry/Hilbert/{TwoJetDegree,DerivativeBidegreePoints}.lean` | `ArkLib.ToMathlib.RingTheory.MvPolynomial.{CappedDegree,AffineHilbertCappedDegree}`; acceptance cases in the matching `ArkLibTest` files and the `CappedBidegreePoints` section of the `CappedBidegree` test | The two-jet map is `monomialMap` on `cappedDegreeExponents`, for any `σ` and cap coordinate; the affine-degree bound drops `c ≤ b` and properness. Derivative-bidegree points are special cases of `MonomialMap` and get no module. Inlined: `twoJetDifference`, `quotientTwoJetLE`, `twoJetCutMap`. |
| [P7b slice 2: first-order interpolation spaces, dimension counts and interpolants (#1009)](https://github.com/Verified-zkEVM/ArkLib/pull/1009) | `ReedSolomon/HiddenDerivative/Interpolation/FirstOrder/{Basic,Counting,Interpolation}.lean` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.FirstOrder.{Space,Dimension,Interpolant}`; acceptance cases in `ArkLibTest/Data/CodingTheory/HiddenDerivative/Interpolation/FirstOrder.lean` | `1 < D` dropped from the rank bounds and interpolant existence (embedding into an exact space with larger `D`, `A`); one `card_nbij'` count replaces the coordinate equivalences; the multiplicity theorem picks `Q` before `P` and needs agreement only at each point. The rank lemmas come from P3 slice 10 (`LinearMap.finrank_range_pi_le_sum`, `LinearMap.exists_ne_zero_map_eq_zero_of_finrank_range_lt`). |
| [P5 slice 18: sharp agreement incidence and polynomial graph counting (#1008)](https://github.com/Verified-zkEVM/ArkLib/pull/1008) | `ToMathlib/AlgebraicGeometry/Incidence/{SharpExcluded,SharpCutFamily,SharpPrimeFamily,OneJetGraphCounting}.lean` | Sharp bounds in `ArkLib.ToMathlib.RingTheory.Nullstellensatz.AgreementIncidence`; `ArkLib.ToMathlib.MvPolynomial.OptionRoots`; acceptance cases in the matching `ArkLibTest` files | One ratio theorem gives both the old and the sharp bound; the sharp theorems drop `0 < b`, `A ≤ card ι` and `s ∉ P`; high cuts of degree at most `h`; the prime-family bound is derived in a test; `oneJetRootPolynomial` is Mathlib's `optionEquivLeft`. |
| [P5 slice 19: evaluation dimension and dimension-sensitive agreement incidence (#1012)](https://github.com/Verified-zkEVM/ArkLib/pull/1012) | `ToMathlib/AlgebraicGeometry/Incidence/{EvaluationDimension,DimensionSensitive}.lean` | `ArkLib.ToMathlib.RingTheory.MvPolynomial.CoefficientEvaluation`, `ArkLib.ToMathlib.RingTheory.Nullstellensatz.DimensionSensitiveIncidence`, `ArkLib.ToMathlib.Combinatorics.Enumerative.IncidenceProduct`, `ArkLib.ToMathlib.LinearAlgebra.Matrix.InvertibleCombination`; one threshold induction in `AgreementIncidence`; acceptance cases in the matching `ArkLibTest` files | One induction over threshold functions gives the constant, sharp, dimension-sensitive and hybrid bounds; `s ∉ P`, `A ≤ n` and `J ≠ ⊤` dropped; additive budgets; iterated families take the weighted potential, so no `B ^ d`; the Vandermonde step holds for submodules. |
| [P8 slice 2: exact power agreement for constant messages (#1006)](https://github.com/Verified-zkEVM/ArkLib/pull/1006) | `ReedSolomon/MutualCorrelatedAgreement/PolynomialCurve/ConstantCode.lean` | `ArkLib.Data.CodingTheory.ReedSolomon.PowerAgreement.ConstantCode`, two list bounds in `ArkLib.Data.CodingTheory.ReedSolomon.AgreementList`; acceptance cases in `ArkLibTest/Data/CodingTheory/ReedSolomon/PowerAgreement.lean` | Any finite `ι`; one exceptional set with bound `ℓ * (card ι).choose 2 / max (A - 1) 1` for every `A`, replacing the `if` on `A = 1`; the list bound holds for any received word; the constant-agreement lemma is an iff. |
| [P9 slice 20: ordered simplex, largest-coordinate law and rate-partition moments (#1002)](https://github.com/Verified-zkEVM/ArkLib/pull/1002) | `HiddenDerivative/Parameters/RatePartition/{OrderedSimplex,Moment}.lean` | `ArkLib.ToMathlib.Analysis.Simplex.{OrderedSimplex,MaxCoordinate}`, `ArkLib.ToMathlib.NumberTheory.Harmonic.Thresholds`, `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.RatePartition.Moment`; dilation lemmas in `ToMathlib.Analysis.Simplex.WeightedVolume`; acceptance cases in the matching `ArkLibTest` files | `simplexMaximum` is `univ.sup'` and its expectations are set averages; dilation for any weights; tails for every `t`, `a` and `d`; harmonic thresholds `180` and `203` in place of `200` and `500`. Not ported: the unused `momentErrorPolynomial`/`momentErrorRatio` variant. The harmonic bounds are in `Thresholds.lean` to avoid a file collision with #970's `Harmonic/Bounds.lean`. |
| [Deferred-name follow-up 1: agreement thresholds, singleton power agreement, `degreeX` of a derivative (#1001)](https://github.com/Verified-zkEVM/ArkLib/pull/1001) | `ReedSolomon/AgreementThreshold.lean`; `uniformExactPowerAgreement_singleton` of `MutualCorrelatedAgreement/UniformPowerAgreement.lean`; `degreeX_derivative_le` of `ToMathlib/Polynomial/SeparableResultant.lean` | `ArkLib.Data.CodingTheory.ReedSolomon.AgreementThreshold`; lemmas in `ListDecodability.AgreementRadius`, `ReedSolomon.PowerAgreement` and `Data.Polynomial.ResultantDegree`; acceptance cases in the matching `ArkLibTest` files | Source names kept, with a finite coordinate type in place of `Fin n`. The PR description records an audit of the other names flagged as missing in 13 ported modules: all are on main under other names or were dropped on purpose, except `ratePartitionRankBound_le_exponential` and `_le_power_sum`, whose subject is only in #973/#974/#977. |
| [P3 slice 13: symbolic interpolation for received lines and curves (#1000)](https://github.com/Verified-zkEVM/ArkLib/pull/1000) | `HiddenDerivative/Interpolation/Symbolic/{ReceivedLine,ReceivedCurve,ColumnHeight}.lean`, the first theorem of `Symbolic/CurveColumnHeight.lean` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Symbolic.{SourceColumn,ChallengeDegree,ConstraintMatrix,ReceivedCurve,ColumnHeight}`, `ArkLib.Data.MvPolynomial.JointDegree`, `ArkLib.ToMathlib.LinearAlgebra.Matrix.SupportedRows`; acceptance cases in the matching `ArkLibTest` files | One curve API covers lines; `CoeffDegreeLE`/`GradedCoeffDegreeLE` are cases of `restrictJointDegree`; the rank hypothesis is on the full matrix after any injective hom into a field; nonvanishing under every ring hom into a nontrivial semiring. Deferred: `Soundness.lean` (needs `WeightedSupport.Basic` and `Global.Multiplicity` from #964/#976/#979/#985) and `exists_primitive_interpolant_of_shifted_height`. |
| [P8 slice 1: power-batched curve agreement, tuple specialization, extension descent and singular tail (#999)](https://github.com/Verified-zkEVM/ArkLib/pull/999) | `MutualCorrelatedAgreement/PolynomialCurve/{Agreement,Specialization,ExtensionDescent}.lean`, the `_map` lemmas of `PolynomialCurve/FullAgreement.lean`, `MutualCorrelatedAgreement/FirstOrder/Squarefree/{Bounds,SingularTail}.lean`, `ToMathlib/Polynomial/SimultaneousRoots.lean` | `ArkLib.Data.CodingTheory.ReedSolomon.PowerAgreement` (extended), `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.{TupleSpecialization,SingularTail}`, `polynomialAgreementSet_map` in `ReedSolomon.Agreement`, `exists_card_le_forall_eval_eq_zero_iff` in `Data.Polynomial.SpecializationAvoidance`; acceptance cases in the matching `ArkLibTest` files | Main's finite `ι` in place of `Fin n`; the simultaneous-roots lemma over any domain and index type; interpolation takes `≤` sample counts; the singular-tail bounds drop `0 < r`, `r ≤ j`, `A.natDegree = r` and the `IsDomain` assumptions. The source's `[DKTZ26]` citation is not in `references.bib`. |
| [P7b slice 1: rounded first-order source and rank counts (#998)](https://github.com/Verified-zkEVM/ArkLib/pull/998) | `HiddenDerivative/Parameters/FirstOrder/AutomaticRecipe.lean` (the recipe-independent counts and rounding estimates) | `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.FirstOrder.RoundedCounts`, `ArkLib.ToMathlib.BigOperators.Intervals`, `weightedHigherJetCount_of_le_one` in `Interpolation.Counting`; acceptance cases in the matching `ArkLibTest` files | The counts take the rate, agreement, multiplicity and caps as parameters; `0 < m` and `0 < n` dropped; the residual comparison takes `D ≤ R n`. Deferred: the recipe itself (the threshold- and `β`-dependent definitions and theorems), which specializes these counts at #988's `firstOrderRateThreshold` and `firstOrderRateBeta`. |
| [P9 slice 19: rate-partition block lengths, finite ratio and closed multiplicity (#997)](https://github.com/Verified-zkEVM/ArkLib/pull/997) | `HiddenDerivative/Parameters/RatePartition/{BlockLength,FiniteRatio,Convergence,FiniteParameters,RoundingLoss,ClosedRatio,Rounding300,UniformParameters}.lean` | `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.RatePartition.{BlockLength,FiniteRatio,ClosedMultiplicity,UniformParameters}`, `ArkLib.ToMathlib.Algebra.Order.Floor.RelativeError`; acceptance cases in the matching `ArkLibTest` files | The two closed multiplicities are one `closedMultiplicity C d`; several guards drop or weaken hypotheses (`0 < d`, `0 < m`, `δ < 6/25`). Deferred: `Asymptotics.lean` (mostly #988's gate; the rest stacks on #988's `rateGamma`), `SupportGuards.lean` and `uniformRatePartition_totalJetDegree_le` (need `RatePartitionEligible`, #977), and `partitionWeightBudget_level_le`. |
| [P7a slice 5: rational Taylor numerators over algebras and their joint degree (#996)](https://github.com/Verified-zkEVM/ArkLib/pull/996) | `rationalTaylorNumeratorOver`, `commonTaylorNumeratorOver` and the coefficient-map lemmas from `HiddenDerivative/RootFinding/Symbolic/TaylorNumerator.lean`; `ChallengeHeightLE` and its lemmas from `Symbolic/TaylorHeight.lean`; `flattenChallenge`, `jointTotalDegree` and the numerator joint-degree bounds from `Symbolic/TaylorDegree.lean` | `ArkLib.ToMathlib.MvPolynomial.PolynomialCoefficients`, `ArkLib.Data.Polynomial.Differential.RationalTaylorAlgebra`, `ArkLib.Data.Polynomial.Differential.RationalTaylorJointDegree`; map lemmas in `TaylorResidual` and `RationalTaylor`; `totalDegree_clearedSubstitution_le_of_coeff` in `ClearedSubstitution`; acceptance cases in the matching `ArkLibTest` files | `ChallengeHeightLE` is `CoeffNatDegreeLE` and `flattenChallenge` is `(optionEquivRight R σ).symm`, both over a `CommSemiring`. The exponent `τ` is explicit and `0 < v` is dropped. `initialJetSeparantOver` is main's `initialJetSeparant`. The bridge from `commonTaylorNumeratorOver` to the field-level `commonTaylorNumerator` of #995 is left for after both land. |
| [P7a slice 4: rational Taylor chart, cuts and chart geometry (#995)](https://github.com/Verified-zkEVM/ArkLib/pull/995) | `initialJetEquation`, `commonTaylorNumerator`, `rationalTaylorMap`, `rationalTaylorPolynomial`, `taylorAgreementEquation` and their lemmas from `HiddenDerivative/RootFinding/Taylor/{Chart,Cuts}.lean`; `highTaylorCutsIdeal`, `regularAgreementCutLocus`, `initialJetPrimeFamily` and the regular-solution lemmas from `HiddenDerivative/RootFinding/Geometry/{AgreementGeometry,SolutionGeometry,InitialGeometry}.lean`; `rationalTaylorCutDegreeBound` from `Geometry/HighCutGeometry.lean` | `ArkLib.Data.Polynomial.Differential.TaylorChart`, `ArkLib.Data.Polynomial.Differential.TaylorChartGeometry`; acceptance cases in the matching `ArkLibTest` files | The exponent `τ` is explicit; each `2K`/`_of_exponent` pair is one theorem under `TaylorExponentSufficient`. `exists_forall_jetEvaluation_ne_zero` holds over infinite domains for any differential polynomial. Deferred: the Hilbert-degree part of `InitialGeometry` and the rest of `HighCutGeometry` (need P5 slices 12–14), and `SolutionExtension`, `SolutionEmbedding` (need the coefficient-map lemmas for differential specialization). |
| [P6 slice 1: Frobenius pullbacks, root contraction, Taylor index weights (#994)](https://github.com/Verified-zkEVM/ArkLib/pull/994) | `variablePowerSubstitution`, `inverseFrobeniusTwist` and their lemmas from `ToMathlib/MvPolynomial/FrobeniusPullback{,Derivative}.lean`; `rootExpansion`, `rootContraction`, `degreeOf_rootContraction_none_mul` from `ToMathlib/MvPolynomial/RootContraction.lean`; `exists_frobeniusContraction` and its irreducible and fraction-ring forms from `ToMathlib/Polynomial/FrobeniusContraction{,FractionRing}.lean`; `indexWeight` and the universal Taylor support-weight lemmas from `HiddenDerivative/RootFinding/Taylor/IndexWeight.lean` | `ArkLib.ToMathlib.MvPolynomial.FrobeniusPullback`, `ArkLib.ToMathlib.MvPolynomial.RootContraction`, `ArkLib.Data.Polynomial.FrobeniusContraction`, `ArkLib.Data.Polynomial.Differential.TaylorIndexWeight`; acceptance cases in the matching `ArkLibTest` files | Hypotheses weakened to `CommSemiring` + `ExpChar` + `PerfectRing`, `NoZeroDivisors`, and no primality of `p`. Not ported: `ToMathlib/Finset/SumRangeFrom.lean` (Mathlib's `Finset.sum_Ico_eq_sum_range`, `Finset.sum_range_add`) and `ToMathlib/Polynomial/PaddedDerivativeResultant{,CommonRoot}.lean` (main's `resultant_derivative_ne_zero_of_separable_map_fractionField`, `map_resultant_eq_zero_of_common_root`). |
| [P6 slice 7: Frobenius contraction in one variable of a multivariate polynomial (#1011)](https://github.com/Verified-zkEVM/ArkLib/pull/1011) | `ToMathlib/MvPolynomial/FrobeniusFactor.lean` | `ArkLib.Data.MvPolynomial.FrobeniusContraction`, `Polynomial.exists_irreducible_frobeniusContraction_expChar` in `ArkLib.Data.Polynomial.FrobeniusContraction`; a concrete two-variable characteristic-two contraction and a fraction-ring application are retained in `ArkLibTest/Data/MvPolynomial.lean`; polynomial acceptance cases remain in `ArkLibTest/Data/Polynomial/FrobeniusContraction.lean` | Coefficients in a ring without zero divisors (a UFD for the fraction-field part) in place of a field; `CharP` for any `p` and `ExpChar` in place of `Fact p.Prime` and the hand split; the fraction-field parts separated from the contraction; the twist-preservation conjunctions are not ported, while their component inverse-twist results remain in P6 slice 1. |
| [P6 slice 7: Frobenius contraction in one variable of a multivariate polynomial (#1011)](https://github.com/Verified-zkEVM/ArkLib/pull/1011) | `ToMathlib/MvPolynomial/FrobeniusFactor.lean` | `ArkLib.Data.MvPolynomial.FrobeniusContraction`, `Polynomial.exists_irreducible_frobeniusContraction_expChar` in `ArkLib.Data.Polynomial.FrobeniusContraction`; acceptance cases in `ArkLibTest/Data/MvPolynomial/FrobeniusContraction.lean` and `ArkLibTest/Data/Polynomial.lean` | Coefficients in a ring without zero divisors (a UFD for the fraction-field part) in place of a field; `CharP` for any `p` and `ExpChar` in place of `Fact p.Prime` and the hand split; the fraction-field parts separated from the contraction; the twist-preservation lemmas are conjunctions of #994 lemmas (derived in the test). |
| [P6 slice 8: Frobenius pullbacks, root contraction, Taylor index weights (#994)](https://github.com/Verified-zkEVM/ArkLib/pull/994) | `variablePowerSubstitution`, `inverseFrobeniusTwist` and their lemmas from `ToMathlib/MvPolynomial/FrobeniusPullback{,Derivative}.lean`; `rootExpansion`, `rootContraction`, `degreeOf_rootContraction_none_mul` from `ToMathlib/MvPolynomial/RootContraction.lean`; `exists_frobeniusContraction` and its irreducible and fraction-ring forms from `ToMathlib/Polynomial/FrobeniusContraction{,FractionRing}.lean`; `indexWeight` and the universal Taylor support-weight lemmas from `HiddenDerivative/RootFinding/Taylor/IndexWeight.lean` | `ArkLib.ToMathlib.MvPolynomial.FrobeniusPullback`, `ArkLib.ToMathlib.MvPolynomial.RootContraction`, `ArkLib.Data.Polynomial.FrobeniusContraction`, `ArkLib.Data.Polynomial.Differential.TaylorIndexWeight`; acceptance cases in the matching `ArkLibTest` files | Hypotheses weakened to `CommSemiring` + `ExpChar` + `PerfectRing`, `NoZeroDivisors`, and no primality of `p`. Not ported: `ToMathlib/Finset/SumRangeFrom.lean` (Mathlib's `Finset.sum_Ico_eq_sum_range`, `Finset.sum_range_add`) and `ToMathlib/Polynomial/PaddedDerivativeResultant{,CommonRoot}.lean` (main's `resultant_derivative_ne_zero_of_separable_map_fractionField`, `map_resultant_eq_zero_of_common_root`). |
| [P7a slice 7: high-cut Taylor chart incidence (#1016)](https://github.com/Verified-zkEVM/ArkLib/pull/1016) | Hilbert-degree part of `HiddenDerivative/RootFinding/Geometry/InitialGeometry.lean`; `Geometry/HighCutGeometry.lean` | `ArkLib.Data.Polynomial.Differential.TaylorChartIncidence`, three theorems in `ArkLib.Data.Polynomial.Differential.TaylorChartGeometry`; acceptance cases in the matching `ArkLibTest` files | The incidence bound is main's hypersurface agreement bound with threshold `k`; `A ≤ n` weakened to `A - k + 1 ≤ #ι`; `0 < k`, `0 < v` and the nonzero hypotheses dropped; the exponent `τ` is explicit. |
| [P5 slice 20: first-order stage charges, hybrid constants and capped mixed volumes (#1017)](https://github.com/Verified-zkEVM/ArkLib/pull/1017) | `HiddenDerivative/Parameters/FirstOrder/{StageCharges,StageComparison,HybridConstants,HybridRateEnvelope}.lean`; `hybridTau` … `hybridEClosed` renamed to `regularTaylorExponent`, `stageStaircase`, `firstOrderListConstant`, `firstOrderExceptionConstant` and the rest; `curveStageZero`/`curveStageOne` renamed to `orderZeroCurveStageCharge`/`orderOneCurveStageCharge` | `cappedDegreeMixedVolume` (from `fixedFiberDerivativeImageDegree`) and `cappedDegreeExponentsFinTwoEquiv` in `ToMathlib/RingTheory/MvPolynomial/CappedDegree`; `cappedBidegreeMixedVolume` (from `mixedDerivativeImageDegree`) in `CappedBidegree`; the #1005 curve and hypersurface affine-degree bounds restated through them; `TwoJetPoints` is the `MonomialMap` API; acceptance cases in the matching `ArkLibTest` files | Mixed-volume names; `r ≤ j` dropped from `_mono_total`, `1 ≤ μ` from the tail and exception bounds, `c ≤ b` from the capped-bidegree affine-degree bound, `A ≤ n` weakened to `D < n` in the retained-ratio bound; unused `K` arguments removed; envelopes take no staircase-nonnegativity hypothesis. |
| [P7a slice 8: equation-restricted correlated agreement descent (#1019)](https://github.com/Verified-zkEVM/ArkLib/pull/1019) | `HiddenDerivative/RootFinding/Symbolic/TaylorWitnessEmbedding.lean`: `challengeSpecialization`; `HiddenDerivative/RootFinding/MutualCorrelatedAgreement/EquationDescent.lean`: `challengeSpecialization_map_coefficients`, `ChallengeHeightLE.map_coefficients` → `MvPolynomial.CoeffNatDegreeLE.map_coefficients`, `map_symbolicDifferentialSpecialization`, `exists_exceptional_equation_correlatedAgreement_descend`; `HiddenDerivative/RootFinding/MutualCorrelatedAgreement/Symbolic/RegularEquation.lean`: `HasExactCorrelatedPair`; `HiddenDerivative/RootFinding/MutualCorrelatedAgreement/ExtensionDescent.lean`: `HasExactCorrelatedPair.descend` | `ArkLib.Data.Polynomial.Differential.BaseChange`; `ArkLib.ToMathlib.MvPolynomial.PolynomialCoefficients`; `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.ExtensionDescent` and `.EquationDescent`; acceptance cases in the matching `ArkLibTest` files | Generalized challenge specialization to arbitrary multivariate variables over commutative semirings, coefficient maps to coefficient-polynomial rings, and differential specialization to commutative semirings; mapped evaluation domain expressed as an embedding composition; agreement descent preserves the exceptional-set size bound. `ChallengeHeightLE` uses existing `MvPolynomial.CoeffNatDegreeLE`; the existing `polynomialAgreementSet_map` is retained in `Agreement.lean`; standalone exceptional correlated-agreement wrapper not added because the equation theorem performs the exceptional-set pullback directly. |
| [P7a slice 10: regular solution geometry across coefficient extensions (#1021)](https://github.com/Verified-zkEVM/ArkLib/pull/1021) | `HiddenDerivative/RootFinding/Geometry/SolutionExtension.lean`: `mapped_regular_solution_family` → `map_regularSolutionFamily`, `exists_common_regular_center_extension` → `exists_forall_jetEvaluation_ne_zero_map`, `mapped_agreement_count` → `card_polynomialAgreement_map` | `map_differentialSpecialization_ne_zero_iff` and `map_regularSolutionFamily` in `ArkLib.Data.Polynomial.Differential.BaseChange`; `exists_forall_jetEvaluation_ne_zero_map` in `ArkLib.Data.Polynomial.Differential.TaylorChartBaseChange`; `card_polynomialAgreement_map` in `ArkLib.Data.CodingTheory.ReedSolomon.Agreement`; acceptance cases in the matching `ArkLibTest` files | Generalized family transport from fields to commutative semirings with explicit injective maps and from the last separant to any jet coordinate; common-center transport to an infinite domain target; agreement counts to finite indexed domains with repeated points. `map_differentialSpecialization_ne_zero_iff` is new. `map_separant`, `card_image_polynomial_map`, `map_binomial_pivots`, and the algebraic-closure specialization wrapper were not ported because existing APIs or the generic theorem suffice; acceptance tests derive these forms. |
| [P7a slice 9: Taylor numerator specialization and jet bounds (#1020)](https://github.com/Verified-zkEVM/ArkLib/pull/1020) | `HiddenDerivative/RootFinding/Symbolic/CoefficientExtension.lean`: `specialize_extendSymbolicCoefficients` → `eval_map_coefficients`, `jetWeight_extendSymbolicCoefficients_le` → `jetTotalDegree_map_le`; `HiddenDerivative/RootFinding/Symbolic/TaylorSpecialization.lean`: `map_rationalTaylorNumeratorOver_eq`, `map_commonTaylorNumeratorOver_eq` | `ArkLib.ToMathlib.MvPolynomial.PolynomialCoefficients`; `ArkLib.Data.Polynomial.Differential.BaseChange`; `.RationalTaylorAlgebra`; `.TaylorChart`; acceptance cases in the matching `ArkLibTest` files | Generalized coefficient evaluation to arbitrary commutative semirings, variable types, coefficient maps, and evaluation points; generalized total jet-degree monotonicity to any ring homomorphism between commutative semirings, including noninjective maps; generalized common-numerator mapping to a natural Taylor index and arbitrary exponent; polynomial-evaluation specializations use the general field-valued map theorems. `commonTaylorNumeratorOver_eq` is a new bridge; `extendSymbolicCoefficients` is omitted as an alias for `MvPolynomial.map`, while challenge-height and separant mapping are covered by existing lemmas. |
| [P3 slice 14: symbolic interpolation on weighted support (#1025)](https://github.com/Verified-zkEVM/ArkLib/pull/1025) | `HiddenDerivative/Interpolation/Symbolic/WeightedSupport.lean`: `weightedSupportColumnIndex`, `weightedSupportColumns`, `weightedSupportColumns_exponent`, `weightedSupportColumns_injective`, `weightedSupportColumns_eligible`, `receivedLine_block_eq_canonical_submatrix`, `receivedLine_block_rank_le_base_actual`, `receivedLine_matrix_rank_le_base_actual`, `y₀_le_two_mul_sub_one_of_eligible`, `noBand_kernel_height_lt` → `kernel_height_lt_twelve_mul_of_margin`, `exists_symbolic_weightedSupport_interpolant_of_fixed_margin`, `exists_constant_interpolant_of_zero_rank`, `SourceColumn.ofExponent`, `SourceColumn.exponent_ofExponent` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Symbolic.SourceColumn`; `.Symbolic.WeightedSupport`; acceptance cases in the matching `ArkLibTest` files | `SourceColumn.ofExponent` and `SourceColumn.exponent_ofExponent` keep their names and move to the existing `SourceColumn` owner; `noBand_kernel_height_lt` is renamed to describe its margin hypothesis and height bound. The received-line block proof uses the current support API and local-constraint operator; the matrix rank bound uses existing `LinearMap.finrank_range_pi_le_sum`. The private mapping lemma is generalized to `map_localConstraintCoordinatesAt`, which handles every differential polynomial. No generic `Matrix.rowBlock` or `Matrix.rank_prod_rows_le_sum` API is added; `WeightedSupportIndex` is replaced by the existing eligible-exponent subtype; `to` is overview prose, not a declaration.`
| [P9 slice 21: shifted finite-sum chunk lemmas (#1024)](https://github.com/Verified-zkEVM/ArkLib/pull/1024) | `ToMathlib/Finset/SumRangeFrom.lean`: shifted sums and adjacent chunk splitting | Mathlib's `Finset.sum_Ico_eq_sum_range` and `Finset.sum_range_add` | No ArkLib sum wrappers are retained: Mathlib supplies the interval form and chunk split. The four- and two-chunk acceptance cases were removed because they only compose the split theorem; there were no production consumers. |
| [P7b slice 3: first-order symbolic matrix rank bound (#1023)](https://github.com/Verified-zkEVM/ArkLib/pull/1023) | `HiddenDerivative/Interpolation/Symbolic/Soundness.lean`: `map_projectLowContact`, `map_unscaledLocalSubstitution`; `HiddenDerivative/Interpolation/FirstOrder/SymbolicRank.lean`: `map_localConstraintAt`, `firstOrder_symbolic_matrix_rank_le` → `rank_firstOrderLocalConstraintMatrix_le`; `HiddenDerivative/Interpolation/Symbolic/ReceivedLine.lean`: `matrix_entry_eq_coeff_localConstraintAt` → `localConstraintMatrix_apply_eq_localConstraintAt_coeff` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Local.ConstraintMap`; `.Symbolic.ConstraintMatrix`; `.FirstOrder.SymbolicRank`; acceptance cases in the matching `ArkLibTest` files | Generalized the matrix-entry formula from `Fin` indices and a field to arbitrary index types and commutative rings, and the rank bound to arbitrary finite point and column types; removed the `1 < D` assumption. Coefficient-change lemmas are stated over commutative rings; `map_unscaledLocalSubstitution` moved from `Symbolic.Soundness` to its local substitution owner and was already public on main. No source declarations were left out. |
| [P5 slice 21: bidegree hypersurface incidence bounds and products (#1022)](https://github.com/Verified-zkEVM/ArkLib/pull/1022) | `ArkLib/ToMathlib/AlgebraicGeometry/Incidence/ProductBounds.lean`: `dimensionSensitiveIncidenceProduct_mono_dimension`, `dimensionSensitiveIncidenceProduct_le_first_pow`, `hybridDimensionSensitiveIncidenceProduct_eq_factor_mul`, `hybridDimensionSensitiveIncidenceProduct_min_le`; `ArkLib/ToMathlib/AlgebraicGeometry/Incidence/BidegreeExcluded.lean`: `bidegreeHypersurface_source_incidence_off_excluded_sharp`, `bidegreeHypersurface_source_incidence_off_excluded_hybrid`, `bidegreeHypersurface_source_incidence_off_excluded_hybrid_two`, `bidegreeHypersurface_source_incidence_off_excluded_sharp_one`, `bidegreeHypersurface_source_incidence_off_excluded_sharp_two` | `ArkLib.ToMathlib.Combinatorics.Enumerative.IncidenceProduct`; `ArkLib.ToMathlib.RingTheory.Nullstellensatz.BidegreeIncidence`; acceptance cases in the matching `ArkLibTest` files | Generalized product monotonicity from pairwise comparison to `Monotone`, and hybrid factorization from `s ≤ k` to `s ≤ k + 1`; sharp bidegree bounds drop `A ≤ n`. Renamed the five `AffineHilbert.bidegreeHypersurface_source_incidence_off_excluded_*` declarations into `MvPolynomial.bidegreeHypersurface_incidence_off_excluded_*`; other declarations keep their names. All nine public declarations are represented; nothing was deferred or left unported. |
| [P7a slice 11: ordinary root presentations and separant bounds (#1026)](https://github.com/Verified-zkEVM/ArkLib/pull/1026) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/Ordinary/Factors/RootPresentation.lean`: `ordinaryRootPresentation`, `natDegree_ordinaryRootPresentation`, `ordinaryRootPresentation_ne_zero`, `irreducible_ordinaryRootPresentation`, `derivative_ordinaryRootPresentation`, `separableResultant_ordinaryRootPresentation_ne_zero` → `resultant_derivative_ordinaryRootPresentation_ne_zero`, `ordinaryRootPresentation_monomial`, `degreeX_ordinaryRootPresentation_le`, `eval_ordinaryRootPresentation`, `ordinary_separant_ne_zero_of_resultant_eval_ne_zero`, `exists_exceptional_ordinary_separant` | `ArkLib.Data.Polynomial.Differential.RootPresentation`; acceptance cases in the matching `ArkLibTest` files | Moved the API to the generic `PolynomialDifferential` owner. Generalized the resultant theorem to the padded derivative resultant, without a separate positive-degree assumption; challenge height uses `MvPolynomial.CoeffNatDegreeLE`. The source separable-resultant declaration is represented by the renamed theorem; nothing was deferred or left unported, and no duplicate wrapper was added. |
| [P6 slice 9: ordinary Frobenius equation contraction (#1029)](https://github.com/Verified-zkEVM/ArkLib/pull/1029) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/Ordinary/Frobenius/Equation.lean`: `flatVariableEquiv`, `rootFirstEquiv`, `baseVariableEquiv`, `baseFlatten`, `ordinaryFlatten`, `ordinaryUnflatten`, `ordinaryFlatten_C`, `ordinaryFlatten_X`, `ordinaryFlatten_Y` → `ordinaryFlatten_root`, `ordinaryFlatten_coeff_X`, `ordinaryFlatten_C_monomial`, `ordinaryFlatten_pderiv_C`, `eval_ordinaryFlatten`, `eval₂_ordinaryFlatten`, `eval_ordinaryUnflatten`, `eval₂_ordinaryUnflatten`, `ordinaryFlatten_pderiv_root`, `ordinaryUnflatten_pderiv_root`, `rootPolynomial_ordinaryFlatten`, `ordinaryUnflatten_monomial`, `degreeOf_none_ordinaryFlatten`, `degreeOf_challenge_ordinaryFlatten_C_le`, `ordinaryFlatten_monomial`, `degreeOf_challenge_ordinaryFlatten_le`, `degreeOf_independent_ordinaryFlatten_C`, `degreeOf_independent_ordinaryFlatten_le`, `degreeOf_independent_ordinaryUnflatten_le`, `degreeOf_some_zero_ordinaryFlatten`, `challengeHeightLE_ordinaryUnflatten_monomial` → `coeffNatDegreeLE_ordinaryUnflatten_monomial`, `challengeHeightLE_ordinaryUnflatten_of_degreeOf_le` → `coeffNatDegreeLE_ordinaryUnflatten_of_degreeOf_le`, `differentialSpecialization_map_eq_eval₂_flatten`, `eval_differentialSpecialization_map_eq_flatten`, `expand_differentialSpecialization_map_eq_eval₂_flatten`, `frobeniusSpecialization_pow`, `frobeniusSpecialization_eq_zero`, `exists_frobeniusEquation`, `map_rootExpansion` → `MvPolynomial.map_rootExpansion`, `eval₂_rootExpansion` → `MvPolynomial.eval₂_rootExpansion`; `ArkLib/ToMathlib/MvPolynomial/PolynomialCoefficients.lean`: `MvPolynomial.map_optionEquivLeft` (relocated to `RootContraction` with its name and statement preserved) | `ArkLib.Data.Polynomial.Differential.FrobeniusEquation`; `ArkLib.ToMathlib.MvPolynomial.RootContraction`; acceptance cases in the matching `ArkLibTest` files | Generalized flattening, evaluation, degree transport, and specialization APIs from fields to commutative semirings; generalized Frobenius specialization and contraction from perfect fields to commutative rings with `ExpChar` and `PerfectRing`, with the stated nontriviality and no-zero-divisor assumptions; replaced `ChallengeHeightLE` with `MvPolynomial.CoeffNatDegreeLE`; generalized root-expansion map and evaluation laws to commutative semirings and moved them to `MvPolynomial`. `ordinaryFlatten_Y` is renamed `ordinaryFlatten_root`. `ordinaryFlatCases_one` remains a private proof helper, `frobeniusEquation_nonconstant_coefficient_canary` is an acceptance example, and the two source `ChallengeHeightLE` lemmas are represented by the renamed `CoeffNatDegreeLE` lemmas. No declaration was blocked or left without a proof. |
| [P7a slice 13: native probability API in uniform tests (#1031)](https://github.com/Verified-zkEVM/ArkLib/pull/1031) | `ArkLib/Data/Probability/Notation.lean`: `Pr_eq_tsum_indicator` (retired; not ported); source uniform-sample event calculation ↔ `SampleableType.prEvent_uniformSample`; `Pr_uniform_equiv` ↔ `SampleableType.prEvent_uniformSample_equiv` | `ArkLibTest.Data.Probability.Uniform`; acceptance cases in the matching `ArkLibTest` files | No new ArkLib public declarations. The PMF event-sum formula is covered by VCVio's `probOutput_true_eq_probEvent` and `probEvent_eq_tsum_indicator` (or finite variants), with no ArkLib wrapper; the acceptance case states the corresponding native-measure formula. The three-sample PMF computation is not added, and `$ᵖ` plus `Pr_{…}[…]` remain retired syntax. |
| [P6 slice 10: padded derivative resultant boundary cases (#1030)](https://github.com/Verified-zkEVM/ArkLib/pull/1030) | `ArkLib/ToMathlib/Polynomial/PaddedDerivativeResultant.lean`: `Polynomial.paddedDerivativeResultant`, `Polynomial.paddedDerivativeResultant_ne_zero_of_map_separable` | `ArkLib.Data.Polynomial.ResultantDegree`; `ArkLib.Data.Polynomial.ResultantSpecialization`; `ArkLib.Data.Polynomial.FractionFieldResultant`; acceptance cases in the matching `ArkLibTest` files | The wrapper is represented by `resultant` and `resultant_comm_sub_one`; the nonvanishing theorem is covered by `Polynomial.resultant_derivative_ne_zero_of_separable_map_fractionField`, generalized to the polynomial's actual degree with no separate degree parameter. The source-shaped fixed-degree statement is derived in the acceptance file using the exact-degree hypothesis. No production declarations were added; the boundary case for an incorrect declared degree is covered by the new acceptance example. |
| [P7b slice 4: finite first-order rate parameters (#1028)](https://github.com/Verified-zkEVM/ArkLib/pull/1028) | `ArkLib/Data/CodingTheory/HiddenDerivative/Parameters/FirstOrder/FiniteRateParameters.lean`: `firstOrderRateDerivativeCap`, `firstOrderRateJetDegree`, `FirstOrderFiniteRateTest`, `firstOrderNormalizedSourceCount`, `firstOrderNormalizedRankCount`, `firstOrderRateChallengeDegree`, `FirstOrderFiniteRateParameters`, `FirstOrderFiniteRateParameters.derivativeCap`, `.jetDegree`, `.rankCount`, `.sourceCount`, `.challengeDegree`, `.sourceCount_gt_rankCount`, `exists_firstOrderFiniteRateParameters_of_tendsto`, `exists_firstOrderFiniteRateParameters_of_rate_limits`, `firstOrderRationalSourceCount`, `FirstOrderRationalFiniteTest`, and its `Decidable` instance; `ArkLib/Data/CodingTheory/HiddenDerivative/Parameters/FirstOrder/RoundedCounts.lean`: `firstOrderRateSourceCount_le_dimensionCount` → `firstOrderSourceCount_mul_le_firstOrderDimensionCount` | `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.FirstOrder.FiniteRateParameters`; `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.FirstOrder.RoundedCounts`; acceptance cases in the matching `ArkLibTest` files | Generalized the source-count dimension bound to the generic count API. Renamed `firstOrderRateSourceCount_le_dimensionCount` to `firstOrderSourceCount_mul_le_firstOrderDimensionCount`. The rate-specific source and rank counts are covered by existing generic counts; cubic rank bounds and the certified rank bound are covered by existing, stronger general declarations. The scaled kernel-height bound follows from `scaledKernelHeight_le_floor`; the source-shaped max-one bound is derived in the acceptance test. |
| [P7a slice 12: symbolic Taylor chart agreement cuts (#1027)](https://github.com/Verified-zkEVM/ArkLib/pull/1027) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/RootFinding/Symbolic/TaylorCuts.lean`: `map_initialJetEquationOver`, `map_initialJetEquationOver_eq` → `map_initialJetEquation`; `aeval_map_initialJetEquationOver` → `aeval_map_initialJetEquation`; `taylorAgreementEquationOver`, `map_taylorAgreementEquationOver`, `map_taylorAgreementEquationOver_eq`, `aeval_map_taylorAgreementEquationOver_of_exponent`, `aeval_map_taylorAgreementEquationOver_eq_zero_iff_of_exponent`, `degree_rationalTaylorPolynomial_lt_of_symbolic_high_cuts_and_exponent`, `aeval_map_commonTaylorNumeratorOver_reconstruction_of_exponent` | `ArkLib.Data.Polynomial.Differential.TaylorChart`; `ArkLib.Data.Polynomial.Differential.TaylorChartAlgebra`; acceptance cases in the matching `ArkLibTest` files | Generalized initial-equation mapping from commutative rings to commutative semirings and arbitrary ring maps, and its evaluation theorem to maps between commutative semirings. The agreement equation uses the destination common-exponent API; agreement, degree-bound, reconstruction, and joint-degree results keep their general exponent-aware statements. Default-exponent twins were removed, so callers provide `τ` and its sufficiency proof. `initialJetEquationOver` is covered by the existing, more general `initialJetEquation`; `map_initialJetSeparantOver_eq` by `map_initialJetSeparant`; common-numerator field-specialization theorems were already in `TaylorChart`, so duplicate copies were removed. |
| [P9 slice 23: fixed-rate gates and least finite partition multiplicity (#1035)](https://github.com/Verified-zkEVM/ArkLib/pull/1035) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Parameters/RatePartition/Gate.lean`: `ratePartitionExponent_eq` → `fixedRateCoefficient_eq`, `ratePartitionGamma_gt_one_of_log_bound` → `rateGamma_gt_one_of_log_bound`, `ratePartitionGamma_gt_one_of_exponent_margin` → `rateGamma_gt_one_of_exponent_margin`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Parameters/RatePartition/Recipe.lean`: `exists_partitionFiniteParameters_of_rateGamma_gt_one`, `rateMultiplicity`, `rateMultiplicity_spec`, `rateMultiplicity_minimal`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/PartitionSupport/RateBound.lean`: `ratePartition_totalJetDegree_le` → `partitionSupport_totalJetDegree_le_rateJetCap` | `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.RatePartition.Gate`; `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.RatePartition.Recipe`; `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.PartitionSupport.RateBound`; acceptance cases in the matching `ArkLibTest` files | Renamed the fixed-rate coefficient and limiting-ratio gate declarations as shown; `exists_partitionFiniteParameters_of_rateGamma_gt_one` bridges the strict limiting gate to finite-ratio existence. The recipe selects the least multiplicity accepted by `partitionFiniteRatio`; the support bound uses the renamed eligibility predicate and degree cap. `log_ratePartitionGamma`, `ratePartitionExponent`, and `ratePartitionExponent_pos` are covered by the existing, more general `log_rateGamma`, `fixedRateCoefficient`, and `fixedRateCoefficient_pos`; `ratePartition_fixedRate_eventually` is covered by `exists_small_gap_rate_gate`, so those were not ported separately. |
| [P7a slice 14: partition-support area slots (#1036)](https://github.com/Verified-zkEVM/ArkLib/pull/1036) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/RatePartition/Area.lean`: `RatePartitionSlot` → `PartitionSupportAreaSlot`, `ratePartitionSlotExponent` → `partitionSupportAreaSlotExponent`, `ratePartitionSlotExponent_eligible` → `partitionSupportAreaSlotExponent_eligible`, `ratePartitionSlotExponent_injective` → `partitionSupportAreaSlotExponent_injective` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.PartitionSupport.Dimension`; acceptance cases in the matching `ArkLibTest` files | The slot type now uses the destination weighted-simplex tuple and its coordinate sum; exponent mapping, eligibility, and injectivity are unchanged mathematically. `ratePartition_dimension_ge_quadratic_sum` is covered by `partitionSupport_dimension_ge_quadratic_sum_real` and `finrank_partitionSupportSpace_eq_card`; the source-shaped cardinal inequality is checked in the matching acceptance module. No other source declaration is omitted. |
| [P7b slice 5: exact first-order rank rounding (#1037)](https://github.com/Verified-zkEVM/ArkLib/pull/1037) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Parameters/FirstOrder/AllMRankRounding.lean`: `firstOrderRateRankCount_floor_le_density_add_rounding_upper` → `firstOrderRankCount_floor_le_density_add_rounding_upper`, `firstOrderRateRankCount_floor_le_density_add_rounding` → `firstOrderRankCount_floor_le_density_add_rounding`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Parameters/FirstOrder/AllMSourceRounding.lean`: `firstOrderSourceDensity_mul_cube_le_rateSourceCount` | `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.FirstOrder.RoundedCounts`; acceptance cases in the matching `ArkLibTest` files | Ported the upper-branch and uniform rounded rank-count bounds against the exact piecewise rank density, using the already-ported rank-count name. The source-density rounding result is covered by `cube_mul_sourceDensity_le_firstOrderSourceCount`, which generalizes to any `mu` above the floor cutoff and to zero multiplicity; the acceptance module derives the ceiling-cap form. |
| [P11 slice 1: correlated-agreement cutoff product bounds (#1034)](https://github.com/Verified-zkEVM/ArkLib/pull/1034) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/Capacity/ProductBounds.lean`: `correlatedProductCutoff`, `correlatedProductCutoff_bounds`, `evaluation_incidence_factor_le`, `evaluation_incidence_product_le`, `correlatedProductCutoff_jointRatio_le`, `correlatedProductCutoff_fiberFactor_le`, `correlatedProductCutoff_fiberProduct_le`, `correlatedProductCutoff_fiberProduct_lt_three` | `ArkLib.ToMathlib.Combinatorics.Enumerative.IncidenceProduct`; `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.Capacity.ProductBounds`; acceptance cases in the matching `ArkLibTest` files | Added reusable shifted-ratio and product bounds to the incidence-product API, generalized to linearly ordered fields with strict ordered-ring structure; `evaluation_incidence_factor_le` and `evaluation_incidence_product_le` use them as specializations. All Reed–Solomon declarations keep their names. No declarations were deferred or left unported. |
| [P9 slice 22: Reed–Solomon capacity-gap certificates and quarter-gap bounds (#1033)](https://github.com/Verified-zkEVM/ArkLib/pull/1033) | `ArkLib/Data/CodingTheory/ReedSolomon/ListDecodability/Capacity/Basic.lean`: `polynomialListBound`, `CapacityGapCertificate`, `CapacityGapCertificate.ofDecoderCertificate`, `PointwiseListBound`, `CapacityGapCertificate.pointwiseListBound`, `UniformPrimeFieldCapacityListBound`, `UniformPrimeFieldCapacityListBound.exists_uniform_pointwise_bound`, `QuarterGapListBound`, `WeightedSupportListBound`; `ArkLib/Data/CodingTheory/ReedSolomon/ListDecodability/Capacity/QuarterGap.lean`: `agreeingPolynomials_encard_le_one_of_half`, `agreeingPolynomials_encard_lt_blockLength_of_quarter`, `quarter_gap_list_bound`; `ArkLib/Data/CodingTheory/ReedSolomon/ListDecodability/Capacity/Radius.lean`: `closeCodewordsRel_eq_eval_image_agreeingPolynomials`, `lambda_le_of_forall_agreeingPolynomials_encard_le`, `CapacityGapCertificate.ofDecoderCertificateAndPointwiseBound` | `ArkLib.Data.CodingTheory.ReedSolomon.ListDecodability.Capacity.Basic`; `ArkLib.Data.CodingTheory.ReedSolomon.ListDecodability.Capacity.QuarterGap`; acceptance cases in the matching `ArkLibTest` files | Generalized certificates, pointwise lists, the radius-to-`Lambda` bridge, and quarter-gap counting from `Fin n` to arbitrary finite coordinate types and semiring alphabets where applicable; prime-field specifications remain over `Fin n`. All listed declarations keep their names; the three bridge and factory declarations moved from `Capacity/Radius` into `Capacity/Basic`. The weighted-support specification uses destination parameter names. No public declaration from the two source modules was omitted. `CapacityGapCertificate.ofPointwiseBound` is outside this unit; `agreeingPolynomials_eq_empty_of_card_lt` is not duplicated because pointwise emptiness follows from `DecoderCertificate.decoder_eq_empty_of_card_lt`. |
| [R9 slice 1: exact-interpolant agreement-list bounds (#1038)](https://github.com/Verified-zkEVM/ArkLib/pull/1038) | `ArkLib/Data/CodingTheory/ReedSolomon/ListDecodability/HiddenDerivativeBound.lean`: `differentialSpecialization_eq_zero_of_agreeingPolynomial`, `agreeingPolynomialsToBoundedSolution`, `agreeingPolynomialsToBoundedSolution_polynomial`, `agreeingPolynomialToBoundedSolution`, `agreeingPolynomialToBoundedSolution_polynomial`, `exists_boundedSolution_polynomial_eq`, `agreeingPolynomials_encard_le_boundedSolution_natCard`, `agreeingPolynomials_encard_le_of_boundedSolution_natCard_le`, `agreeingPolynomials_encard_le_two_mul_pow_of_exactInterpolant` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.SolutionEmbedding`; `ArkLib.Data.CodingTheory.ReedSolomon.ListDecodability.HiddenDerivativeBound`; acceptance cases in the matching `ArkLibTest` files | Added a general root-proof-based embedding and polynomial projection laws; generalized the agreement-list representative and cardinality results by removing the positive interpolation-budget assumption. The pointwise bound keeps its bound, uses jet-cast and binomial hypotheses, and derives positive budget from nonzero exact-space membership. The generic bounded-solution constructor was removed and callers construct the subtype directly. No source public declarations were omitted. |
| [R9 slice 2: first-order interpolation height counts (#1039)](https://github.com/Verified-zkEVM/ArkLib/pull/1039) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/FirstOrder/Counting.lean`: `firstOrderMonomialExponent`, `firstOrderMonomialExponent_none`, `firstOrderMonomialExponent_some_zero`, `firstOrderMonomialExponent_some_one`, `firstOrderMonomialExponent_eta`, `firstOrderExponentDimensionIndex_y₀`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/FirstOrder/HeightCounting.lean`: `firstOrderColumnSlotCount`, `firstOrderHeightSlotCount`, `firstOrderY₀Weight`, `firstOrder_y₀_le_μ`, `firstOrderColumnSlotCount_add_y₀Weight`, `firstOrderCertificateHeight`, `firstOrder_rowTotal_mul_height_lt_columnSlotCount`, `sum_firstOrderDimensionIndex_height`, `firstOrderColumnSlotCount_eq_heightSlotCount`, `firstOrder_rowTotal_mul_height_lt_heightSlotCount`, `firstOrderColumns`, `firstOrderColumns_exponent`, `firstOrderColumns_injective`, `firstOrderColumns_eligible` | `ArkLib.ToMathlib.BigOperators.LinearBudget`; `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.FirstOrder.Dimension`; `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.FirstOrder.HeightCounting`; acceptance cases in the matching `ArkLibTest` files | Added generic finite-set weight and slot-surplus results to `LinearBudget`. The first-order support-coordinate equivalence and exponent constructor are owned by `Dimension`; `firstOrderExponentDimensionIndex_y₀` → `firstOrderCoordinatesEquiv_y₀`, and `sum_firstOrderDimensionIndex_height` → `sum_firstOrderDimensionCoordinates_height`. The height-count statements keep their names; `firstOrderColumns` and its three theorems move from `ReedSolomon.HiddenDerivative.SymbolicWeightedSupportInterpolation` to `ReedSolomon.HiddenDerivative`. The dependent `FirstOrderDimensionIndex` representation is not recreated; coordinate statements use the existing finite coordinate set. No listed unit statement was omitted. |
| [P6 slice 11: separability of radical multivariate factors (#1041)](https://github.com/Verified-zkEVM/ArkLib/pull/1041) | `ArkLib/ToMathlib/MvPolynomial/OrdinaryFactorSeparable.lean`: `ordinaryRootPolynomial`, `natDegree_ordinaryRootPolynomial`, `ordinaryRootPolynomial_map_fraction_separable`, `paddedDerivativeResultant_ordinaryRootPolynomial_ne_zero` | `ArkLib.Data.MvPolynomial.RadicalSplit.Separable`; acceptance cases in the matching `ArkLibTest` files | Generalized the ordinary-root polynomial and degree theorem to UFD coefficients using `radicalPrimPart`; generalized separability to arbitrary distinguished variables, UFD coefficients, and fraction-field presentations, with the ordinary-root result as the `none` specialization. Renamed `ordinaryRootPolynomial_map_fraction_separable` to `ordinaryRootPolynomial_map_fractionRing_separable` and `paddedDerivativeResultant_ordinaryRootPolynomial_ne_zero` to `Polynomial.resultant_derivative_ne_zero_ordinaryRootPolynomial`; the latter uses the existing generic separable-map resultant theorem. Added `MvPolynomial.radicalPrimPart_map_optionEquivLeft_fractionRing_separable`. No public source declarations were omitted; private derivative and factor-degree helpers are replaced by `pderiv_ne_zero_of_natCast_ne_zero` and the radical split degree bound. |
| [P9 slice 24: partition-support integral bounds (#1040)](https://github.com/Verified-zkEVM/ArkLib/pull/1040) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/RatePartition/Integral.lean`: `ratePartition_dimension_ge_integral`, `ratePartition_dimension_ge_rate_integral`, `ratePartition_floor_integral`, `ratePartition_triangle_rate_lower` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.PartitionSupport.FloorTransfer`; `ArkLib.ToMathlib.MeasureTheory.Integral.PositivePart`; acceptance cases in the matching `ArkLibTest` files | Renamed the dimension bounds to `partitionSupport_dimension_ge_integral_on` and `partitionSupport_dimension_ge_rate_integral_on`, the floor bound to `partition_floor_square_integral`, and the scalar comparison to `max_sub_zero_sq_scaled_le`. Generalized the dimension bounds to real cutoffs and weighted-simplex subsets, with dimension expressed as `finrank`; the rate form needs no separate integrability hypotheses. Generalized the scalar comparison to linearly ordered fields and derives rate positivity from the other hypotheses. The floor theorem is specialized to the whole weighted simplex; no specialized arbitrary-subset floor theorem was added because `Finset.setIntegral_le_sum_natWeightedSimplex` already supplies that generic result. |
| [P9 slice 25: fixed-rate and uniform partition gates (#1045)](https://github.com/Verified-zkEVM/ArkLib/pull/1045) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/RatePartition/Gate.lean`: `ratePartitionGamma_factorization`, `ratePartitionGamma_ge_one_add_inv_of_factor_six`, `ratePartitionGamma_gt_one_of_factor_six`, `fixedRatePartitionOrder`, `fixedRatePartitionOrder_ge_500`, `fixedRatePartition_cutoff_eq`, `fixedRatePartitionGamma_gt_one`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/RatePartition/FixedRateGate.lean`: `fixedRatePartitionMultiplicity`, `fixedRatePartitionMultiplicity_spec`, `fixedRatePartitionFiniteParameters`, `exists_fixedRatePartitionFiniteParameters`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/RatePartition/UniformGamma.lean`: `uniformRatePartitionOrder_ge_500`, `uniformRatePartitionGamma_low_base_gt`, `uniformRatePartitionGamma_low_gt`, `uniformRatePartitionGamma_high_base_gt`, `uniformRatePartitionGamma_high_gt` | `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.RatePartition.Gate`; `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.RatePartition.FixedRateGate`; `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.RatePartition.UniformGamma`; acceptance cases in the matching `ArkLibTest` files | Added `rateGamma_eq_exponential` without a direct source declaration. Renamed the factorization, factor-six bounds, strict fixed-rate gate, and uniform-rate theorems to the `rateGamma`/`uniformRateGamma` API. Generalized factorization to any real rate with positive agreement and order; generalized low- and high-rate bounds to any positive order meeting the logarithmic lower bound. The fixed-rate selector uses the main branch's `rateMultiplicity` API. No compatibility bridge for `ratePartitionGamma_eq_rateGamma`, weight-budget equality, or finite-ratio equality was needed because the destination APIs are canonical; a separate least-multiplicity family was not ported because `rateMultiplicity`, its specification, and its minimality already provide the selector API. |
| [P3 slice 16: rate-partition moment source estimate (#1042)](https://github.com/Verified-zkEVM/ArkLib/pull/1042) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/RatePartition/SourceEstimate.lean`: `ratePartition_dimension_gt_of_moment` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.PartitionSupport.MomentSource`; acceptance cases in the matching `ArkLibTest` files | Covered by the existing generalized `partitionSupport_dimension_gt_moment`, which allows an arbitrary moment bound, rate, level, logarithm, and natural cutoff, and concludes with finrank and explicit simplex volume. The aggregate acceptance test retains a concrete dimension case at `d = 500`, `W = 1`. No source declaration is omitted; a source-specific production wrapper is not needed. |
| [P7a slice 15: symbolic Taylor chart point recognition (#1043)](https://github.com/Verified-zkEVM/ArkLib/pull/1043) | `ArkLib/Data/Polynomial/Differential/Basic.lean`: `polynomialJet_affine_combination`; `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/TaylorChart/PointRecognition.lean`: `ReedSolomon.exists_graphLine_pair_of_symbolic_sample` | `ArkLib.Data.Polynomial.Differential.Basic`; `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.TaylorChart.PointRecognition`; acceptance cases in the matching `ArkLibTest` files | Renamed `polynomialJet_affine_combination` to `PolynomialDifferential.polynomialJet_add_C_mul` and generalized it from fields to semirings. `ReedSolomon.exists_graphLine_pair_of_symbolic_sample` keeps its name and mathematics, using the current explicit `2 * K` exponent API. TaylorCuts bridge declarations are supplied by the current `TaylorChartAlgebra` API; no declarations from `PointRecognition.lean` were omitted. |
| [P7a slice 17: regular solution families in Taylor charts (#1046)](https://github.com/Verified-zkEVM/ArkLib/pull/1046) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/RootFinding/Geometry/SolutionEmbedding.lean`: `exists_regular_solution_jet_family`, `exists_regular_solution_jet_family_of_exponent`, `totalJetDegree_map_eq` | `ArkLib.Data.Polynomial.Differential.TaylorChartBaseChange`; `ArkLib.Data.Polynomial.Differential.BaseChange`; acceptance cases in the matching `ArkLibTest` files | Generalized `exists_regular_solution_jet_family_of_exponent` from `Fin n` to any finite coordinate type and from base-field pivots to pivots over the chart field. `exists_regular_solution_jet_family` is omitted as a thin default-`2K` wrapper; use the exponent theorem with `taylorExponentSufficient_two_mul`. `totalJetDegree_map_eq` is covered by `PolynomialDifferential.jetTotalDegree_map_eq` in `BaseChange.lean`. `exists_forall_jetEvaluation_ne_zero_map` is retained in the combined module, with no corresponding declaration in this unit's source snapshot. |
| [P3 slice 15: order-zero local image rank bounds (#1032)](https://github.com/Verified-zkEVM/ArkLib/pull/1032) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/OrderZero/LocalImage.lean`: `zeroLocalExponent` → `zeroOrderLocalExponent`, `zeroLocalExponent_reconstruct` → `zeroOrderLocalExponent_reconstruct`, `zeroLocalExponents` → `zeroOrderLocalExponents`, `mem_zeroLocalExponents` → `mem_zeroOrderLocalExponents`, `localConstraint_zero_support` → `localConstraintAt_zeroOrder_support`, `range_localConstraint_zero_le` → `range_localConstraintAt_zeroOrder_le`, `finite_localConstraint_zero_range` → `finite_range_localConstraintAt_zeroOrder`, `card_zeroLocalExponents_le` → `card_zeroOrderLocalExponents_le`, `finrank_localConstraint_zero_le` → `finrank_range_localConstraintAt_zeroOrder_le`, `finrank_localConstraint_zero_domRestrict_le` → `finrank_range_localConstraintAt_zeroOrder_domRestrict_le` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Local.ZeroOrder`; acceptance cases in the matching `ArkLibTest` files | Renamed the order-zero declarations to identify their specialization; their statements and bounds are unchanged. `unscaled_zero_support` is covered by `localE_le_localT_of_mem_support`, and `localContactOrder_zero` by `localContactOrder_eq`; `ZeroLocalIndex` and `card_zeroLocalIndex` are kept internal as the finite Sigma index and its cardinality calculation. |
| [P7a slice 16: symbolic Taylor cuts and degree bounds (#1044)](https://github.com/Verified-zkEVM/ArkLib/pull/1044) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/RootFinding/Symbolic/FrobeniusCuts.lean`: `challengeHeightLE_initialJetEquationOver`, `jointTotalDegree_initialJetEquationOver_le`, `jointTotalDegree_initialJetEquationOver_le_of_source`, `totalDegree_initialJetEquationOver_le`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/RootFinding/Symbolic/TaylorCutDegree.lean`: `taylorAgreementEquationOver`, `aeval_map_commonTaylorNumeratorOver_reconstruction_of_exponent`, `jointTotalDegree_taylorAgreementEquationOver_le_of_exponent`, `jointTotalDegree_taylorAgreementEquationOver_le_of_source_and_exponent`, `jointTotalDegree_taylorAgreementEquationOver_le_of_source`, `sparse_rationalTaylorPolynomial_of_symbolic_cuts` | `ArkLib.Data.Polynomial.Differential.TaylorChartAlgebra`; `ArkLib.ToMathlib.MvPolynomial.PolynomialCoefficients`; acceptance cases in the matching `ArkLibTest` files | Renamed `challengeHeightLE_initialJetEquationOver` to `coeffNatDegreeLE_initialJetEquation`, `jointTotalDegree_initialJetEquationOver_le` to `jointTotalDegree_initialJetEquation_le`, and the `_of_source` initial-jet theorem to `_of_coeffNatDegreeLE`; generalized coefficient bounds from `ChallengeHeightLE` to `CoeffNatDegreeLE`, the initial-jet center from a constant to any polynomial, and source-derived agreement bounds to `jetTotalDegree` and `CoeffNatDegreeLE`, without a positive-degree premise. The exponent-aware agreement degree theorem is `jointTotalDegree_taylorAgreementEquationOver_le_of_exponent`; its default-exponent specialization was removed. The coefficient-height theorem is `jointTotalDegree_taylorAgreementEquationOver_le_of_coeffNatDegreeLE_and_exponent`, with `τ` and its sufficiency proof explicit. Callers replace the removed default form with `τ := 2 * K` and `taylorExponentSufficient_two_mul`. `sparse_rationalTaylorPolynomial_of_symbolic_cuts` is unchanged. Added `MvPolynomial.jointTotalDegree_affine_le`. `totalDegree_initialJetEquationOver_le` is covered by the existing, more general `PolynomialDifferential.totalDegree_initialJetEquation_le`; `taylorAgreementEquationOver` and `aeval_map_commonTaylorNumeratorOver_reconstruction_of_exponent` are supplied by main under those destination names. |
| [P3 slice 17: weighted-support curve certificates (#1056)](https://github.com/Verified-zkEVM/ArkLib/pull/1056) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/Symbolic/ReceivedCurve.lean`: `SymbolicReceivedCurve.block_eq_canonical_submatrix` → `localConstraintBlock_eq_weightedSupportSubmatrix`, `SymbolicReceivedCurve.constraintMatrix_rank_le` → `localConstraintMatrix_rank_le_weightedSupport`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/Symbolic/ReceivedLine.lean`: `SymbolicReceivedInterpolation.coeff_interpolant_natDegree_lt` is covered by `SourceColumn.coeff_interpolant_natDegree_le` at cutoff `B - 1`, `SymbolicReceivedInterpolation.Certificate` is covered by `SymbolicReceivedCurve.Certificate` at `ℓ = 1` with `w i = receivedLine (f i) (g i)`, `exists_weightedSupport_certificate_of_fixed_margin`, `exists_weightedSupport_certificate_of_rate`, `exists_prescribed_symbolic_weightedSupport_certificate`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/Symbolic/WeightedSupport.lean`: `exists_primitive_weightedSupport_interpolant`, `localConstraintBlock_rank_le_base_actual`, `weightedSupportColumnIndex`, `interpolant_mem_weightedSupportSpace`, `weightedSupportCutoff_le_two_mul`, `y₀_le_two_mul_sub_one_of_eligible`, and the deleted `_two_mul_sub_one`, `_pred`, and `_lt` degree wrappers are covered by `SourceColumn.interpolant_totalJetDegree_le`, `SourceColumn.map_interpolant_jetTotalDegree_le`, and `totalJetDegree_le_pred_of_weightedSupportEligible`; `map_interpolant_mem_weightedSupportSpace` is omitted because it is unused and follows from `SourceColumn.map_interpolant` plus weighted-support membership. New shared helper: `Matrix.rank_prod_rows_le_sum` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Symbolic.WeightedSupportCertificate`; `ArkLib.ToMathlib.LinearAlgebra.Matrix.RowBlocks`; acceptance cases in the matching `ArkLibTest` files | Generalized curve results to arbitrary finite point/column types and specialized the main curve certificate to received lines for the weighted-support constructors. The generic point-block rank theorem is specialized to received lines, and the full matrix bound uses `Matrix.rank_prod_rows_le_sum`. The prescribed-threshold result drops the unused positive-message-dimension premise. The source strict coefficient-degree and weighted-support degree wrappers are covered by existing `SourceColumn` bounds and the eligibility degree theorem. `weightedSupportCutoff_le_two_mul` is a shared cutoff lemma, and `Matrix.rank_prod_rows_le_sum` bounds rank by row-block ranks. `finiteConstraintMatrix_rank_le` is covered by the stronger `rank_map_supportedLocalConstraintMatrix`; no library wrapper for `Matrix.rowBlock` is needed because the submatrix expression supplies that operation. |
| [P7a slice 19: power-batched symbolic point recognition (#1054)](https://github.com/Verified-zkEVM/ArkLib/pull/1054) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/PolynomialCurve/PointRecognition.lean`: `commonCurveAgreementSet_map` → same name, `exists_exceptional_powerBatched_extension` → same name, `powerBatchedJetGraph` → same name, `polynomialJet_powerBatched` → same name, `exists_polynomialGraph_of_symbolic_sample_of_exponent` → same name | `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.PowerBatchedPointRecognition`; acceptance cases in `ArkLibTest/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement.lean` | Generalized the agreement-set map and exceptional-challenge bound from `Fin n` to any finite coordinate type, using `Fintype.card α` in the bound. Generalized symbolic recognition to any embedded coordinate type without requiring finiteness; its regularity, high-cut, and agreement premises use the field-valued Taylor-chart API after specialization. The jet declarations are unchanged. No declarations were left out; intermediate over-algebra bridge statements are not duplicated and the proof uses existing field-valued Taylor-chart results. |
| [P7a slice 26: sparse Frobenius power-batched polynomial graph recognition (#1082)](https://github.com/Verified-zkEVM/ArkLib/pull/1082) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/PowerBatchedPointRecognition.lean`: `ReedSolomon.exists_frobeniusPowerGraph_polynomials_of_sample` | `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.PowerBatchedPointRecognition`; acceptance cases in the matching `ArkLibTest` files | Generalized from `Fin n` to any embedded coordinate type `α`, and restricted the root condition to the sample. Did not port `exists_exceptional_frobeniusPower_challenges_of_sample` because `ReedSolomon.exists_exceptional_exactPowerAgreement` already provides it: sample agreement proves the common-agreement lower bound, and specialization to `Fin n` gives the source bound. |
| [P7a slice 25: retained component dimension and incidence bounds (#1081)](https://github.com/Verified-zkEVM/ArkLib/pull/1081) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/TaylorChart/ComponentDimension.lean`: `ChartRing`, `ChartAway`, `localizedChartJet`, `localizedChartCoefficient`, `localizedChartCoefficient_eq_jet_of_exponent`, `chartCoefficientMap`, `chartCoordinate_mem_range_chartCoefficientMap_of_exponent`, `fixedCoefficientEvaluation_mem_ker_chartCoefficientMap_of_exponent`, `chart_prime_hilbertPolynomial_natDegree_le_of_agreements_of_exponent` → `chart_prime_affineHilbertPolynomial_natDegree_le_of_agreements_of_exponent`, `chart_dimensionSensitive_component_of_exponent`, `SourceRing`, `SourceAway`, `localizedSourceChallenge`, `localizedSourceJet`, `localizedSourceCoefficient`, `localizedSourceCoefficient_eq_jet_of_exponent`, `sourceCoefficientMap`, `sourceCoordinate_mem_range_sourceCoefficientMap_of_exponent`, `polynomialCoefficientEvaluation_mem_ker_sourceCoefficientMap_of_exponent`, `symbolicSource_prime_hilbertPolynomial_natDegree_le_of_polynomial_agreements_of_exponent` → `symbolicSource_prime_affineHilbertPolynomial_natDegree_le_of_polynomial_agreements_of_exponent`, `symbolicSourcePolynomial_dimensionSensitive_component_of_exponent`, `symbolicSource_dimensionSensitive_component_of_exponent`, `firstOrder_symbolicSource_dimensionSensitive_component_of_exponent`, `finite_symbolicSource_agreementLocus_off_excluded_and_ncard_le_hybrid_of_exponent`; `ArkLib/ToMathlib/RingTheory/MvPolynomial/CoefficientEvaluation.lean`: new `MvPolynomial.natDegree_affineHilbertPolynomial_add_ncard_le_of_polynomialCoefficientEvaluation` | `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.ComponentDimension`; `ArkLib.ToMathlib.RingTheory.MvPolynomial.CoefficientEvaluation`; acceptance cases in the matching `ArkLibTest` files | Generalized the chart prime bound by removing `c ≤ k`, the polynomial source kernel theorem by removing primality and separant nonvanishing assumptions, and the finite incidence theorem by removing `A ≤ n` and an unused initial separant nonvanishing premise. Reused the joint Taylor API and `MvPolynomial.natDegree_affineHilbertPolynomial_le_of_away_range` in `AwayPresentation`; added a set-indexed evaluation count bound for ideals of dimension greater than one. Replaced principal-open/agreement-index helpers with direct zero-locus and `Set.ncard` expressions. Omitted the unused affine kernel specialization and the component-or-excluded interface with no caller. `symbolicSource_prime_hilbertPolynomial_natDegree_le_of_agreements_of_exponent` was not ported: it specializes the polynomial-valued bound to affine received lines, and after the component proof was routed through the polynomial-valued theorem it has no caller. The separate prime Hilbert-degree wrapper is covered by the existing generic membership theorem. |
| [P6 slice 12: Frobenius Taylor chart recognition from a sample (#1083)](https://github.com/Verified-zkEVM/ArkLib/pull/1083) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/Ordinary/Frobenius/TaylorChart.lean`: `exists_frobeniusGraphLine_of_symbolic_sample` → same name | `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.TaylorChart.PointRecognition`; acceptance cases in the matching `ArkLibTest` files | Generalized the theorem from `Fin n` to an arbitrary embedded coordinate type and requires root equations only on the sample, as used by interpolation. The source file's only public declaration is covered. Its interpolation and Taylor-cut helpers already live in the current `GraphLine` and `TaylorChartAlgebra` APIs; nothing is deferred or unported. |
| [P9 slice 29: ordinary Johnson certificate construction (#1087)](https://github.com/Verified-zkEVM/ArkLib/pull/1087) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/Ordinary/JohnsonCertificate.lean`: `JohnsonColumnIndex`, `JohnsonLocalRowIndex`, `JohnsonRowIndex`, `johnsonColumns`, `johnsonColumns_y₀`, `johnsonColumns_x`, `johnsonColumns_totalJetDegree`, `johnsonColumns_injective`, `johnsonLocalRow`, `johnsonLocalRow_localJetDegree`, `johnsonConstraintMatrix`, `johnsonFinMatrix`, `johnsonFinRowWeight`, `card_johnsonColumnIndex`, `sum_johnsonColumns_slots`, `sum_johnsonFinRowWeight_slots`, `johnsonConstraintMatrix_kernel_iff`, `johnsonFinMatrix_kernel_iff`, `johnsonConstraintMatrix_degree_le`, `johnsonConstraintMatrix_eq_zero_of_grade_lt`, `johnsonFinMatrix_degree_le`, `johnsonFinMatrix_eq_zero_of_grade_lt`, `JohnsonSymbolicCertificate`, `johnsonColumns_weightedSupportEligible`, `johnsonInterpolant_jetDegree_le`, `exists_johnson_symbolic_certificate`; `map_interpolant_mem_weightedSupportSpace`, `differentialSpecialization_curve_interpolant_eq_zero_of_agreements`, `differentialSpecialization_map_interpolant_eq_zero_of_agreements`, `differentialSpecialization_map_interpolant_eq_zero_of_degree_lt` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Symbolic.JohnsonCertificate`; `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Symbolic.Soundness`; acceptance cases in the matching `ArkLibTest` files | Generalized `johnsonLocalRow_localJetDegree` to the current local jet-degree weight interface; removed unused received-polynomial degree bounds from matrix vanishing results and an unused row-weight premise from `johnsonFinMatrix_degree_le`; generalized `exists_johnson_symbolic_certificate` by removing unused agreement-at-most-one and threshold-at-most-length assumptions. Generalized `map_interpolant_mem_weightedSupportSpace` to any finite column type and the curve agreement theorem to selected finite sets over any index and finite column types. The coefficient-height bound, weighted-support membership, and support-range results are covered by generic declarations; the source `TaylorHeight` import is unnecessary. |
| [P7a slice 27: capped Taylor chart degree bounds (#1084)](https://github.com/Verified-zkEVM/ArkLib/pull/1084) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/RootFinding/Symbolic/TaylorDerivativeSupport.lean`: `mem_restrictDerivativeBidegree_of_bidegree` → `MvPolynomial.mem_restrictCappedBidegree_of_mem_restrictBidegree`, `commonTaylorNumeratorOver_mem_restrictDerivativeBidegree` → `PolynomialDifferential.commonTaylorNumeratorOver_mem_restrictCappedBidegree`, `degreeOf_initialJetEquationOver_firstOrder` → `PolynomialDifferential.degreeOf_initialJetEquation_le`, `degreeOf_taylorAgreementEquationOver_firstOrder` → `PolynomialDifferential.degreeOf_taylorAgreementEquationOver_firstOrder`, `initialJetEquationOver_mem_restrictDerivativeBidegree` → `PolynomialDifferential.initialJetEquation_mem_restrictCappedBidegree`, `taylorAgreementEquationOver_mem_restrictDerivativeBidegree` → `PolynomialDifferential.taylorAgreementEquationOver_mem_restrictCappedBidegree`; `MvPolynomial.optionEquivRight_symm_mem_restrictCappedBidegree` is new | `ArkLib.ToMathlib.RingTheory.MvPolynomial.CappedBidegree`; `ArkLib.ToMathlib.MvPolynomial.PolynomialCoefficients`; `ArkLib.Data.Polynomial.Differential.JetDegree`; `ArkLib.Data.Polynomial.Differential.RationalTaylorBidegree`; acceptance cases in the matching `ArkLibTest` files | Generalized the capped-membership support lemma to arbitrary commutative semirings and indexed coordinates, and added a flattening lemma combining coefficient, total-degree, and coordinate bounds. Generalized the initial-equation degree and capped-membership results to arbitrary differential order. The common-numerator and Taylor-agreement bounds use the destination capped-bidegree APIs; the agreement degree bound retains arbitrary polynomial centers and evaluation points. Omitted `derivativeWeight_eq_piSingle` because it is a source-specific encoding helper; the destination API bounds an indexed coordinate directly and uses the existing weighted-degree identity. |
| [P8 slice 3: power-batched component agreements and geometric transfer (#1085)](https://github.com/Verified-zkEVM/ArkLib/pull/1085) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/PolynomialCurve/ComponentAgreement.lean`: `commonAgreement_of_curveCut_mem_prime_of_exponent` → `commonCurveAgreement_of_jointTaylorAgreementEquation_mem_prime`, `exists_polynomialGraph_of_symbolic_prime_agreements_of_exponent` → `exists_polynomialGraph_of_primeTaylorComponent_agreements`; `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/PolynomialCurve/GeometricTransfer.lean`: `geometricTransferBound`, `exists_geometricTransfer_exceptional`, `exists_geometricTransfer_baseField_semantic` | `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.PowerBatchedComponentAgreement`; `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.PowerBatchedGeometricTransfer`; acceptance cases in the matching `ArkLibTest` files | Generalized component agreement extraction to every constituent of an arbitrary power-batched tuple; graph extraction supports any index set of size `L` with `k ≤ L` and selects the interpolation sample internally. `geometricTransferBound` uses `dimensionSensitiveIncidenceProduct` directly. Generalized the exceptional-set and base-field semantic theorems from `Fin n` to any finite coordinate type; removed unused algebraic-closure and parameter-bound hypotheses from the exceptional-set theorem. Omitted `symbolicSourceCurveAgreement_eq_zero_iff_of_exponent` because the generic `PolynomialDifferential.aeval_map_taylorAgreementEquationOver_eq_zero_iff_of_exponent` covers it; omitted `geometricTransferIncidenceProduct` and `geometricTransferIncidenceProduct_zero` because `dimensionSensitiveIncidenceProduct` covers them at degree one and its zero case. Source `ExtensionDescent` functionality is supplied by `HasExactPowerAgreement.descend` in `PowerAgreement.lean`. |
| [P7a slice 28: retired first-order flattening API (#1088)](https://github.com/Verified-zkEVM/ArkLib/pull/1088) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/FirstOrder/Squarefree/Flattening.lean`: `flattenFirstOrderChallenge` → `(optionEquivRight F (JetVariable 1)).symm Q`, `liftedSourceWeight` → `fun v ↦ v.elim 0 w`, `flattenFirstOrderChallenge_ne_zero_iff` → `(optionEquivRight F (JetVariable 1)).symm.injective.ne_iff` with `map_zero`, `flattenFirstOrderChallenge_challengeDegree_le` → `weightedTotalDegree_optionEquivRight_symm_coefficientDegree_le` (using `weightedTotalDegree_piSingle` for the `degreeOf none` form), `flattenFirstOrderChallenge_yOneDegree_le`, `flattenFirstOrderChallenge_jetWeight_le`, `flattenChallenge_weightedTotalDegree_le` → `weightedTotalDegree_optionEquivRight` and `AlgEquiv.apply_symm_apply` | `ArkLib.ToMathlib.MvPolynomial.OptionWeightedDegree`; `ArkLib.ToMathlib.MvPolynomial.PolynomialCoefficients`; acceptance cases in the matching `ArkLibTest` files | Retired the named flattening alias, generic helper, and all first-order wrappers because they are direct specializations of generic option-coordinate and weighted-degree results. The source unit and its acceptance file were removed; the existing `ArkLibTest/ToMathlib/MvPolynomial.lean` example exercises the coefficient-degree theorem. `PositiveProduct`, `RetainedCurve`, and `RetainedTail` should use the generic forms; any repeated `degreeOf` specialization in `PositiveProduct` should be private or local. |
| [R9 slice 4: explicit finite-field capacity list bound (#1086)](https://github.com/Verified-zkEVM/ArkLib/pull/1086) | `ArkLib/Data/CodingTheory/ReedSolomon/ListDecodability/Capacity/FiniteField.lean`: `exists_field_bounded_capacity_list` → same name | `ArkLib.Data.CodingTheory.ReedSolomon.ListDecodability.Capacity.FiniteField`; acceptance cases in the matching `ArkLibTest` files | Generalized the theorem by dropping the unused assumptions `delta < 1` and `A ≤ 2 * n`, and stating the derivative order and multiplicity through the canonical capacity parameter definitions. No declarations were left unported. |
| [P7a slice 29: Frobenius prime component recognition (#1090)](https://github.com/Verified-zkEVM/ArkLib/pull/1090) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/FrobeniusComponentRecognition.lean`: `frobeniusInitialGraph` → `frobeniusInitialGraph`, `exists_frobeniusGraph_of_symbolic_prime_sample` → `exists_frobeniusGraph_of_symbolic_prime_sample` | `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.FrobeniusComponentRecognition`; acceptance cases in the matching `ArkLibTest` files | Generalized `frobeniusInitialGraph` from a `Fin 1` jet-coordinate function to `Option (Fin 1)`, mapping the challenge coordinate to `X`, and from fields to commutative semirings. Generalized the theorem from `Fin n` to an arbitrary embedded type `ι`, and requires Frobenius roots only on the sample. It uses the current affine Hilbert polynomial and graph-restriction APIs. `symbolicSourceFrobeniusAgreement` is covered by the existing `jointTaylorAgreementEquation` in `PolynomialDifferential.TaylorChartAlgebra`, so no duplicate definition or wrapper was added. No new acceptance example was retained; a trial concrete component example exceeded 200,000 heartbeats, and the existing TaylorChart acceptance module exercises the point-recognition result. |
| [P8 slice 4: root-first challenge-degree budget acceptance case (#1091)](https://github.com/Verified-zkEVM/ArkLib/pull/1091) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/FirstOrder/Squarefree/PositiveProduct.lean`: `flattenedRootFirstEquiv`, `flattenedRootFirst`, `flattenedRootFirst_ne_zero_iff`, `flattenedRootFirst_rootDegree_le`, `flattenedContent`, `flattenedPositiveRootProduct`, `flattenedContent_rootDegree`, `flattenedContent_ne_zero`, `flattenedPositiveRootProduct_ne_zero`, `flattened_split_zero_iff`, `flattened_factorRootDegrees_le`, `flattened_content_add_factorChallengeDegrees_le`, `flattenedPositiveRootProduct_map_fraction_separable`, `flattenedPositiveRootProduct_resultant_ne_zero` | `ArkLibTest.ToMathlib.MvPolynomial`; acceptance cases in the matching `ArkLibTest` files | No ArkLib module or public declarations were added. The acceptance case composes existing coefficient-height, option-equivalence flattening, variable-renaming, and radical-split challenge-degree APIs for `t * Y₀ + Y₁` at height one. The first-order names are omitted as redundant: use the generic radical-content and radical-primitive-part APIs, weighted-degree and factor-class degree bounds, supported-map zero-locus theorem, separability results, and resultant theorem. Root-first flattening and root-degree facts are derived locally; no wrappers are needed. |
| [P7a slice 30: power-moment lifts for bounded polynomial coefficients (#1096)](https://github.com/Verified-zkEVM/ArkLib/pull/1096) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/PolynomialCurve/Degree.lean`: `challengeHeightLE_clearedSubstitution` → `CoeffNatDegreeLE.clearedSubstitution`; `ReedSolomon.PowerLiftIndex` → `MvPolynomial.PowerMomentIndex`, `ReedSolomon.powerMomentMap` → `MvPolynomial.powerMomentMap`, `ReedSolomon.powerMomentIdeal` → `MvPolynomial.powerMomentIdeal`, `powerMomentIdeal_isPrime`, `powerMomentMap_surjective`, `coefficientPowerLift`, `powerMomentMap_coefficientPowerLift`, `polynomialPowerLift`, `powerMomentMap_polynomialPowerLift`, `coefficientPowerLift_totalDegree_le_one`, `polynomialPowerLift_totalDegree_le` | `ArkLib.ToMathlib.MvPolynomial.PolynomialCoefficients`; `ArkLib.ToMathlib.MvPolynomial.PowerMomentLift`; `ArkLib.Data.Polynomial.Differential.RationalTaylorJointDegree`; acceptance cases in `ArkLibTest/ToMathlib/MvPolynomial.lean` | Generalized the cleared-substitution bound to an independent coefficient bound `a`, concluding `H * h + a` and restricting the coefficient hypothesis to `Q.support`. Moved the power-moment API into the generic MvPolynomial owner; generalized maps and lifts from fields to commutative semirings, primality to additively cancellative semirings with a domain structure, and degree bounds to nontrivial commutative semirings. The polynomial lift uses `CoeffNatDegreeLE` and maps to `(optionEquivRight R σ).symm P`. `ChallengeHeightLE` and its closure lemmas are represented by `CoeffNatDegreeLE` and existing lemmas; `prod_bound` is not promoted because no public result needs it and its finite-product estimate is handled internally. The private `TaylorBidegree.lean` copy is subsumed. Rational Taylor and bidegree results are covered by existing generalized coefficient, jet-degree, and rectangle-membership theorems; no paired common-denominator bidegree wrapper was added because existing separant coefficient-degree, power, and jet-degree bounds suffice. |
| [P9 slice 30: shared weighted Johnson certificate construction (#1092)](https://github.com/Verified-zkEVM/ArkLib/pull/1092) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/Ordinary/Weighted.lean`: `WeightedJohnsonLocalRowIndex`, `WeightedJohnsonRowIndex`, `weightedJohnsonLocalRowIndexToJohnson`, `weightedJohnsonLocalRowIndexToJohnson_errorGrade`, `weightedJohnsonConstraintMatrix`, `weightedJohnsonFinMatrix`, `weightedJohnsonFinRowWeight`, `sum_weightedJohnsonFinRowWeight_slots`, `weightedJohnsonConstraintMatrix_kernel_iff`, `weightedJohnsonFinMatrix_kernel_iff`, `weightedJohnsonFinMatrix_degree_le`, `weightedJohnsonFinMatrix_eq_zero_of_grade_lt`, `exists_weighted_johnson_symbolic_certificate`, `IsJohnsonWeightedCertificate.exists_symbolic`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/Symbolic/JohnsonCertificate.lean`: `exists_johnson_symbolic_certificate`, `exists_johnson_symbolic_certificate_of_shifted_surplus`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/Symbolic/ColumnHeight.lean`: `exists_primitive_interpolant_of_shifted_height`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/FirstOrder/CurveHeightCounting.lean`: `exists_primitive_firstOrderCurve_interpolant_of_shifted_height` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Symbolic.WeightedJohnsonCertificate`; `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Symbolic.JohnsonCertificate`; `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Symbolic.ColumnHeight`; `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.FirstOrder.CurveHeightCounting`; acceptance cases in the matching `ArkLibTest/Data/CodingTheory/HiddenDerivative/Interpolation/Symbolic.lean` file | Generalized the two weighted matrix kernel theorems by removing the redundant received-polynomial degree bound, and `weightedJohnsonFinMatrix_degree_le` by removing the unused row-weight premise. Added `exists_johnson_symbolic_certificate_of_shifted_surplus` as the shared constructor; generalized `exists_primitive_interpolant_of_shifted_height` to require only that matrix-kernel vectors satisfy the local constraints, and updated its first-order caller to use that implication. Added acceptance cases for ordinary and weighted certificate construction and a truncated weighted-kernel instance with multiplicity two and cutoff `0 < 2 - 1`. No source declarations were omitted. |
| [P7a slice 32: chunked power-moment lifts (#1099)](https://github.com/Verified-zkEVM/ArkLib/pull/1099) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/PolynomialCurve/ChunkedPowerLift.lean`: `ReedSolomon.chunkedCoefficientPowerLift` → `MvPolynomial.chunkedCoefficientPowerLift`, `ReedSolomon.powerMomentMap_chunkedCoefficientPowerLift` → `MvPolynomial.powerMomentMap_chunkedCoefficientPowerLift`, `ReedSolomon.chunkedPolynomialPowerLift` → `MvPolynomial.chunkedPolynomialPowerLift`, `ReedSolomon.powerMomentMap_chunkedPolynomialPowerLift` → `MvPolynomial.powerMomentMap_chunkedPolynomialPowerLift`, `ReedSolomon.chunkedCoefficientPowerLift_totalDegree_le` → `MvPolynomial.chunkedCoefficientPowerLift_totalDegree_le`, `ReedSolomon.chunkedPolynomialPowerLift_totalDegree_le` → `MvPolynomial.chunkedPolynomialPowerLift_totalDegree_le` | `ArkLib.ToMathlib.MvPolynomial.PowerMomentLift`; acceptance cases in the matching `ArkLibTest` files | Generalized the lifts and map theorems from fields to commutative semirings, and the multivariate lift and map from `ChallengeHeightLE` to `CoeffNatDegreeLE`; total-degree bounds additionally require nontrivial coefficients. Reused `PowerMomentIndex`, `powerMomentMap`, `CoeffNatDegreeLE`, and `optionEquivRight` for flattening. All six public declarations are represented; nothing was deferred or omitted. |
| [P7b slice 10: first-order curve certificates (#1100)](https://github.com/Verified-zkEVM/ArkLib/pull/1100) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/FirstOrder/CurveSymbolic.lean`: `FirstOrderCurveCertificate` → `FirstOrderCurveCertificate` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.FirstOrder.CurveCertificate`; acceptance cases in the matching `ArkLibTest` files | Kept the declaration's mathematical fields and specialization contract unchanged, using `SourceColumn.interpolant` in the destination API. Nothing was not ported. |
| [P7a slice 33: retained-curve equations in the generic differential API (#1101)](https://github.com/Verified-zkEVM/ArkLib/pull/1101) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/FirstOrder/Squarefree/RetainedCurve.lean`: `flattenedRootFirst (flattenFirstOrderChallenge Q)` → `challengeRetainingRootFirst`, `fromFlattenedRootFirst`, `flattenChallenge_fromFlattenedRootFirst` → `flattenedCoordinates_fromFlattenedRootFirst`, `positiveCurveEquation`, `positiveCurveEquation_ne_zero`, `curveJetReindex`, `curveJetView`, `curveJetView_totalDegree`, `positiveCurveEquation_jetWeight` → `positiveCurveEquation_jetTotalDegree_eq_curveJetView`, `flattenChallenge_fromFlattenedRootFirst` + `fromFlattenedRootFirst` → `fromFlattenedRootFirst_rootFirstChallenge`, `positiveCurveEquation_jetWeight_le` → `positiveCurveEquation_jetTotalDegree_le`, `positiveCurveEquation_yOneDegree`, `positiveCurveEquation_yOneDegree_le`, `positiveCurveEquation_challengeHeightLE` → `positiveCurveEquation_coeffNatDegreeLE`, `flattenChallenge_degreeOf_eq` → `degreeOf_optionEquivRight`, `flattenChallenge_weightedTotalDegree_eq`, `flattenedRootJetWeight` | `ArkLib.Data.Polynomial.Differential.RetainedCurve`; `ArkLib.ToMathlib.MvPolynomial.OptionWeightedDegree`; acceptance cases in the matching `ArkLibTest` files | Moved the retained positive-factor equation API to the generic `PolynomialDifferential` owner. Generalized the view degree identity to canonical `jetTotalDegree`, removed nonzero-input assumptions from the jet and `Y₁` degree bounds, and generalized `degreeOf_optionEquivRight` to arbitrary `σ` and commutative semirings. `flattenChallenge_weightedTotalDegree_eq` is supplied directly by the generic `weightedTotalDegree_optionEquivRight` result; `flattenedRootJetWeight` is represented by `jetTotalDegree` through `curveJetView_totalDegree`. |
| [P7a slice 34: admissible Frobenius graph pairs and witness equations (#1102)](https://github.com/Verified-zkEVM/ArkLib/pull/1102) | `ArkLib/Data/Polynomial/Differential/FrobeniusTaylorWitness.lean`: `symbolicFrobeniusWitness_equations` → `frobeniusExpansion_satisfies_jointTaylorCuts`; `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/FrobeniusAdmissibility.lean`: `IsAdmissibleFrobeniusPair` and projections `degree_left`, `degree_right`, `initial`, `regular`, `sparse`, `sample`; `exists_admissibleFrobeniusPair_of_symbolic_prime_sample`; `IsAdmissibleFrobeniusPair.specialize`; `IsAdmissibleFrobeniusPair.eq_of_initialGraph_eq` | `ArkLib.Data.Polynomial.Differential.FrobeniusTaylorWitness`; `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.FrobeniusAdmissibility`; acceptance cases in the matching `ArkLibTest` files | Generalized the admissible pair and extraction, specialization, and uniqueness theorems from `Fin n` to arbitrary embedded index types `ι`; grouped sample values and Frobenius root equations in one sampled condition, requiring root equations only on the sample. The witness theorem uses the joint Taylor-chart API and a flattened joint point. `frobeniusTaylorExponentSufficient` is covered by `taylorExponentSufficient_two_mul_sub_three` at `K = D * s + 1`, and `symbolicFrobeniusWitness_reconstruction` by `rationalTaylorPolynomial_polynomialJet` at order zero; no wrappers were added. No other public source declaration from the two files was left out. |
| [P7a slice 35: finite admissible Frobenius pair degree bound (#1103)](https://github.com/Verified-zkEVM/ArkLib/pull/1103) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/Ordinary/Frobenius/GraphCounting.lean`: `admissibleFrobeniusPairs_card_le_degreeOf` → `admissibleFrobeniusPairs_card_le_degreeOf` | `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.FrobeniusAdmissibility`; acceptance cases in the matching `ArkLibTest` files | Generalized the theorem from `Fin n` to an arbitrary embedded index type `ι`; the global Frobenius-root premise is unnecessary because uniqueness uses roots on the admissible sample. Reused the joint initial-jet equation and existing generic polynomial graph root count. Added a singleton-family acceptance case in `ArkLibTest/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement.lean`. Nothing was deferred or left unported. |
| [P7a slice 37: polynomial-curve agreement for Frobenius Taylor witnesses (#1105)](https://github.com/Verified-zkEVM/ArkLib/pull/1105) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/Ordinary/PolynomialCurve/Witness.lean`: `symbolicFrobeniusPowerWitness_equations` → `PolynomialDifferential.frobeniusExpansion_satisfies_jointTaylorCuts`, `symbolicFrobeniusPowerWitness_reconstruction` → `PolynomialDifferential.rationalTaylorPolynomial_polynomialJet` | `ArkLib.Data.Polynomial.Differential.FrobeniusTaylorWitness`; acceptance cases in the matching `ArkLibTest` files | Generalized the agreement characterization from a two-term Frobenius power curve to any polynomial-valued curve; specializing to `ReedSolomon.frobeniusPowerCoordinate` recovers the source result. The reconstruction result is covered by the general rational Taylor theorem at order zero, so its source wrapper was not added. |
| [P7a slice 38: finite family of admissible Frobenius pairs (#1106)](https://github.com/Verified-zkEVM/ArkLib/pull/1106) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/Ordinary/Frobenius/RetainedFamily.lean`: `frobeniusRetainedPairFamily` → `frobeniusRetainedPairFamily`, `mem_frobeniusRetainedPairFamily_iff` → `mem_frobeniusRetainedPairFamily_iff`, `frobeniusRetainedPairFamily_card_le` → `frobeniusRetainedPairFamily_card_le`, `exists_exceptional_frobeniusRetainedPairFamily` → `exists_exceptional_frobeniusRetainedPairFamily` | `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.FrobeniusRetainedFamily`; acceptance cases in the matching `ArkLibTest` files | Generalized the family and all four declarations from `Fin n` to any finite embedded coordinate type. Defined the family as a filter of the existing correlated-pair family; the cardinality bound uses the joint initial equation, since root equations are part of each admissible pair's sample witness. The exceptional-set theorem specializes the existing correlated-pair theorem. No declarations were renamed or deferred; all four public source declarations are covered. |
| [P7b slice 11: finite first-order curve certificates (#1107)](https://github.com/Verified-zkEVM/ArkLib/pull/1107) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/FirstOrder/CurveFinite.lean`: `exists_finite_firstOrder_curve_certificate_of_heightSlotCount` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.FirstOrder.CurveCertificate`; acceptance cases in the matching `ArkLibTest` files | The declaration keeps its name, assumptions, and conclusion, and is consolidated into the existing certificate owner rather than a parallel module. Its proof uses the existing weighted-degree global multiplicity theorem. Nothing from the source unit is omitted. |
| [P3 slice 22: rate-dependent partition-support certificates (#1108)](https://github.com/Verified-zkEVM/ArkLib/pull/1108) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/RatePartition/Adapter.lean`: `exists_ratePartitionFinite_certificate` → `exists_partitionSupport_curve_certificate_of_finiteRatio`, `exists_ratePartitionRate_certificate` → `exists_partitionSupport_curve_certificate_of_paddedRateBlockThreshold`, `exists_ratePartitionMathematical_certificate` → `exists_partitionSupport_curve_certificate_of_rateBlockThreshold` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.PartitionSupport.RateCertificate`; acceptance cases in the matching `ArkLibTest` files | Generalized the finite-ratio theorem by deriving `0 < n` from its remaining hypotheses; the two block-threshold theorems are mathematically unchanged and use the renamed threshold and finite-parameter APIs. All three public source declarations are ported; none are deferred or omitted. |
| [P7b slice 12: finite first-order symbolic line certificate (#1109)](https://github.com/Verified-zkEVM/ArkLib/pull/1109) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/FirstOrder/FiniteCertificate.lean`: `exists_finite_firstOrder_symbolic_certificate_of_heightSlotCount` → `exists_finite_firstOrder_symbolic_certificate_of_heightSlotCount` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.FirstOrder.CurveCertificate`; acceptance cases in the matching `ArkLibTest` files | The theorem keeps its assumptions, conclusion, and name, and specializes the existing finite curve-certificate constructor at curve degree one. It transfers specialization soundness to affine combinations of the two received words while preserving support, coefficient-height, primitivity, and local-constraint fields. No declarations are deferred or omitted. |
| [P7a slice 39: admissible Frobenius power tuple counting by degree (#1110)](https://github.com/Verified-zkEVM/ArkLib/pull/1110) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/Ordinary/PolynomialCurve/Admissible.lean`: `IsAdmissibleFrobeniusPowerTuple`, `IsAdmissibleFrobeniusPowerTuple.specialize`, `IsAdmissibleFrobeniusPowerTuple.eq_of_initialGraph_eq`; `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/Ordinary/PolynomialCurve/Counting.lean`: `admissibleFrobeniusPowerTuples_card_le_degreeOf` | `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.PowerTupleCounting`; acceptance cases in the matching `ArkLibTest` files | Generalized tuple admissibility, specialization, uniqueness, and finite-family counting from a `Fin n` domain to an arbitrary embedded coordinate type; uniqueness uses `polynomialTuple_eq_of_infinite_specializations`. The declarations keep their names. The prime-component adapter `exists_admissibleFrobeniusPowerTuple_of_symbolic_prime_sample` is not ported because constructing admissible tuples from prime components is outside this counting unit. |
| [P9 slice 31: uniform rate-partition curve certificates (#1111)](https://github.com/Verified-zkEVM/ArkLib/pull/1111) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/PartitionSupport/RateBound.lean`: `partitionSupport_totalJetDegree_le_of_ambient_lower_bound`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/PartitionSupport/RateCertificate.lean`: `MathematicalRatePartitionEnvelope.exists_curve_certificate` → `RatePartitionEnvelope.exists_curve_certificate`, `UniformRatePartitionEnvelope.exists_curve_certificate` → `RatePartitionEnvelope.exists_curve_certificate`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Parameters/RatePartition/MathematicalUniform.lean`: `uniformMathematical_totalJetDegree_le` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.PartitionSupport.RateBound`; `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.PartitionSupport.RateCertificate`; `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.RatePartition.MathematicalUniform`; acceptance cases in the matching `ArkLibTest` files | Generalized the ambient lower-bound total-jet estimate to any multiplicity, giving cap `⌈m / δ²⌉₊ - 1`, and reused it in the mathematical-uniform degree theorem. Consolidated the mathematical-uniform and uniform-envelope curve certificates into `RatePartitionEnvelope.exists_curve_certificate`, generalized to qualifying scales; the source block-length premise is unnecessary once the envelope is supplied. Both rate-dependent and gap-dependent bounds use a shared private positive-denominator comparison while retaining separate ceiling steps. No declarations were left unported. |
| [P7b slice 13: first-order finite-rate certificates (#1112)](https://github.com/Verified-zkEVM/ArkLib/pull/1112) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/FirstOrder/RateCertificate.lean`: `FirstOrderFiniteRateParameters.rankCount_mul_lt_dimensionCount`, `FirstOrderFiniteRateParameters.kernelHeight_le_challengeDegree`, `exists_firstOrderRate_symbolicCertificate`, `FirstOrderSymbolicCertificate.toCurve` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.FirstOrder.RateCertificate`; `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.FirstOrder.CurveCertificate`; acceptance cases in the matching `ArkLibTest` files | Generalized `kernelHeight_le_challengeDegree` by dropping the unnecessary `0 < n` hypothesis. Placed `FirstOrderSymbolicCertificate.toCurve` in the canonical curve-certificate owner. Added the shared `differentialSpecialization_eq_zero_of_firstOrderSpace` theorem for both curve-certificate constructors. Did not port `firstOrderExponents_subset_degree_two` because the destination rank theorem handles arbitrary degree and agreement parameters; did not add `firstOrder_rate_curve_matrix_rank_le` because `rank_firstOrderLocalConstraintMatrix_le` and the existing rank-count identity cover it. |
| [P7b slice 14: verified first-order interpolation profiles (#1113)](https://github.com/Verified-zkEVM/ArkLib/pull/1113) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/FirstOrder/Profile.lean`: `LineProfile`, `LineProfile.D` → `LineProfile.candidateDegree`, `LineProfile.computedDimension`, `LineProfile.computedLocalRank`, `LineProfile.computedHeightSlots`, `LineProfile.shiftedRowSlots`, `LineProfile.shiftedHeightSlots`, `LineProfile.Verification` and projections `candidateDegree_pos`, `budget_pos`, `cap_le_height`, `dimension_eq`, `localRank_eq`, `heightSlots_eq`, `columnWeight_eq`, `heightSurplus`, `Verification.support_card_eq`, `Verification.columnY₀Weight_eq`, `LineProfile.columns`, `LineProfile.SymbolicCertificate`, `Verification.exists_symbolicCertificate`, `CurveVerification`, `Decidable CurveVerification`, `CurveVerification.exists_certificate` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.FirstOrder.Profile`; acceptance cases in the matching `ArkLibTest` files | Generalized `LineProfile.Verification`, `support_card_eq`, `columnY₀Weight_eq`, symbolic certificate construction, `CurveVerification`, and curve certificate construction by omitting `LineProfile.Verification.degree_le` and the tautological degree conjunct in `CurveVerification`, deriving the condition internally; renamed source `D` to `candidateDegree`, with unchanged mathematics. The automatic recipe bridge is deferred: `automaticFirstOrderThreshold_eq_firstOrderRateThreshold`, `automaticBeta_eq_firstOrderRateBeta`, `automaticDerivativeCapRaw_eq_firstOrderRateDerivativeCap`, `automaticJetDegree_eq_firstOrderRateJetDegree`, `automaticSourceCount_eq_firstOrderRateSourceCount`, `automaticRankCount_eq_firstOrderRateRankCount`, and `automaticChallengeHeight_eq_firstOrderRateChallengeDegree` are recipe-specific. `automaticSourceCount_le_dimensionCount` and `certifiedEnlargedRankBound_one_eq_automaticRankCount` are covered by generic results; `automaticFirstOrderFiniteRateParameters` and `exists_automaticFirstOrder_symbolicCertificate` require unavailable `automaticMultiplicity_pos` and `automaticRankCount_lt_sourceCount`. |
| [P7b slice 15: automatic first-order slack bounds and curve-stage sums (#1118)](https://github.com/Verified-zkEVM/ArkLib/pull/1118) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Parameters/FirstOrder/AutomaticParameters.lean`: definitions `automaticAgreement`, `automaticGapBracket`, `automaticSurplus`, `automaticMultiplicity`, `automaticDerivativeCapRaw`, `automaticJetDegree`, `automaticDerivativeCap`, `automaticSourceCount`, `automaticRankCount`, `automaticChallengeHeight`, `automaticSourceDensity`, `automaticRankDensityEnvelope`; `automaticBeta` → `automaticDerivativeRatio`; theorems `automaticAgreement_eq_min`, `automatic_threshold_lt_agreement`, `automaticAgreement_le`, `automaticAgreement_lt_one`, `rho_lt_automaticAgreement`, `automaticSurplus_pos`, `four_le_automaticMultiplicity_mul_surplus`, `automaticMultiplicity_pos`, `automaticDerivativeCapRaw_le_jetDegree`, `automaticDerivativeCap_eq_raw`, `automaticSourceCount_eq_raw`, `automaticRankCount_eq_raw`, `automaticRankCount_le_densityEnvelope_add_rounding`, `automaticSourceDensity_mul_cube_le_sourceCount`, `automatic_rounding_loss_bound`, `automaticSurplusQuarter_le_source_sub_rank`, `automaticRankCount_lt_sourceCount`, `automaticChallengeDenominator_pos`, `automaticChallengeHeight_eq`, `automaticJetDegree_pos`, `automatic_degree_le_rate_mul`; `automaticBeta_pos` → `automaticDerivativeRatio_pos`, `automaticBeta_lt_three_four` → `automaticDerivativeRatio_lt_three_four`, `automaticBeta_lt_agreement_div_rate` → `automaticDerivativeRatio_lt_agreement_div_rate`, `automatic_sourceDensity_sub_rankDensityEnvelope`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Parameters/FirstOrder/AutomaticBounds.lean`: definitions `automaticRateGap`, `automaticSurplusSlope`, `automaticMultiplicityBoundConstant`, `automaticJetBoundConstant`, `automaticHeightBoundConstant`, `automaticMomentBoundConstant`, `automaticExceptionBoundConstant`, `automaticHybridEnvelopeConstant` → `automaticRateEnvelopeConstant`, `automaticLambdaBoundConstant` → `automaticListBoundConstant`; theorems `automaticRateGap_pos`, `automatic_eta_lt_rateGap`, `half_eta_le_automaticAgreement_sub_threshold`, `half_agreement_slack_le_automaticGapBracket`, `automaticSurplusSlope_mul_eta_le`, `automaticSurplusSlope_pos`, `automaticMultiplicityBoundConstant_pos`, `automaticJetBoundConstant_pos`, `automaticMultiplicity_le_inv_eta`, `automaticDerivativeCap_le_inv_eta`, `automaticJetDegree_le_inv_eta`, `automaticRankDensityEnvelope_le_one`, `automaticRankCount_le_four_mul_cube`, `automaticChallengeHeight_le_inv_eta_sq`, `automaticParameterBounds`, `automaticJetBoundConstant_le_envelope`, `automaticHeightBoundConstant_le_envelope`, `automaticMomentBoundConstant_le_envelope`; `automaticHybridT_le_inv_eta_cube` → `automaticStaircaseMoment_le_inv_eta_cube`, the hybrid-envelope bounds renamed to `automaticRateEnvelopeConstant_one_le` and `automaticRateGapInv_le_rateEnvelopeConstant`, and the closed-envelope bounds renamed to `automaticListConstant_le_cubic_slack_envelope`, `automaticExceptionConstant_le_quintic_slack_envelope`, and `automaticClosedListAndExceptionBounds`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Parameters/FirstOrder/HybridConstants.lean`: `hybridT_le_three_mul_cube` → `stageStaircase_le_three_mul_cube`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Parameters/FirstOrder/StageSum.lean`: new `firstOrderCurveIncidenceRatio`, definitions `firstOrderCurveJointRatio`, `firstOrderCurveFiberRatio`, `firstOrderCurveDirectRatio`, `firstOrderCurveStageCharge`, theorem `firstOrderCurveDirectRatio_le_jointRatio`, `firstOrderCurveStageCap_add_height_eq_of_factors`, `SymbolicSeparantChain.Chain.sum_firstOrderCurveStageCharge_add_height_le_of_factors` → `PolynomialDifferential.SeparantChain.sum_firstOrderCurveStageCharge_add_height_le_of_factors`, `SymbolicSeparantChain.Chain.sum_firstOrderCurveStageCharge_add_height_le_of_exponent` → `sum_firstOrderCurveStageCharge_add_height_le_of_directRatio` | `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.FirstOrder.AutomaticParameters`; `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.FirstOrder.AutomaticBounds`; `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.FirstOrder.HybridConstants`; `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.FirstOrder.StageSum`; acceptance cases in the matching `ArkLibTest` files | Generalized `automatic_sourceDensity_sub_rankDensityEnvelope` by dropping unused agreement assumptions and generalized both chain-sum theorems from fields to commutative semirings while dropping unused `0 < k` and `k ≤ L` guards. Derivative-ratio bounds and slack comparisons drop unused guards. Omitted the three role-specific ratio lower-bound wrappers because the chain proofs apply `one_le_incidenceFactor` directly with `b = 1`; omitted `firstOrderCurveStageCap_add_height_eq_of_exponent` as an unused specialization of the factors identity. Unexposed unused `AutomaticRecipe` helpers, the private `automaticSourceLowerCount` helper, and later challenge-height applications with no consumer. Recipe-specific count and surplus results are covered by existing first-order declarations. |
| [P5 slice 23: sharp capped-degree hypersurface incidence bound (#1114)](https://github.com/Verified-zkEVM/ArkLib/pull/1114) | `ArkLib/ToMathlib/AlgebraicGeometry/Incidence/TwoJet.lean`: `AffineHilbert.twoJetHypersurface_source_incidence_sharp` → `MvPolynomial.cappedDegreeHypersurface_incidence_sharp`; new declaration: `Ideal.map_prime_principalOpenData_of_surjective` | `ArkLib.ToMathlib.RingTheory.Nullstellensatz.CappedDegreeIncidence`; `ArkLib.ToMathlib.RingTheory.Ideal.SurjectiveMap`; `ArkLib.ToMathlib.RingTheory.Nullstellensatz.CappedBidegreeIncidence`; acceptance cases in the matching `ArkLibTest` files | Generalized the incidence theorem by removing `0 < k`, `c ≤ b`, and properness of `span {g}`, and stating agreement counts as `Set.ncard`. Added a generic surjective-map theorem transporting prime ideal, comap, principal-open, generator, and lifted-cut data; both capped incidence proofs use it. The acceptance case checks `(1, 0)` on `X 1 = 0` with concrete cuts and computed hypotheses. Nothing from the source unit was deferred or omitted. |
| [P7a slice 41: count admissible correlated pairs in Taylor charts (#1117)](https://github.com/Verified-zkEVM/ArkLib/pull/1117) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/TaylorChart/PairCounting.lean`: `chartPairPullback`, `chartPairJet`, `IsAdmissibleChartPair`, `eval_chartPairPullback_symbolic`, `degree_correlatedPairSpecialization_lt`, `coeff_taylor_correlatedPairSpecialization`, `IsAdmissibleChartPair.specialize`, `admissibleChartPairs_card_le`, `admissibleChartPairFamily`, `mem_admissibleChartPairFamily_iff`, `admissibleChartPairFamily_card_le` | `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.TaylorChart.PairCounting`; acceptance cases in the matching `ArkLibTest` files | Generalized `admissibleChartPairs_card_le` and `admissibleChartPairFamily_card_le` to include `k = 0`, using a singleton bound there and retaining the incidence estimate for positive `k`. The chart numerators' default exponent `2*K` is explicit in the destination API. The private helper `jetDegree_pos_of_initialSeparant_ne_zero` is not ported because the destination incidence theorem handles regular-jet counting without its positivity premise. |
| [P7a slice 40: sharp regular Taylor-chart counting (#1116)](https://github.com/Verified-zkEVM/ArkLib/pull/1116) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/RootFinding/Geometry/SharpCounting.lean`: `finite_regularHighCutJets_card_le_sharp_of_exponent`, `finite_regularHighCutJets_card_le_sharp` → `card_le_of_highTaylorCuts_of_agreement_sharp`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/RootFinding/Geometry/RegularCounting.lean`: `finite_regular_solutions_card_le` → `card_le_of_regular_solutions_agreement` | `ArkLib.Data.Polynomial.Differential.TaylorChartIncidence`; `ArkLib.Data.Polynomial.Differential.TaylorChartBaseChange`; acceptance cases in the matching `ArkLibTest` files | Generalized the sharp high-cut-jet bound to any finite index type and explicit sufficient Taylor exponent, using `jetTotalDegree` and dropping positive-degree, positive-`k`, and global nonzero-separant assumptions while retaining per-jet regularity. Generalized the regular-solution count to any algebraically closed field extension and explicit sufficient exponent, with sharp numerator `n - k + 1`, `jetTotalDegree`, and no positive-degree or positive-`k` assumptions. `finite_regularHighCutJets_card_le_sharp` is not separate because the explicit-exponent theorem covers its fixed `2 * K` case via `taylorExponentSufficient_two_mul`; no source result was omitted because of a proof blocker. |
| [P8 slice 5: power-batched graph admissibility (#1115)](https://github.com/Verified-zkEVM/ArkLib/pull/1115) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/PolynomialCurve/GraphAdmissibility.lean`: `chartTuplePullback`, `chartTupleJet`, `powerBatchedTaylorCoefficient`, `powerBatchedTaylorCoefficient_eval`, `IsAdmissibleChartTupleAtExponent`, `eval_chartTuplePullback`, `eval_chartTuplePullback_symbolic`, `IsAdmissibleChartTupleAtExponent.specialize`, `exists_admissibleChartTuple_of_symbolic_prime_agreements_of_exponent` → `exists_admissibleChartTuple_of_primeTaylorComponent_agreements` | `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.PowerBatchedAdmissibility`; acceptance cases in the matching `ArkLibTest` files | Kept the graph restriction, specialized jet, Taylor coefficient, admissibility fields, evaluation identities, specialization theorem, and mathematical result of the prime-component theorem. The symbolic equation names come from the existing joint Taylor-chart API, and the theorem reuses `exists_polynomialGraph_of_primeTaylorComponent_agreements`. The acceptance example applies the new theorem to the component ideal and agreement cuts, then specializes the returned admissible tuple at zero and checks the Taylor reconstruction identity. No public declarations were omitted. |
| [P8 slice 6: uniform first-order numerical bounds (#1119)](https://github.com/Verified-zkEVM/ArkLib/pull/1119) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Parameters/FirstOrder/HybridConstants.lean`: `ReedSolomon.hybridB1_mono` → `ReedSolomon.HiddenDerivative.regularFiberStageSum_mono`, `ReedSolomon.hybridJ1_mono` → `ReedSolomon.HiddenDerivative.regularJointStageSum_mono`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Parameters/FirstOrder/UniformMCA.lean`: `ReedSolomon.uniformFirstOrderMCASplit` → `ReedSolomon.HiddenDerivative.uniformFirstOrderMcaSplit`, `ReedSolomon.uniformFirstOrderMCASplit_bounds` → `ReedSolomon.HiddenDerivative.uniformFirstOrderMcaSplit_bounds`, `ReedSolomon.uniformFirstOrderMCASplit_lambdaOne_le` → `ReedSolomon.HiddenDerivative.uniformFirstOrderMcaSplit_retainedCoordinateRatio_le`, `ReedSolomon.uniformFirstOrderMCASplit_lambdaTwo_le` → `ReedSolomon.HiddenDerivative.uniformFirstOrderMcaSplit_fixedCoordinateRatio_le`, `ReedSolomon.uniformFirstOrderMCA_D_mul_theta_le` → `ReedSolomon.HiddenDerivative.uniformFirstOrderMca_degree_mul_agreementIncidenceRatio_le`, `ReedSolomon.uniformFirstOrderMCA_heightSlotCount` → `ReedSolomon.HiddenDerivative.uniformFirstOrderMca_heightSlotCount`, `ReedSolomon.uniformFirstOrderMCA_hybridB1_four` → `ReedSolomon.HiddenDerivative.uniformFirstOrderMca_regularFiberStageSum_four`, `ReedSolomon.uniformFirstOrderMCA_hybridB1_four_one` → `ReedSolomon.HiddenDerivative.uniformFirstOrderMca_regularFiberStageSum_four_one`, `ReedSolomon.uniformFirstOrderMCA_hybridJ1_four` → `ReedSolomon.HiddenDerivative.uniformFirstOrderMca_regularJointStageSum_four`, `ReedSolomon.uniformFirstOrderMCA_hybridJ1_four_one` → `ReedSolomon.HiddenDerivative.uniformFirstOrderMca_regularJointStageSum_four_one`, `ReedSolomon.uniformFirstOrderMCA_hybridEOptimizedRaw_le_ceiling` → `ReedSolomon.HiddenDerivative.uniformFirstOrderMca_optimizedExceptionCharge_le_ceiling`, `ReedSolomon.uniformFirstOrderMCA_hybridERaw_le` → `ReedSolomon.HiddenDerivative.uniformFirstOrderMca_exceptionCharge_le`, `ReedSolomon.uniformFirstOrderMCA_hybridERaw_le_ceiling` → `ReedSolomon.HiddenDerivative.uniformFirstOrderMca_exceptionCharge_le_ceiling`, `ReedSolomon.uniformFirstOrderMCA_parameters` → `ReedSolomon.HiddenDerivative.uniformFirstOrderMca_parameters`, `ReedSolomon.uniformFirstOrderMCA_theta_le` → `ReedSolomon.HiddenDerivative.uniformFirstOrderMca_agreementIncidenceRatio_le`; `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/SingularTail.lean`: `ReedSolomon.FirstOrder.Squarefree.squarefreeListExpression_le`, `ReedSolomon.FirstOrder.Squarefree.squarefreeListExpression_le_rate_envelope` | `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.FirstOrder.HybridConstants`; `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.FirstOrder.UniformMca`; `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.SingularTail`; acceptance cases in the matching `ArkLibTest` files | Generalized `uniformFirstOrderMcaSplit_fixedCoordinateRatio_le`, `uniformFirstOrderMca_heightSlotCount`, `uniformFirstOrderMca_parameters`, and `uniformFirstOrderMca_agreementIncidenceRatio_le` by dropping unused `A ≤ n` premises; renamed the split and ratio, stage-sum, and exception-charge declarations to the destination names. Added squarefree list-expression bounds, including the `7 C^3 n q^2` rate envelope, using the destination stage-exponent API. `firstOrderCurveFiberStageOne_le_four_mul` is covered by the stronger `firstOrderCurveFiberStageOne_regularTaylorExponent_le` and is derived locally instead of ported. The case-only `UniformMCA` → `UniformMca` path change still needs index access and a generator rerun; the root import remains `UniformMCA`. |
| [P7a slice 43: sharp admissible Taylor chart pair bounds (#1122)](https://github.com/Verified-zkEVM/ArkLib/pull/1122) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/TaylorChart/PairCounting.lean`: `admissibleChartPairs_card_le_sharp` → same name, `admissibleChartPairFamily_card_le_sharp` → same name | `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.TaylorChart.PairCounting`; acceptance cases in the matching `ArkLibTest` files | Generalized both bounds to allow `k = 0`, where degree constraints force all admissible pairs to be zero and the count is at most one. For positive `k`, both use the sharp factor with numerator `n - k + 1`; the proof specializes the generic sharp high-cut incidence theorem. No public source declaration was omitted. The source-private `jetDegree_pos_of_initialSeparant_ne_zero_sharp` was not ported because the current generic sharp incidence theorem has no positive jet-degree premise. |
| [P7b slice 16: first-order regular-stage curve exceptions (#1120)](https://github.com/Verified-zkEVM/ArkLib/pull/1120) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/FirstOrder/CurveTransfer.lean`: `exists_exceptional_of_regular_stage_bounds_of_factors` → same name | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.FirstOrder.CurveCertificate`; acceptance cases in the matching `ArkLibTest` files | Generalized the theorem by dropping the unused assumptions `0 < k` and `k ≤ L`. Deferred `exists_exceptional_of_regular_stage_bounds_of_exponent` as a direct-ratio specialization of the factors theorem; the ratio lower bound follows from the existing incidence estimate. |
| [P7a slice 42: agreement-gap geometric counting bounds (#1121)](https://github.com/Verified-zkEVM/ArkLib/pull/1121) | `ArkLib/ToMathlib/Analysis/SpecificLimits/GeometricBounds.lean`: `geometric_ratio_le` → `agreementGap_geometricRatio_le`, `geometric_count_le_manuscript_bound` → `geometricCount_le_of_agreementGap` | `ArkLib.ToMathlib.Analysis.SpecificLimits.GeometricBounds`; acceptance cases in the matching `ArkLibTest` files | Generalized both theorems' conclusions from `ℝ` to any linearly ordered field; `geometricCount_le_of_agreementGap` retains its rational count premise. The characteristic-based binomial pivot condition is covered by `PolynomialDifferential.natCast_choose_ne_zero_of_ringChar`, specialized at each index and combined with binomial symmetry, so it was not ported. No Reed–Solomon wrapper was added because the numerical statements have no Reed–Solomon-specific data. |
| [P7a slice 44: Taylor-chart points outside admissible pair graphs (#1123)](https://github.com/Verified-zkEVM/ArkLib/pull/1123) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/TaylorChart/Incidence.lean`: `sourceChartHighCuts` → `jointTaylorHighCutList`, `symbolicSourceNumerator_mem_sourceChartHighCuts` → `jointCommonTaylorNumerator_mem_jointTaylorHighCutList`, `sourceChartPairLocus` → `admissibleChartPairGraphLocus`, `principalOpen_subset_sourceChartPairLocus` → `principalOpen_subset_admissibleChartPairGraphLocus`, `finite_sourceChart_points_off_pairs_card_le` → `finite_regularJointTaylorChartPoints_off_admissiblePairGraphs_card_le`, `finite_sourceChart_points_off_pairs_card_le_of_source` → `finite_regularJointTaylorChartPoints_off_admissiblePairGraphs_card_le_of_jetDegree` | `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.TaylorChart.Incidence`; acceptance cases in the matching `ArkLibTest` files | Generalized the graph-locus inclusion to `Set.ncard` cut counts and explicit exponent `2 * K`. Generalized the finite incidence bound to `L ≤ A` and `A - L + 1 ≤ n`, using `Set.ncard` for agreement counts and dropping the unnecessary positive cut-degree premise. Generalized its specialization to `jetTotalDegree` and `CoeffNatDegreeLE`, dropping the positive jet-degree premise. No public source declarations were omitted; finite index-set encodings use `Set.ncard` directly. |
| [P3 slice 23: prescribed geometric Taylor cutoff bounds (#1127)](https://github.com/Verified-zkEVM/ArkLib/pull/1127) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Parameters/TaylorCutoff.lean`: `prescribed_geometric_parameters` (same name), `geometric_characteristic_components` → `characteristic_bounds_of_max`, `geometric_below_characteristic` → `jetDegreeCastsNeZero_of_jetTotalDegree_charGuard` | `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.WeightedSupport.Capacity`; `ArkLib.Data.Polynomial.Differential.DerivativeDescent`; `ArkLib.Data.CodingTheory.ReedSolomon.ListDecodability.Capacity.WeightedSupportInterpolant`; acceptance cases in the matching `ArkLibTest` files | Generalized `prescribed_geometric_parameters` by dropping `0 < k` and using the current harmonic and agreement-threshold APIs. Generalized the characteristic bounds from fields to commutative semirings and dropped `0 < K`; generalized the cast theorem to any commutative semiring and the current `JetDegreeCastsNeZero` contract, without unused message-dimension and block-length parameters. The weighted-support construction reuses the generic cast theorem. Did not recreate the source-shaped `IsBelowCharacteristic (k - 1) Q` result because that predicate is absent from the current API; its jet-degree consequence is provided through `JetDegreeCastsNeZero`, while ambient-cutoff bounds remain separate in the current certificate API. |

| [P3 slice 19: large-order finite rate-partition surplus (#1068)](https://github.com/Verified-zkEVM/ArkLib/pull/1068) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/RatePartition/Bound.lean`: `partitionSupport_finiteRatio_surplus`, `ratePartition_dimension_gt_finiteRatio` → `partitionSupport_largeOrder_finiteRatio_surplus` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.PartitionSupport.FiniteSurplus`; `ArkLibTest/Data/CodingTheory/HiddenDerivative/Interpolation/PartitionSupport.lean`; acceptance cases in the matching `ArkLibTest` files | Retained the moment-conditional `partitionSupport_finiteRatio_surplus` and its second-moment lower-bound hypothesis. Generalized the large-order result to any natural cutoff satisfying the level bound and stated it using the partition-support finrank and local coordinate budget. No source declaration is unported. |
| [R9 slice 3: weighted-support interpolation and finite-field list bound (#1070)](https://github.com/Verified-zkEVM/ArkLib/pull/1070) | `ArkLib/Data/CodingTheory/ReedSolomon/ListDecodability/Capacity/WeightedSupportInterpolant.lean`: `exists_weightedSupport_interpolant_of_fixed_margin`, `exists_prescribed_weightedSupport_construction_core`, `exists_prescribed_weightedSupport_construction`, `HiddenDerivativeInterpolationCertificate.agreeingPolynomials_encard_le_totalJetDegree` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Symbolic.Soundness`; `ArkLib.Data.CodingTheory.ReedSolomon.ListDecodability.Capacity.WeightedSupportInterpolant`; `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Symbolic.WeightedSupport`; `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Local.ConstraintMap`; acceptance cases in the matching `ArkLibTest` files | Generalized the symbolic construction to the canonical weighted-support and local-constraint APIs and specialized it at prescribed capacity parameters; the extension-field list bound is `4 * m * q ^ (e * d)`. Renamed `noBand_kernel_height_lt` to `kernel_height_lt_twelve_mul_of_margin`; represented support indices with `weightedSupportExponents` rather than retaining `WeightedSupportIndex`. `Matrix.rowBlock` and `Matrix.rank_prod_rows_le_sum` are replaced by range embedding and finite-dimensional rank bounds; `map_projectLowContact` and `map_unscaledLocalSubstitution` come from the existing local-constraint API. Differential-specialization agreement lemmas are outside the unit's main statements; the list bound uses the existing solution embedding and bounded-solution root count. |
| [P7a slice 22: agreements from regular Reed–Solomon components (#1071)](https://github.com/Verified-zkEVM/ArkLib/pull/1071) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/TaylorChart/ComponentAgreement.lean`: `symbolicSourceAgreement_eq_zero_iff_of_exponent`, `symbolicSourceAgreement_eq_zero_iff` → `aeval_jointTaylorAgreementEquation_eq_zero_iff`; `commonAgreement_of_symbolicSourceAgreement_mem_prime_of_exponent`, `commonAgreement_of_symbolicSourceAgreement_mem_prime` → `commonAgreement_of_jointTaylorAgreementEquation_mem_prime`; `exists_graphLine_pair_of_symbolic_prime_agreements_of_exponent`, `exists_graphLine_pair_of_symbolic_prime_agreements` → `exists_graphLine_pair_of_regular_component_agreements` | `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.GraphLineComponent`; acceptance cases in the matching `ArkLibTest` files | Combined the exponent-specific and default forms into statements for any sufficient exponent `τ`, using the joint chart API and current affine-pair curve parametrization. The component theorem retains the regular-locus parametrization and restriction identities with the common-agreement bound. No source declaration is unported; the default-exponent declarations are covered by the exponent-general statements and have no separate wrappers. |
| [P7b slice 9: uniform first-order shifted-slot certificate (#1072)](https://github.com/Verified-zkEVM/ArkLib/pull/1072) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/FirstOrder/CurveHeightCounting.lean`: `firstOrderCurveShiftedRowSlotBound_le_of_rankBound`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Parameters/FirstOrder/Uniform.lean`: `uniformFirstOrderGradedRankProfile`, `firstOrderGradedRankBound_le_uniformFirstOrderProfile`, `uniformFirstOrderHeightWeightSum`, `uniformFirstOrderHeightTotalDegreeSum`, `uniformFirstOrderHeightFirstJetSum`, `uniformFirstOrderRowWeightSum`, `uniformFirstOrderHeightWeightSum_eq`, `uniformFirstOrderHeightTotalDegreeSum_eq`, `uniformFirstOrderHeightFirstJetSum_eq`, `uniformFirstOrderRowWeightSum_eq`, `uniformFirstOrder_parameters` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.FirstOrder.CurveHeightCounting`; `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.FirstOrder.Uniform`; acceptance cases in the matching `ArkLibTest` files | Moved the uniform declarations into the `ReedSolomon.HiddenDerivative` namespace; their names otherwise stay the same. Generalized `uniformFirstOrder_parameters` by dropping the unnecessary `2 ≤ n` and `A ≤ n` premises; `2 ≤ k` and `25 * k + 6 * n ≤ 25 * A` suffice. The fixed profile values, sum, and weighted sum remain acceptance examples because they have no production consumers. |
| [P7a slice 23: prime Taylor components for power-batched tuples (#1073)](https://github.com/Verified-zkEVM/ArkLib/pull/1073) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/PolynomialCurve/ComponentRecognition.lean`: `exists_polynomialGraph_of_symbolic_prime_sample_of_exponent` → `exists_polynomialGraph_of_primeTaylorComponent`, `powerBatchedJetGraphMap` (new), `eval_jointInitialJetSeparant` → `aeval_jointInitialJetSeparant`, `eval_jointCommonTaylorNumerator` → `aeval_jointCommonTaylorNumerator`, `eval_jointTaylorAgreementEquation` → `aeval_jointTaylorAgreementEquation`, `regular_principalOpen_graph_restriction` (new); `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/PolynomialCurve/ExceptionalSet.lean`: `exists_exceptional_exactPowerAgreement`, `exists_exceptional_exactPowerAgreement_family` | `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.PowerBatchedComponentRecognition`; `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.PowerBatchedPointRecognition`; `ArkLib.Data.Polynomial.Differential.TaylorChartAlgebra`; `ArkLib.ToMathlib.RingTheory.Nullstellensatz.PrincipalOpenParametrization`; `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.GraphLineComponent`; acceptance cases in the matching `ArkLibTest` files | Renamed the prime-component theorem to use the destination joint Taylor cuts and generalized it to tuples of arbitrary length; added the graph map and shared evaluator and principal-open results. Generalized both exact power-agreement bounds from `Fin n` to any finite coordinate type. The family result reuses `exists_exceptional_powerBatched_family` on the mapped family. Did not port `symbolicSourceCurveAgreement_of_exponent` or `symbolicSourceCurveAgreement`: their cut polynomial is `jointTaylorAgreementEquation`, and the default-exponent form follows by taking exponent `2 * K`. |
| [P3 slice 20: symbolic rank acceptance bounds (#1074)](https://github.com/Verified-zkEVM/ArkLib/pull/1074) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/Symbolic/RateHeight.lean`: `kernel_height_le_ratePartitionHeight` → `kernel_height_le_marginHeight`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/Symbolic/RatePartitionMatrix.lean`: `mapped_curve_block_eq_local_matrix` → `localConstraintMatrix_map`, `monomial_local_matrix_rank_le` → `localConstraintCoordinates_rank_le_of_derivative_weight`, `finiteConstraintMatrix_rank_le_partition` → `rank_map_supportedLocalConstraintMatrix_le_of_derivative_weight`, symbolic weighted-support `Matrix.rank_prod_rows_le_sum` → generic `Matrix.rank_prod_rows_le_sum` | `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.RatePartition.BlockLength`; `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Symbolic.PartitionRank`; `ArkLib.ToMathlib.LinearAlgebra.Matrix.Rank`; acceptance cases in the matching `ArkLibTest` files | Adapted `kernel_height_le_ratePartitionHeight` to the current `marginHeight` API. Generalized the algebra-map block identity to entrywise base change along any ring homomorphism, monomial columns to arbitrary finite polynomial families with bounded support weight, and the symbolic weighted-support product-row rank lemma to arbitrary product-row matrices, moving it to the generic matrix-rank owner. Adapted the global bound to the existing supported-row matrix and `localDerivativeCoordinateBudget`. The strict quotient estimate is included in `kernel_height_le_marginHeight`; the block-only wrapper, monomial-only rank theorem, and `Matrix.rowBlock` helper were not ported because the generalized theorems cover or inline them. |
| [P7a slice 24: derivative-variable Taylor numerator degree bounds (#1075)](https://github.com/Verified-zkEVM/ArkLib/pull/1075) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/RootFinding/Symbolic/TaylorDerivativeDegree.lean`: `degreeOf_clearedSubstitution` → same name, `degreeOf_initialJetSeparantOver_firstOrder` → `degreeOf_initialJetSeparant_le`, `degreeOf_rationalTaylorNumeratorOver_firstOrder` → same name, `degreeOf_commonTaylorNumeratorOver_firstOrder` → same name | `ArkLib.Data.MvPolynomial.WeightedDegree`; `ArkLib.ToMathlib.MvPolynomial.ClearedSubstitution`; `ArkLib.Data.Polynomial.Differential.RationalTaylorDerivativeDegree`; acceptance cases in the matching `ArkLibTest` files | Generalized `degreeOf_clearedSubstitution` from `CommRing` to `CommSemiring` and derived it from a shared weighted-degree bound; generalized the separant estimate from fields to nontrivial commutative semirings. Added arbitrary-order rational and common Taylor numerator bounds over fields, retaining the first-order corollaries. `flattenChallenge_degreeOf_le` has no wrapper because the weighted-degree identities already give the coordinate-degree identity; removed the redundant `degreeOf_optionEquivRight_symm_le` declaration and its constant-coefficient test. |
| [P3 slice 21: partition-support curve certificates (#1080)](https://github.com/Verified-zkEVM/ArkLib/pull/1080) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/RatePartition/Certificate.lean`: `ratePartitionColumns` → `partitionSupportColumns`, `ratePartitionColumns_exponent` → `partitionSupportColumns_exponent`, `ratePartitionColumns_injective` → `partitionSupportColumns_injective`, `ratePartitionColumns_eligible` → `partitionSupportColumns_eligible`, `exists_ratePartition_certificate` → `exists_partitionSupport_curve_certificate` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Symbolic.SourceColumn`; `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Symbolic.WeightedSupport`; `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.FirstOrder.HeightCounting`; `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.PartitionSupport.CurveCertificate`; acceptance cases in the matching `ArkLibTest` files | Added reusable `SourceColumn.enumerate` exponent, injectivity, and membership APIs; weighted-support and first-order column declarations retain their names and statements and use the helper. Adapted the certificate to `PartitionSupportEligible`, `localDerivativeCoordinateBudget`, and `SymbolicReceivedCurve.Certificate`, preserving the exact natural-number challenge-height formula. The helper bounds `monomial_local_matrix_rank_le` and `finiteConstraintMatrix_rank_le_partition` were not copied because `main` has more general local-coordinate and supported-matrix rank theorems. |
| [P9 slice 28: weighted-support capacity list bounds (#1078)](https://github.com/Verified-zkEVM/ArkLib/pull/1078) | `ArkLib/Data/CodingTheory/ReedSolomon/ListDecodability/Capacity/WeightedSupport.lean`: `exists_weightedSupport_hiddenDerivativeConstruction`, `weightedSupport_capacity_list_bound_four_mul`, `weightedSupport_capacity_list_bound` | `ArkLib.Data.CodingTheory.ListDecodability.Capacity.WeightedSupport`; acceptance cases in the matching `ArkLibTest` files | The prescribed weighted-support parameters instantiate `WeightedSupportConstruction` through the existing prescribed interpolation result. The pointwise bound has prefactor `4m` and exponent `2d`, improving to `d` under the large-field condition; the packaged theorem supplies `WeightedSupportListBound`. The conclusions keep their names and mathematical statements, using the destination's order-indexed multiplicity API. Added the generic `CapacityGapCertificate.ofPointwiseBound` helper in the capacity owner; it has no source declaration and constructs an exact decoder certificate over arbitrary finite coordinate types. Did not add `weightedSupportMultiplicity_pos`, since `weightedSupportMultiplicity_pos_iff` and `capacityDerivativeOrder_lower` cover it. |

| [P7a slice 18: rational Taylor cut bidegree bounds (#1050)](https://github.com/Verified-zkEVM/ArkLib/pull/1050) | `ArkLib/ToMathlib/AlgebraicGeometry/Hilbert/Bidegree.lean`: `source_mem_restrictBidegree_mono`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/RootFinding/Symbolic/TaylorCutDegree.lean`: `challengeHeightLE_clearedSubstitution`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/RootFinding/Symbolic/TaylorBidegree.lean`: `flattenChallenge_challengeDegree_le`, `flattenChallenge_mem_restrictBidegree`, `challengeHeightLE_rationalTaylorNumeratorOver`, `challengeHeightLE_commonTaylorNumeratorOver_of_exponent`, `totalDegree_rationalTaylorNumeratorOver_le_of_jet`, `totalDegree_commonTaylorNumeratorOver_le_of_jet_and_exponent`, `initialJetEquationOver_mem_restrictBidegree`, `initialJetSeparantOver_mem_restrictBidegree`, `commonTaylorNumeratorOver_mem_restrictBidegree` (exponent and default-exponent statements), `taylorAgreementEquationOver_mem_restrictBidegree` (exponent and default-exponent statements) | `ArkLib.Data.Polynomial.Differential.RationalTaylorBidegree`; `ArkLib.ToMathlib.MvPolynomial.PolynomialCoefficients`; `ArkLib.ToMathlib.RingTheory.MvPolynomial.Bidegree`; acceptance cases in the matching `ArkLibTest` files | Renamed `source_mem_restrictBidegree_mono` to `mem_restrictBidegree_mono` and generalized to arbitrary `MvPolynomial` coefficients. Renamed the cleared-substitution and coefficient-degree bounds to use `CoeffNatDegreeLE`; generalized cleared substitution to commutative semirings and made it reusable in the coefficient-degree owner. Renamed `flattenChallenge_challengeDegree_le` to `weightedTotalDegree_optionEquivRight_symm_coefficientDegree_le`, and generalized the flattening bounds to arbitrary variable types and nontrivial commutative semirings. Rephrased jet-degree bounds using `jetTotalDegree`; the initial equation and separant bounds use the current APIs. Replaced exponent and default-exponent bidegree statements with explicit-exponent theorems and sufficiency proofs. Default-exponent wrappers are covered by explicit-exponent theorems using `taylorExponentSufficient_two_mul`; `flattenChallenge_jetDegree_le` is covered by `MvPolynomial.totalDegree_optionEquivRight`. The agreement-equation definition is supplied by main and retained unchanged; no source theorem remains blocked. |
| [P7b slice 6: first-order curve-rank profiles and symbolic certificates (#1055)](https://github.com/Verified-zkEVM/ArkLib/pull/1055) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/FirstOrder/CurveRank.lean`: `FirstOrderCurveGradedRowIndex`, `firstOrderCurveGradedRowLocalIndex`, `firstOrderCurveGradedConstraintMatrix`, `firstOrderCurveGradedConstraintMatrix_mulVec_eq_coeff`, `firstOrderCurveGradedConstraintMatrix_kernel_iff`, `firstOrderCurveGradedConstraintMatrix_degree_le`, `firstOrderCurveGradedConstraintMatrix_eq_zero_of_grade_lt`, `firstOrderCurveGradedFinMatrix`, `firstOrderCurveGradedFinMatrix_kernel_iff`, `firstOrderCurveGradedFinRowWeight`, `firstOrderCurveGradedRowWeight`, `firstOrderCurveGradedFinMatrix_degree_le`, `firstOrderCurveGradedFinMatrix_eq_zero_of_weight_lt`, `firstOrderGradedSourceCount`, `firstOrderGradedSourceColumn`, `firstOrderGradedSourceColumn_x`, `firstOrderGradedSourceColumn_y₀`, `firstOrderGradedSourceColumn_higher`, `firstOrderGradedSourceColumn_injective`, `firstOrderGradedSourceColumn_bounds`, `firstOrderGradedSourceColumn_totalJetDegree`, `firstOrderGradedSourceColumn_x_add_y₀`, `firstOrderGradedSourceColumn_mem`, `firstOrderGradedSourceColumnIndex`, `firstOrderGradedSourceColumn_index_eq`, `firstOrderGradedTargetExponent`, `firstOrderGradedTargetExponent_of_local`, `firstOrderGradedTargetExponent_localT`, `firstOrderGradedTargetLowContactIndex`, `localJetDegree_one` → `weight_localJetDegreeWeight_one_eq`, `localJetDegree_firstOrderGradedTargetExponent` → `weight_localJetDegreeWeight_one_firstOrderGradedTargetExponent`, `localContactOrder_one`, `localContactOrder_firstOrderGradedTargetExponent`, `localConstraintAt_zero_sourceMonomial_isSliceHomogeneous`, `coeff_localConstraintAt_zero_sourceMonomial_eq_zero_of_T_degree_ne`, `firstOrderOriginGradedSliceMatrixOver`, `firstOrderOriginGradedSliceMatrix_map`, `firstOrderOriginGradedSliceMatrix_mulVec_coeff`, `firstOrderOriginGradedSliceMatrix_rank_le`, `firstOrderOriginGradedSelectedRow`, `firstOrderOriginGradedSelectedRow_span`, `firstOrderOriginGradedBaseSelectedMatrix`, `firstOrderOriginGradedBaseSelectedMatrix_mulVec_eq_zero_iff`, `firstOrderOriginGradedBaseSelectedMatrix_mulVec_coeff`, `firstOrderOriginGradedSelectedCoefficients_eq_zero_iff`, `firstOrderOriginGradedRank`, `firstOrderGradedRankBound`, `firstOrderOriginGradedRank_le_bound`, `globalPointTranslation_mem_firstOrderSpace`, `firstOrder_curve_matrix_rank_le` → `rank_firstOrderLocalConstraintMatrix_le`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/FirstOrder/Symbolic.lean`: `FirstOrderSymbolicCertificate`, `coeff_interpolant_natDegree_le` → `SourceColumn.coeff_interpolant_natDegree_le`, `interpolant_mem_firstOrderSpace` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.FirstOrder.CurveRank`; `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.FirstOrder.Symbolic`; `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.FirstOrder.SymbolicRank`; `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Symbolic.SourceColumn`; acceptance cases in the matching `ArkLibTest` files | Generalized the local rank theorem from received lines and `Fin n` to arbitrary received polynomials and finite point and column types, and generalized coefficient-height preservation to every derivative order and commutative semirings. Strengthened the finite-matrix degree bound by removing an unused weight hypothesis and generalized its zero-entry bound by removing an unused received-curve degree bound; generalized `interpolant_mem_firstOrderSpace` from fields to commutative semirings. Omitted the compressed-matrix interface, unused row count, redundant rank alias, fixed-argument weight aliases, unused exponent/basis lemmas, and `firstOrderColumns` API as described in the port report; `map_localConstraintAt` is owned by `Local.ConstraintMap`, and the exact interpolation weight exponent follows from `SourceColumn.weight_exponent`. |
| [P7a slice 21: exceptional challenges for jet-independent content (#1061)](https://github.com/Verified-zkEVM/ArkLib/pull/1061) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/Ordinary/Factors/RootPresentation.lean`: `ordinaryRootPresentation`, `natDegree_ordinaryRootPresentation`, `ordinaryRootPresentation_ne_zero`, `irreducible_ordinaryRootPresentation`, `derivative_ordinaryRootPresentation`, `ordinaryRootPresentation_monomial`, `degreeX_ordinaryRootPresentation_le`, `eval_ordinaryRootPresentation`; `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/Ordinary/Factors/ContentExceptions.lean`: `exists_exceptional_ordinaryContent` → `exists_exceptional_jet_independent_content` | `ArkLib.Data.Polynomial.Differential.RootPresentation`; `ArkLib.Data.Polynomial.Differential.ContentExceptions`; acceptance cases in the matching `ArkLibTest` files | Generalized the root-presentation declarations from fields to commutative rings and the exceptional-content theorem from fields to commutative integral domains, using `MvPolynomial.CoeffNatDegreeLE` for coefficient height. The resultant and separant bounds retain their field assumptions. The acceptance case uses a height-one nonconstant coefficient equation vanishing at zero and checks that zero lies in an exceptional set of cardinality at most one. Nothing was deferred or left unported. |
| [P9 slice 26: uniform finite-parameter rate envelope (#1062)](https://github.com/Verified-zkEVM/ArkLib/pull/1062) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Parameters/RatePartition/UniformEnvelope.lean`: `UniformRatePartitionEnvelope`, `exists_uniformRatePartitionEnvelope` | `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.RatePartition.UniformEnvelope`; acceptance cases in the matching `ArkLibTest` files | Renamed both declarations into the `RatePartition` namespace without changing their names. The gap determines the derivative order, multiplicity, and block threshold, while ambient degree and scalar parameters may depend on the message dimension; existence supplies parameters for the actual message dimension and agreement count. No declarations from the designated source file were left unported. The generalized Gamma bounds and uniform-order specializations are supplied by `UniformGamma`; the proof uses the existing `rateGamma_eq_exponential` identity, so the redundant branch-local `rateGamma_eq_exp` alias was removed. |
| [P7a slice 20: Taylor-chart and component graph-line recognition (#1058)](https://github.com/Verified-zkEVM/ArkLib/pull/1058) | `ArkLib/Data/CodingTheory/ReedSolomon/MutualCorrelatedAgreement/TaylorChart/ComponentRecognition.lean`: `symbolicSourceInitialEquation` → `jointInitialJetEquation`, `symbolicSourceSeparant` → `jointInitialJetSeparant`, `symbolicSourceNumerator` → `jointCommonTaylorNumerator`, `symbolicSourceAgreement` and `symbolicSourcePolynomialAgreement` → `jointTaylorAgreementEquation`, `symbolicSourceReconstructionError` → `jointTaylorReconstructionError`, `exists_graphLine_pair_of_symbolic_sample_of_exponent`, `exists_graphLine_pair_of_symbolic_prime_sample_of_exponent`; `ArkLib/ToMathlib/AlgebraicGeometry/Incidence/GraphPullback.lean`: `aeval_map_optionEquivRight`, `aeval_optionEquivRight_symm` | `ArkLib.Data.Polynomial.Differential.TaylorChartAlgebra`; `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.TaylorChart.PointRecognition`; `ArkLib.Data.CodingTheory.ReedSolomon.MutualCorrelatedAgreement.GraphLineComponent`; `ArkLib.ToMathlib.MvPolynomial.PolynomialCoefficients`; acceptance cases in the matching `ArkLibTest` files | Moved the generic chart equations and affine-pair curve to `PolynomialDifferential`; generalized the evaluation bridges from fields to commutative semiring algebras. The exponent-aware point theorem keeps its name in the canonical point-recognition owner; added joint-chart recognition and renamed/reformulated prime-sample exponent recognition as `exists_graphLine_pair_of_regular_component`. Added `quadraticJetSampleEquation` and its high-numerator identity as shared test fixtures. `exists_graphLine_pair_of_symbolic_prime_sample` is not added as a default-exponent wrapper because the regular-component theorem accepts any sufficient exponent; `symbolicSourcePolynomialAgreement` is covered by `jointTaylorAgreementEquation`. |
| [P3 slice 18: finite partition-ratio surplus bound (#1060)](https://github.com/Verified-zkEVM/ArkLib/pull/1060) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/RatePartition/Ratio.lean`: `ratePartition_dimension_gt_ratio_of_moment` → `partitionSupport_finiteRatio_surplus` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.PartitionSupport.FiniteSurplus`; acceptance cases in the matching `ArkLibTest` files | Generalized to the current partition-support space and coordinate-budget APIs; omitted the redundant `0 < n` hypothesis because it follows from the other hypotheses. The floor bound supplies the moment rescaling condition, and the finite ratio cancels the exponential coordinate-budget envelope. No source declaration is unported. |
| [P7b slice 7: symbolic curve certificates and rank bounds (#1063)](https://github.com/Verified-zkEVM/ArkLib/pull/1063) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/Symbolic/CurveStages.lean`: `Certificate`, `Certificate.Q`, `Certificate.challengeHeight_le` → `Certificate.challengeDegree_le`, `Certificate.totalJetDegree_le`, `Certificate.specialization_sound`, `Certificate.nonzero`, `Certificate.jetWeight_le` → `Certificate.jetTotalDegree_le`, `Certificate.exists_separant_chain` → `Certificate.exists_separantChain`, `Certificate.exists_exceptional_stage_coverage`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/Symbolic/RankCertificate.lean`: `exists_certificate_of_monomial_rank_bound`, `exists_certificate_of_rank_bound`, `interpolant_totalJetDegree_le`, `map_interpolant_jetTotalDegree_le` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Symbolic.CurveCertificate`; `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Symbolic.SourceColumn`; `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Local.ConstraintMap`; acceptance cases in the matching `ArkLibTest` files | Generalized point and column indices to arbitrary finite types, and rank transport to injective ring homomorphisms into any field. Renamed `challengeHeight_le` to `challengeDegree_le`, `jetWeight_le` to `jetTotalDegree_le`, and `exists_separant_chain` to `exists_separantChain`; moved the interpolant jet-degree bounds to `SourceColumn` and generalized coefficient maps to commutative semirings. Separate `challengeHeight_le` and `jetWeight_le` aliases were not added; `Matrix.kernel_height_lt_div_margin` is deferred to the RateHeight port. Source support-certificate and jet-prefix imports were replaced by existing received-curve, source-column, and separant-chain APIs. |
| [P5 slice 22: capped bidegree hybrid incidence bound (#1064)](https://github.com/Verified-zkEVM/ArkLib/pull/1064) | `ArkLib/ToMathlib/AlgebraicGeometry/Incidence/DerivativeBidegreeExcluded.lean`: `derivativeBidegreeHypersurface_source_incidence_off_excluded_hybrid_two_of_lt`, `derivativeBidegreeHypersurface_source_incidence_off_excluded_hybrid_two` → `MvPolynomial.cappedBidegreeHypersurface_incidence_off_excluded_hybrid_two` | `ArkLib.ToMathlib.RingTheory.Nullstellensatz.CappedBidegreeIncidence`; acceptance cases in the matching `ArkLibTest` files | Generalized to every positive `c` and dropped the assumptions `r ≤ j` and `A ≤ n`. Both source theorems are covered by the generalized declaration; no source declaration is unported. |
| [P7b slice 8: shifted first-order curve height interpolation (#1066)](https://github.com/Verified-zkEVM/ArkLib/pull/1066) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/FirstOrder/CurveHeightCounting.lean`: `firstOrderExponentDimensionIndex_totalJetDegree` → `firstOrderCoordinatesEquiv_totalJetDegree`, `firstOrderCurveShiftedColumnSlotCount`, `firstOrderCurveShiftedHeightSlotCount`, `firstOrderCurveShiftedColumnSlotCount_eq_heightSlotCount`, `firstOrderCurveShiftedRowSlotCount`, `firstOrderCurveShiftedRowSlotBound`, `firstOrderCurveShiftedRowSlotCount_le_bound`, `sum_firstOrderCurveGradedRowIndex_slots`, `sum_firstOrderCurveGradedFinRowWeight_slots`, `sum_firstOrderColumns_shifted_slots`, `FirstOrderCurveShiftedHeightSurplus`, `firstOrderTotalJetWeight`, `firstOrderGradedRowWeight`, `firstOrderCurveShiftedColumnSlotCount_add_weight`, `firstOrderCurveShiftedRowSlotCount_add_weight`, `exists_primitive_firstOrderCurve_interpolant_of_shifted_height`, `exists_primitive_firstOrderCurve_interpolant_of_shifted_height_bound`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Interpolation/Symbolic/CurveColumnHeight.lean`: `CurveColumnHeight.exists_primitive_interpolant_of_shifted_height` → `ReedSolomon.HiddenDerivative.exists_primitive_interpolant_of_shifted_height` | `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.FirstOrder.CurveHeightCounting`; `ArkLib.Data.CodingTheory.HiddenDerivative.Interpolation.Symbolic.ColumnHeight`; acceptance cases in the matching `ArkLibTest` files | Generalized the generic shifted-surplus constructor to per-column total-jet-degree shifts in the symbolic column-height owner. Adapted the total-jet-degree identity to the existing support-coordinate equivalence and used `firstOrderOriginGradedRank` in place of `firstOrderGradedRank`. The first-order declarations otherwise keep their names. Omitted `firstOrderCurveShiftedRowSlotBound_eq_sumRangeFrom` and `firstOrderCurveShiftedHeightSlotCount_eq_sumRangeFrom` because they only unfold `Finset.sumRangeFrom` at zero and have no consumers; omitted `firstOrderCurveShiftedRowSlotBound_le_of_rankBound` because it has no consumer and a needed profile comparison follows from finite-sum monotonicity. |
| [P9 slice 27: shared rate-partition envelopes across multiplicities (#1067)](https://github.com/Verified-zkEVM/ArkLib/pull/1067) | `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Parameters/RatePartition/UniformEnvelope.lean`: `UniformRatePartitionEnvelope` → `RatePartitionEnvelope`, `exists_uniformRatePartitionEnvelope`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Parameters/RatePartition/MathematicalUniform.lean`: `MathematicalRatePartitionEnvelope` → `RatePartitionEnvelope`, `uniformRatePartitionMathematicalMultiplicity` → `uniformMathematicalMultiplicity`, `uniformRatePartitionMathematicalJetBound` → `uniformMathematicalJetBound`, `uniformRatePartitionMathematicalLength` → `uniformMathematicalLength`, `uniformRatePartitionMathematicalLength_eq_ceil` → `uniformMathematicalLength_eq_ceil`, `uniformRatePartitionOrder_ge_519` → `uniformDerivativeOrder_ge_519`, `uniformRatePartitionOrder_le_mathematicalJetBound` → `uniformDerivativeOrder_le_mathematicalJetBound`, `uniformRatePartitionMathematical_integer_guards` → `uniformMathematical_integer_guards`, `uniformRatePartitionMathematical_totalJetDegree_le` → `uniformMathematical_totalJetDegree_le`, `uniformRatePartitionMathematical_low_ratio_gt` → `uniformMathematical_low_ratio_gt`, `uniformRatePartitionMathematical_high_ratio_gt` → `uniformMathematical_high_ratio_gt`, `exists_mathematicalRatePartitionEnvelope`; `ArkLib/Data/CodingTheory/ReedSolomon/HiddenDerivative/Parameters/RatePartition/UniformGamma.lean`: `log_forty_ninths_lt_d9` | `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.RatePartition.MathematicalUniform`; `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.RatePartition.UniformEnvelope`; `ArkLib.Data.CodingTheory.HiddenDerivative.Parameters.RatePartition.UniformGamma`; acceptance cases in the matching `ArkLibTest` files | Generalized `RatePartitionEnvelope` with an explicit interpolation multiplicity and factored the common branch construction into `exists_ratePartitionEnvelope`; the scale-1000 and scale-300 existence theorems specialize it. Renamed the listed mathematical declarations. Added the public `log_forty_ninths_lt_d9` bound for the scale-300 margin. `uniformCapacityLengthThreshold300` is deferred because this unit has no consumer; `ratePartitionMathematicalMultiplicity_ge_order` is covered by the stronger generic `add_two_le_closedMultiplicity`. |

## Exact declaration inventory

The P9 discrete-simplex unit owns `Finset.natWeightedSimplex`,
`Finset.weightedSum_le_of_mem_natWeightedSimplex`, `Finset.mem_natWeightedSimplex`,
`Finset.card_natWeightedSimplex_one`, `Finset.choose_le_card_natWeightedSimplex_mul_prod`,
`Finset.card_natWeightedSimplex_mul_prod_le_choose`,
`Finset.succ_pow_le_factorial_mul_prod_mul_card_natWeightedSimplex`,
`Finset.factorial_mul_prod_mul_card_natWeightedSimplex_le`,
`Finset.card_natWeightedSimplex_le`, and `Finset.natWeightedSimplex_succ_sandwich`.
The source revision for this unit is `a5aa2677fee4e3a79d6bb05136631cce4a08587d`.

The P9 exact-simplex and shell unit owns `Finset.natWeightedSimplexShell`,
`Finset.mem_natWeightedSimplexShell`, `Finset.weightedSum_equivFunOnFinite`,
`Finset.natWeightedSimplexFinsuppEquiv`, `Finset.natWeightedSimplexShellFinsuppEquiv`,
`Finset.card_finsupp_weight_le_eq_card_natWeightedSimplex`,
`Finset.card_finsupp_weight_eq_card_natWeightedSimplexShell`,
`Finset.card_natWeightedSimplex_eq_sum_card_shell`, and
`Finset.natWeightedSimplexOneEquivExact`. Its source revision is
`a5aa2677fee4e3a79d6bb05136631cce4a08587d`.

The weighted-support unit owns `restrictWeightedDegree`, `mem_restrictWeightedDegree`,
`mem_restrictWeightedDegree_iff_weightedTotalDegree_le`, `restrictWeightedDegree_mono`,
`restrictWeightedDegree_one`, `mem_restrictDegree_iff_forall_mem_restrictWeightedDegree_piSingle`,
`monomial_mem_restrictWeightedDegree`, `C_mem_restrictWeightedDegree`,
`X_mem_restrictWeightedDegree`, `mul_mem_restrictWeightedDegree`,
`pow_mem_restrictWeightedDegree`, `weightedTotalDegree_mul_le`, `weightedTotalDegree_C`,
`weightedTotalDegree_monomial`, `weightedTotalDegree_pow_le`, `weightedTotalDegree_bind₁_le`,
`weightedTotalDegree_bind₁_le_of_le`, `weightedTotalDegree_aeval_le`,
`weightedTotalDegree_aeval_le_of_le`, `bind₁_mem_restrictWeightedDegree`,
`aeval_mem_restrictWeightedDegree`, `bind₁_comp_mem_restrictWeightedDegree`,
`X_pow_mem_restrictWeightedDegree_zero`, `eq_zero_of_mem_restrictWeightedDegree_zero`,
`eq_C_coeff_zero_of_mem_restrictWeightedDegree_zero`, `weightedDegreeZeroSubalgebra`,
`mem_weightedDegreeZeroSubalgebra`, `basisRestrictWeightedDegree`,
`basisRestrictWeightedDegree_repr_apply`, `restrictWeightedDegreeCoeff`,
`restrictWeightedDegreeCoeff_apply`, `coeff_eq_zero_of_mem_restrictWeightedDegree`,
`weightedDegreeSupport_finite`, `restrictWeightedDegree_fg`, and `finrank_restrictWeightedDegree`.

The P1 partial-derivative unit owns `natCast_ne_zero_of_ringChar_eq_zero_or_lt`,
`MvPolynomial.degreeOf_pderiv_le_sub_one`, `MvPolynomial.degreeOf_pderiv_le`,
`MvPolynomial.coeff_pderiv_sub_single_one`, `MvPolynomial.pderiv_ne_zero_of_natCast_ne_zero`,
`MvPolynomial.degreeOf_pderiv_eq_sub_one_of_natCast_ne_zero`,
`MvPolynomial.degreeOf_iterate_pderiv_le_sub`, `MvPolynomial.degreeOf_iterate_pderiv_le`,
`MvPolynomial.pderiv_eq_zero_of_degreeOf_eq_zero`,
`MvPolynomial.iterate_pderiv_eq_zero_of_degreeOf_lt`,
`MvPolynomial.degreeOf_iterate_pderiv_eq_sub_of_natCast_ne_zero`,
`MvPolynomial.iterate_pderiv_ne_zero_of_natCast_ne_zero`,
`MvPolynomial.weightedTotalDegree_pderiv_le_sub`, `MvPolynomial.weightedTotalDegree_pderiv_le`,
`MvPolynomial.pderiv_mem_restrictWeightedDegree`,
`MvPolynomial.natDegree_aeval_le_weightedTotalDegree`, and
`MvPolynomial.natDegree_aeval_le_weightedTotalDegree_of_le`.
The source revision for this unit is `a5aa2677fee4e3a79d6bb05136631cce4a08587d`.

The P1 differential-polynomial core owns `PolynomialDifferential.JetVariable`,
`DifferentialPolynomial`, `differentialSpecializationHom`, `differentialSpecialization`,
`differentialSpecializationHom_apply`, `differentialSpecialization_C`,
`differentialSpecialization_x`, `differentialSpecialization_jet`, `jetEvaluation`,
`polynomialJet`, `eval_differentialSpecialization`, `jetDegree`, `separant`,
`differentialWeight`, `differentialWeight_none`, `differentialWeight_some`,
`differentialWeight_some_pos_of_order_lt_degree`, `differentialWeight_top_eq_zero`,
`differentialWeightedDegree`, `differentialWeightedDegree_map_eq`,
`natDegree_differentialVariable_le`, `natDegree_differentialSpecialization_le`,
`natDegree_differentialSpecialization_separant_le_sub`,
`natDegree_differentialSpecialization_separant_le`, `jetDegreeWeight`,
`jetDegreeWeight_none`, `jetDegreeWeight_some`, `totalJetDegree`, `totalJetDegree_eq_sum`,
`totalJetDegree_eq_degree_some`, `jetTotalDegree`, `jetTotalDegree_le_iff`,
`jetDegree_le_total`, and `separant_total_le`. The source revision for this unit is
`a5aa2677fee4e3a79d6bb05136631cce4a08587d`.

The P1 finite-Taylor-prefix unit owns `Polynomial.centeredCoefficientPrefix`,
`taylor_centeredCoefficientPrefix`, `coeff_taylor_centeredCoefficientPrefix`,
`hasseCoeffAt_centeredCoefficientPrefix`, `degree_centeredCoefficientPrefix_lt`,
`centeredCoefficientPrefix_mem_degreeLT`, `hasseJet_centeredCoefficientPrefix`,
`hasseJet_centeredCoefficientPrefix_of_le`, `centeredCoefficientPrefix_zero`, and
`centeredCoefficientPrefix_succ`. The source revision for this unit is
`a5aa2677fee4e3a79d6bb05136631cce4a08587d`.

The exact-list unit owns the generic `Finset.Enumeration`, `Finset.IsExactEnumeration`,
`Finset.EnumerationCertificate`, `Finset.CandidateCertificate`, `Finset.filterCandidates`,
and their membership, cardinality, exactness, soundness, completeness, and empty-enumeration laws.
Its Reed–Solomon adapter owns `MessagePolynomial`, `Decoder`, `IsExactDecoder`,
`DecoderCertificate`, `CandidateCertificate`, `messagePolynomialEmbedding`,
`messagePolynomialValue`, `messagePolynomialValue_apply`,
`CandidateCertificate.filteredDecoder`, `CandidateCertificate.mem_filteredDecoder`,
`CandidateCertificate.filteredDecoder_isExact`, `CandidateCertificate.filteredDecoder_card_le`,
`CandidateCertificate.toDecoderCertificate`, `DecoderCertificate.agreement_le_of_mem`,
`DecoderCertificate.mem_of_agreement_le`, `IsExactDecoder.decoder_eq_empty_of_card_lt`, and
`DecoderCertificate.decoder_eq_empty_of_card_lt`.

The P5 double-counting unit owns `Finset.card_bipartiteAbove_sub_card_le_card_bipartiteAbove_sdiff`,
`Finset.card_mul_sub_card_le_sum_card_bipartiteBelow_sdiff`,
`Finset.card_mul_sub_add_one_le_sum_card_bipartiteBelow_sdiff`,
`Finset.card_mul_sub_card_le_sum_compl_card_bipartiteBelow`, and
`Finset.card_mul_sub_add_one_le_sum_compl_card_bipartiteBelow`.
The source revision for this unit is `a5aa2677fee4e3a79d6bb05136631cce4a08587d`.

The row-basis and kernel-height slice owns `Matrix.exists_rows_linearIndependent_span_eq`,
`Matrix.exists_ne_zero_mulVec_eq_zero_degreeLT`, and
`Matrix.exists_ne_zero_mulVec_eq_zero_natDegree_le`. The submodule-avoidance slice owns
`Submodule.exists_forall_notMem_of_card_le`.

The generic interleaving slice owns `Code.projectedWord_rowCombination_mem`,
`Code.goodRowFunctionals`, `Code.mem_goodRowFunctionals_iff`,
`Code.goodRowFunctionals_eq_top_iff`, and `Code.exists_rowFunctional_forall_notMem` in
`ArkLib.Data.CodingTheory.InterleavedCode.Projection`. Its generator layer owns
`CoreDefinitions.exists_forall_isMCA_of_forall_isMCA_interleaved`,
`CoreDefinitions.mcaError_moduleInterleavedCode_le_of_card_le`,
`CoreDefinitions.mcaError_le_mcaError_moduleInterleavedCode`, and
`CoreDefinitions.mcaError_moduleInterleavedCode_eq_of_card_le` in
`ArkLib.Data.CodingTheory.ProximityGenerator.Interleaving`.

The intrinsic-rank and primitive-kernel slice owns
`Matrix.exists_rows_submatrix_mulVec_eq_zero_iff`, `Finset.span_gcd`, `Ideal.span_range_eq_top_iff_univ_gcd_eq_one`,
`Ideal.comp_ne_zero_of_span_range_eq_top`, `Matrix.exists_primitive_kernel_vector_eq_smul`,
`Polynomial.natDegree_le_of_mem_degreeLT_succ`, `Polynomial.mem_degreeLT_of_mul_left`,
`Matrix.exists_ne_zero_mulVec_eq_zero_degreeLT_of_rank_le`,
`Matrix.exists_ne_zero_mulVec_eq_zero_natDegree_le_of_rank_le`, and
`Matrix.exists_primitive_ne_zero_mulVec_eq_zero_degreeLT_of_rank_le`.

The P2 shifted-kernel unit owns `Polynomial.mem_degreeLT_add_one_sub_iff`,
`Matrix.exists_ne_zero_mulVec_eq_zero_shifted_degreeLT`,
`Matrix.exists_ne_zero_mulVec_eq_zero_shifted_degreeLT_of_natDegree_le`,
`Matrix.exists_primitive_ne_zero_mulVec_eq_zero_shifted_degreeLT`,
`Matrix.exists_primitive_ne_zero_mulVec_eq_zero_shifted_degreeLT_of_natDegree_le`,
`Matrix.exists_ne_zero_mulVec_eq_zero_column_degreeLT`,
`Matrix.exists_ne_zero_mulVec_eq_zero_column_degreeLT_of_rank_le`,
`Matrix.exists_primitive_ne_zero_mulVec_eq_zero_column_degreeLT_of_rank_le`, and
`Matrix.exists_primitive_kernel_vector_degreeLT`.
The source revision for this unit is `a5aa2677fee4e3a79d6bb05136631cce4a08587d`.

The resultant-specialization slice owns `Polynomial.resultant_comm_sub_one`,
`Polynomial.map_resultant_eq_zero_of_common_root`,
`Polynomial.eval_derivative_map_ne_zero_of_resultant_derivative_padded_ne_zero`,
`Polynomial.natDegree_map_eq_of_resultant_derivative_padded_ne_zero`,
`Polynomial.isCoprime_map_of_resultant_padded_ne_zero`,
`Polynomial.separable_map_of_resultant_derivative_padded_ne_zero`,
`Polynomial.card_le_natDegree_of_injOn_of_eval_eq_zero`,
`Polynomial.exists_mem_eval_ne_zero_of_card_add_natDegree_lt_card`,
`Polynomial.exists_mem_map_evalRingHom_ne_zero_of_card_add_natDegree_lt_card`,
`Polynomial.exists_map_evalRingHom_ne_zero_avoiding`, and
`Polynomial.exists_map_evalRingHom_ne_zero`.

The P5 minimal-prime unit owns `Ideal.minimalPrimesFinset`, `Ideal.mem_minimalPrimesFinset`,
`Ideal.coe_minimalPrimesFinset`, `Ideal.minimalPrimesFinset_eq_empty_iff`,
`Ideal.minimalPrimesFinset_of_isPrime`, `Ideal.retainedMinimalPrimes`,
`Ideal.mem_retainedMinimalPrimes`, `Ideal.retainedMinimalPrimes_subset`,
`Ideal.exists_mem_retainedMinimalPrimes_le`, `Ideal.retainedMinimalPrimes_eq_empty_iff`,
`Ideal.retainedMinimalPrimes_of_isUnit`, `MvPolynomial.mem_zeroLocus_iff_le_ker_aeval`,
`MvPolynomial.zeroLocus_sup`, `MvPolynomial.mem_zeroLocus_sup_span_singleton_iff`,
`MvPolynomial.exists_retainedMinimalPrime_of_mem_zeroLocus`,
`MvPolynomial.mem_zeroLocus_and_eval_ne_zero_iff_retained`, and
`MvPolynomial.mem_zeroLocus_and_cut_iff_retained`.
The source revision for this unit is `a5aa2677fee4e3a79d6bb05136631cce4a08587d`.

The P5 finite-quotient unit owns `MvPolynomial.zeroLocusPointHom`,
`MvPolynomial.zeroLocusPointHom_injective`,
`MvPolynomial.finite_zeroLocus_of_finite_quotient`, and
`MvPolynomial.ncard_zeroLocus_le_finrank_quotient`. The generic finiteness and cardinality bound
remain the pinned Mathlib declarations `Finite.algHom` and `card_algHom_le_finrank`; ArkLib does not
duplicate them. The source revision for this unit is
`a5aa2677fee4e3a79d6bb05136631cce4a08587d`.

The P5 principal-cut dimension unit owns `Ideal.ringKrullDim_quotient_succ_le_of_lt`,
`Ideal.lt_of_mem_minimalPrimes_sup_span`, and `Ideal.retained_cut_krullDim_succ_le`.
The generic dimension engine remains pinned Mathlib's `ringKrullDim_succ_le_of_surjective`;
ArkLib owns only the ideal-quotient and principal-cut adapters. The source revision for this unit
is `a5aa2677fee4e3a79d6bb05136631cce4a08587d`.
